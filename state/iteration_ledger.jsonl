{"iteration_id": "bootstrap-0", "started_at": "2026-07-25T01:24:50-0700", "finished_at": "2026-07-25T01:24:50-0700", "phase": "foundation", "actions_taken": ["initialized git repo, pnpm workspace, 15 packages", "installed dependencies; resolved global ignore-scripts=true blocking native builds", "built better-sqlite3 for both Node and Electron ABIs into separate paths", "wrote frozen spec, milestone criteria, ralph loop harness, completion verifier", "implemented shared-types and document-model with 59 passing tests", "recorded real Zotero local API fixtures"], "criteria_advanced": ["L06", "T04", "T05", "T06", "T09"], "artifacts_created": ["PROMPT.md", "loop.sh", "ralph_pretty.py", "scripts/verify_completion.py", "docs/SPEC.md", "docs/MILESTONE.md", "README.md", "THIRD_PARTY_NOTICES.md"], "errors": [], "next_action": "Implement @wr/database (T01/M03).", "git_commit": null, "state_version": 1}
{"checkpoint_id": "cp-state-resync", "iteration_hint": "post-6-commit resync", "started_at": "2026-07-25T06:18:28-0700", "finished_at": "2026-07-25T06:18:28-0700", "phase": "e2e-remaining", "actions_taken": ["pushed 6 unpushed commits to origin/main", "ran full gates: 330 vitest tests, typecheck, lint all green", "ran verify_completion.py: 61/72 checks pass", "resynced state/ which had gone 6 commits stale", "added missing test:e2e script to package.json"], "criteria_advanced": [], "artifacts_created": ["state/MILESTONE_STATUS.json (regenerated)", "state/NEXT_ACTION.md"], "errors": ["state/ had not been written since the bootstrap iteration"], "next_action": "Write tests/e2e/*.spec.ts for M01,M02,M05,M06,M07,M11,L02,L08.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-e2e-shell", "iteration_hint": "e2e wave 1", "started_at": "2026-07-25T06:25:25-0700", "finished_at": "2026-07-25T06:25:25-0700", "phase": "e2e-remaining", "actions_taken": ["added Playwright electron fixture (tests/e2e/support/app.ts)", "wrote tests/e2e/shell.spec.ts: 5 specs covering M01/M02/M05", "added @wr workspace deps to root package.json so the e2e harness resolves them", "made tsconfig project references explicit file paths so Playwright's loader resolves them", "fixed launch env: ELECTRON_RENDERER_URL must be absent, not empty, when unpackaged"], "criteria_advanced": ["M01", "M02", "M05"], "artifacts_created": ["tests/e2e/support/app.ts", "tests/e2e/shell.spec.ts"], "errors": ["firstWindow timeout traced to the dev-branch renderer URL", "navigation assertion had to move to the main process"], "next_action": "Specs for M06, M07, M11, L02, L08.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-e2e-reader-green", "iteration_hint": "reader spec repair", "started_at": "2026-07-25T11:10:00-0700", "finished_at": "2026-07-25T11:36:00-0700", "phase": "e2e-remaining", "actions_taken": ["diagnosed all three reader.spec.ts failures against the running app rather than the notes", "unwrapped the IpcResult envelope in the M06 bridge assertion (rr.invoke returns {ok,value}, not the payload)", "renamed the page-count testid pdf-page-count -> pdf-total-pages: the old name is a prefix of pdf-page-<i>, so [data-testid^=pdf-page-] counted the toolbar label as a fourth page", "rewrote readAnnotations to open the database read-only from the Playwright process; electronApplication.evaluate has no module scope so dynamic import() throws", "corrected the M11 anchor assertion to the real PdfAnchor shape (quote.exact/position/pageTextHash/contentHash) and strengthened it", "retitled the second M06 spec to what it actually asserts"], "criteria_advanced": ["M06", "M07", "M11"], "artifacts_created": [], "errors": ["the notes predicted M11 failed at highlight creation; it actually failed in the test's own database helper, after every UI assertion had passed"], "next_action": "Write tests/e2e/links.spec.ts for L02 and L08.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-e2e-links-green", "iteration_hint": "L02 + L08 specs", "started_at": "2026-07-25T11:40:00-0700", "finished_at": "2026-07-25T12:06:00-0700", "phase": "e2e-remaining", "actions_taken": ["wrote tests/e2e/links.spec.ts covering L02 and L08", "L02 reaches the seeded note through the references panel, hovers one document chip and presses F12; asserts the hovered target opens and that F12 with nothing under the cursor is inert", "L08 walks both results of the note's reference set, asserting per hop that the row's named document opened and the bottom panel stayed open, then that the close button still closes it", "reworked the L08 assertion after learning Dockview unmounts inactive tabs, so asserting both readers visible at the end would have asserted navigation did not happen", "narrowed the title->id map to the two referenced documents; the fixture library reuses a title across its six PDFs"], "criteria_advanced": ["L02", "L08"], "artifacts_created": ["tests/e2e/links.spec.ts"], "errors": ["first L08 attempt asserted both readers visible simultaneously", "second attempt built a library-wide title map, which is ambiguous in the fixture"], "next_action": "Run scripts/verify_completion.py end to end, then the audit.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-verifier-e2e-gate", "iteration_hint": "verifier e2e parse fix", "started_at": "2026-07-25T11:16:00-0700", "finished_at": "2026-07-25T11:23:00-0700", "phase": "audit", "actions_taken": ["ran the full verifier for the first time end to end: 64/77, with every e2e criterion failing despite the suite passing 11/11", "diagnosed the mismatch: pnpm forwards the literal '--' from 'pnpm test:e2e -- --reporter=json' to playwright, whose parser treats it as end-of-options and demotes --reporter=json to a positional file filter, so the config's list reporter stayed in effect and no JSON was ever produced", "fixed the invocation to 'pnpm test:e2e --reporter=json' with PLAYWRIGHT_JSON_OUTPUT_NAME pointed at logs/verify/playwright.json", "unlink the report before the run so a stale green report cannot outlive a failing one", "gave run() an env parameter", "corrected the playwright.config.ts comment that documented the old contract"], "criteria_advanced": ["M01", "M02", "M05", "M06", "M07", "M11", "L02", "L08"], "artifacts_created": [], "errors": ["the e2e gate had never once passed; the suite was green the whole time, so the failure looked like a product defect rather than a broken verifier invocation"], "next_action": "Independent audit -> reports/AUDIT.md; then phase -> milestone-1-complete.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-audit-majors-closed", "iteration_hint": "resolve every major audit finding", "started_at": "2026-07-25T13:30:00-0700", "finished_at": "2026-07-25T13:52:00-0700", "phase": "audit", "actions_taken": ["landed the in-flight symlink and loopback-host fixes with tests/integration/security.test.ts (10 cases, all failing before the fix)", "replaced the [L03] and [L04] tests: the audit proved by mutation that gutting LinkRepository to return [] left both green; they now run over the real router into the real store with a negative case in each, and re-running the mutation fails four of them", "implemented extractHtmlText for [T05], which claimed HTML normalization while feeding plain strings; a scanner rather than a regex because archived HTML is hostile and /<[^>]*>/g breaks on a quoted '>'", "rewrote the docs/SECURITY.md threat model: it said hostile PDFs are parsed in the main process, but the reading view parses them in the renderer", "moved setWindowOpenHandler and will-navigate to app.on('web-contents-created') and compared the dev URL by origin instead of prefix", "added setPermissionCheckHandler: the synchronous permission path never reached the request handler", "widened the verifier's renderer-boundary gate to source roots so apps/desktop/src/renderer is scanned, and added @wr/search, the workers, and the bare fs/path/child_process specifiers"], "criteria_advanced": ["L03", "L04", "T05"], "artifacts_created": ["tests/integration/security.test.ts", "packages/document-model/src/normalize.ts:extractHtmlText"], "errors": ["first extractHtmlText passed source line-wrapping through as paragraph breaks; in HTML a newline inside a paragraph is collapsible whitespace, so it now folds outside <pre> and is preserved inside one"], "next_action": "Write reports/AUDIT.md, then flip phase to milestone-1-complete.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-milestone-1-complete", "iteration_hint": "audit report + verifier 80/80", "started_at": "2026-07-25T13:52:00-0700", "finished_at": "2026-07-25T14:05:00-0700", "phase": "milestone-1-complete", "actions_taken": ["stopped zotero:import leaking raw Error.message into the response warnings, with a test that fails against the previous line", "wrote reports/AUDIT.md consolidating the security and test-honesty lenses: 6 major findings closed, 5 minor closed, 7 minor left open with reasons, plus the invariants checked and found sound", "flipped state/experiment_state.json phase to milestone-1-complete", "ran scripts/verify_completion.py end to end: 80/80, exit 0 - 361 unit tests, 11 e2e specs, all 34 criterion tags matched, typecheck/lint/security/boundary/audit/state/git all green"], "criteria_advanced": [], "artifacts_created": ["reports/AUDIT.md"], "errors": [], "next_action": "Milestone 2: activate W01-W10 tags in the verifier, then work docs/MILESTONE2.md in order.", "git_commit": "55d07c9", "pushed": true, "state_version": 1}
{"checkpoint_id": "cp-w06-w08-markdown-model", "iteration_hint": "milestone 2 opens: markdown domain model", "started_at": "2026-07-25T14:06:00-0700", "finished_at": "2026-07-25T14:22:00-0700", "phase": "milestone-2", "actions_taken": ["activated the W01-W12 tags in scripts/verify_completion.py so the milestone-2 criteria stop being inert", "added parseMarkdown/resolveWikilinks to @wr/document-model over remark-parse + GFM: headings with slug paths, section chunks, a prose projection, and wikilinks read from mdast text nodes by source slice so a code fence is excluded structurally rather than by pattern", "added createMarkdownAnchor/resolveMarkdownAnchor: quote + offsets into normalized text, hashed, with no geometry to fall back on because a markdown file is edited outside the app", "widened DocumentType, DocumentLocation, AnnotationAnchor, ReaderSelection and the chunk kinds with markdown; migration 002 rebuilds the three tables whose CHECK constraints had to widen and adds documents.slug plus wanted_pages", "replaced the annotations repo's two-branch anchor ternary with an explicit per-kind anchorColumns()"], "criteria_advanced": ["W06", "W08"], "artifacts_created": ["packages/document-model/src/markdown.ts", "packages/document-model/src/markdown-anchor.ts", "packages/document-model/src/markdown.test.ts", "packages/database/src/migrations/002_markdown.ts"], "errors": ["github-slugger maps each space to a dash, so '[[ Field Station ]]' slugged to '--field-station--'; slugify now collapses whitespace first"], "next_action": "W01: markdown reader package, panel kind, corpus ingestion, e2e spec.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-w07-corpus", "iteration_hint": "markdown corpus ingestion", "started_at": "2026-07-25T14:22:00-0700", "finished_at": "2026-07-25T14:34:00-0700", "phase": "milestone-2", "actions_taken": ["MarkdownCorpusImporter walks a configured root, hashes each file, and only re-parses what moved; the root comes from WR_MARKDOWN_ROOT or the users home, never from the renderer, so corpus:import takes no path argument", "links.replaceDerived scopes the delete to origin=derived AND generator, which is the bug W07 exists to catch: deleting by source_id destroys hand-made links and still passes a wikilink test", "wanted_pages repository plus corpus:wantedPages; SearchIndexer.indexExtractedChunks indexes markdown sections"], "criteria_advanced": ["W07"], "artifacts_created": ["apps/desktop/src/main/corpus.ts", "packages/database/src/repositories/wanted-pages.ts", "tests/integration/corpus.test.ts"], "errors": [], "next_action": "W01/W02: markdown reader package, panel kind, e2e.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-w01-markdown-reader", "iteration_hint": "markdown opens in a tab and renders", "started_at": "2026-07-25T14:34:00-0700", "finished_at": "2026-07-25T14:58:00-0700", "phase": "milestone-2", "actions_taken": ["packages/markdown-reader renders mdast to React node by node: no dangerouslySetInnerHTML anywhere, and a raw HTML block renders as the visible characters the author typed rather than being executed or dropped", "wikilink chips carry data-wanted, so a link to an unwritten page is visibly wanted rather than an error; what a chip does on click is the workbench's decision, handed in", "panel kind markdown-reader through layout.ts, readerDescriptorFor, titleFor and DOCKVIEW_COMPONENTS; readerTypeFor sends anything that is not pdf or markdown to the article reader", "main scans the corpus once after createWindow and publishes library:changed; the renderer builds wikilinkTargets from the library list's slugs so every markdown panel shares one index", "e2e workspace writes a real .md wiki and passes WR_MARKDOWN_ROOT; the app imports it with the real MarkdownCorpusImporter, so the spec asserts on rows ingestion made"], "criteria_advanced": ["W01"], "artifacts_created": ["packages/markdown-reader/src/{MarkdownReaderView.tsx,render.tsx,anchoring.ts,styles.css}", "tests/e2e/markdown.spec.ts"], "errors": ["isReaderPanelKind(descriptor.kind) narrows the kind but not the descriptor it came from, so every descriptor.documentId read after it failed to typecheck; added isReaderPanel(descriptor) as a guard on the descriptor itself", "[M05] asserted the sidebar row count equalled the Zotero import, which the corpus pages then broke; the count now includes corpusPageCount rather than being dropped, so a phantom row is still caught"], "next_action": "W02: a markdown highlight survives restart, as an integration test over a real corpus file.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-w02-markdown-highlight", "iteration_hint": "a markdown highlight survives restart", "started_at": "2026-07-25T14:58:00-0700", "finished_at": "2026-07-25T15:10:00-0700", "phase": "milestone-2", "actions_taken": ["tests/integration/markdown-highlight.test.ts takes 'survives restart' literally: the database is closed and reopened from the same path between making the highlight and finding it again, so nothing under test is carried across in memory", "the selection is derived the way MarkdownReaderView derives it -- offsets against the normalized document text, not the DOM -- and the anchor is built by the reader's own createMarkdownAnchorFromSelection", "the load-bearing case is the edited file: a section inserted above the quote invalidates every stored offset, the re-import changes the contentHash, and resolution re-finds the quote by context instead", "the third test is the one that keeps the second honest: when the sentence is deleted the anchor resolves to null rather than to a plausible wrong paragraph, and the annotation row is kept"], "criteria_advanced": ["W02"], "artifacts_created": ["tests/integration/markdown-highlight.test.ts"], "errors": ["@wr/markdown-reader has no vitest alias and its entrypoint pulls in React; the anchoring module is imported by path instead, as the main-process modules in this suite already are"], "next_action": "W03-W05: saved web pages.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-w04-snapshot-protocol", "iteration_hint": "rrfile:// serves a snapshot's resources", "started_at": "2026-07-25T15:10:00-0700", "finished_at": "2026-07-25T15:24:00-0700", "phase": "milestone-2", "actions_taken": ["parseFileRequest replaces parseFileId internally and returns {fileId, resourcePath}; parseFileId is kept and still returns null for any path within a file, so the single-file callers cannot silently start accepting one", "a resource is only served when the addressed row is text/html: a PDF row is not a handle on the directory it happens to share with other people's documents, which is how Zotero stores attachments", "the resource is bounded twice -- lexically against the snapshot directory, then again after resolveAllowedPath follows symlinks -- because the allowed roots are the whole library and would happily serve a sibling item", "blocksRemoteRequest is extracted from lockDownNavigation so the thing that stops archived markup phoning home is assertable without an Electron session", "FileRequestServices narrows AppServices to db/allowed/logger so the rules are exercised against a real database and real files with no Zotero client standing by to satisfy a type"], "criteria_advanced": ["W04"], "artifacts_created": ["tests/integration/snapshot-protocol.test.ts"], "errors": ["the encoded-separator forms (..%2f..) are the ones that actually reach the handler: the URL parser collapses `..` and `%2e%2e` only when they stand as a whole segment, so the boundary cannot be enforced during parsing and is enforced against the resolved path instead"], "next_action": "W03: a saved web page renders as the original in a sandboxed iframe (e2e), then W05.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-w03-saved-page", "iteration_hint": "a saved web page renders as the original", "started_at": "2026-07-25T15:24:00-0700", "finished_at": "2026-07-25T15:32:00-0700", "phase": "milestone-2", "actions_taken": ["packages/html-reader replaces its milestone-1 stub with HtmlReaderView: the snapshot is framed from its own rrfile:// origin rather than injected as markup, so relative URLs resolve themselves and nothing has to guess which of a page's URLs were meant to be local", "the iframe carries sandbox with no tokens at all -- every token is a capability handed back, and allow-same-origin in particular would give the page a real origin on this scheme", "snapshotSecurityHeaders serves default-src 'none' with the bytes, so the policy arrives even though the markup may carry a <meta> policy of its own; the allowances name the rrfile scheme rather than 'self' because a sandboxed frame's origin is opaque and 'self' would match nothing, its own stylesheet included", "the reader probes the entry document before framing it: a missing or non-HTML file becomes a stated error instead of Chromium's error page inside the frame, which reads as a badly saved page rather than an app that could not open it", "the e2e workspace writes a real archived page -- entry document, assets/page.css, assets/img/figure-1.png, plus the external script, inline script and tracking pixel a real one carries", "renderer CSP frame-src 'none' -> frame-src rrfile:, the narrowest grant that lets the frame exist"], "criteria_advanced": ["W03"], "artifacts_created": ["packages/html-reader/src/{HtmlReaderView.tsx,index.ts,styles.css}", "tests/e2e/webpage.spec.ts"], "errors": ["asserting on requests rather than responses would have been wrong: a request cancelled in the main process is still observable being issued. The claim that fails when the block is removed is that nothing remote ever answered"], "next_action": "W05: a web-snapshot highlight survives restart.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-w05-snapshot-highlight", "iteration_hint": "a web-snapshot highlight survives restart", "started_at": "2026-07-25T15:32:00-0700", "finished_at": "2026-07-25T15:35:00-0700", "phase": "milestone-2", "actions_taken": ["packages/document-model/src/html-anchor.ts: createHtmlAnchor/resolveHtmlAnchor, mirroring markdown-anchor. The offsets are into the text *extracted from* the archive, not into the archive, so a change to extractHtmlText moves them even though the bytes are identical -- resolution therefore trusts the offsets only while they still hold the recorded words, and re-finds the quote otherwise", "resolveHtmlAnchor refuses a readerMode mismatch outright: offsets over the original markup and over an extracted article are different coordinate systems of the same shape, and resolving one against the other lands a highlight on unrelated words at full confidence", "the nearest-occurrence search shared by both anchor kinds moved into text-quote.ts as locateNearest, rather than being copied", "tests/integration/html-highlight.test.ts drives the real ZoteroImporter over the recorded fixtures with real archived pages on disk"], "criteria_advanced": ["W05"], "artifacts_created": ["packages/document-model/src/html-anchor.ts", "packages/html-reader/src/anchoring.ts", "tests/integration/html-highlight.test.ts"], "errors": ["the re-save case first asserted a changed contentHash and failed: ZoteroImporter short-circuits an item whose Zotero version is unchanged, and skipping the item skips its attachments, so the bytes are never re-hashed. The fixture now bumps the version the way saving a page again does upstream -- rewriting the file alone would have left the test asserting against a stale row"], "next_action": "W09-W10: the graph.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-w10-graph-boundary", "iteration_hint": "graph queries run in main; the renderer gets a neighbourhood", "started_at": "2026-07-25T15:36:00-0700", "finished_at": "2026-07-25T15:47:00-0700", "phase": "milestone-2", "actions_taken": ["packages/graph holds the Cytoscape model: createGraph builds a headless instance, boundedNeighbourhood expands level by level over Cytoscape's own neighbourhood algebra and caps the result, layoutPositions arranges it in concentric rings by hop distance. Main bounds the query with it and the renderer lays the answer out with it, so 'within N hops' cannot mean two different things", "GraphRepository expands the frontier with one indexed lookup per node (EDGES_PER_NODE caps a hub) and never reads links whole; node titles and degrees are resolved in main so activating a node needs no second round trip", "graph:neighbourhood caps depth at 3 and nodeLimit at 300 in the *contract*, so the bound is not something a renderer remembers to apply", "elision is reported: truncated + elidedNodes, and the panel shows 'N more not shown' rather than presenting a window as the whole picture", "edges are induced over the kept nodes only, so no line is drawn to a node the renderer was never sent", "openLinkGraph stopped throwing NotImplementedError and now plans a link-graph panel; the class had no other thrower and was removed, and [L09] now asserts an unknown command id rejects"], "criteria_advanced": ["W10"], "artifacts_created": ["packages/graph/{package.json,tsconfig.json,src/index.ts,test/graph.test.ts}", "packages/database/src/repositories/graph.ts", "apps/desktop/src/renderer/graph-panel.tsx", "tests/integration/graph.test.ts"], "errors": ["the first elision expectation was arithmetic, not observation: at depth 2 from the hub the far end of the chain is inside the bound too, so 13 nodes qualify and 9 are dropped, not 8"], "next_action": "W09: e2e - the graph renders nodes and edges and clicking a node opens that document.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-w09-graph-view", "iteration_hint": "the graph renders and a node opens its document", "started_at": "2026-07-25T15:47:00-0700", "finished_at": "2026-07-25T15:58:00-0700", "phase": "milestone-2", "actions_taken": ["the graph panel draws SVG rather than Cytoscape's canvas: Cytoscape keeps the model and the traversal (the same module main bounds the query with), and a node stays a real element -- focusable, keyboard-activatable, and nameable by a test. On a canvas 'click the node for this document' would be a pixel calculation", "clicking a node navigates through workbench.navigate in 'side' mode, so the graph is still there to click again -- it is a way of getting around, not a dialog that closes behind you", "the e2e spec reads the wikilink edge out of the database the app is writing to and asserts the drawn edge carries that link row's id, so the panel cannot pass by inventing an edge between two nodes it happens to have", "panelSubjectKey keyed markdown-reader by kind alone: every markdown page was the same subject, so opening a second one revealed the panel showing the first. Now keyed by document like the other readers -- the same defect a wikilink chip would have hit"], "criteria_advanced": ["W09"], "artifacts_created": ["tests/e2e/graph.spec.ts"], "errors": ["the tab-order assertion assumed the new reader would land after the graph tab; it lands in the group beside, so the DOM order is source, target, Graph"], "next_action": "W11: six named highlight colours, edited from a popover that also edits the comment and deletes.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-w11-highlight-colors", "iteration_hint": "six named highlight colours, edited from a popover", "started_at": "2026-07-25T16:00:00-0700", "finished_at": "2026-07-25T16:22:00-0700", "phase": "milestone-2", "actions_taken": ["the palette moved into @wr/shared-types as an enum of six names; annotation:create and annotation:update take HighlightColorSchema, so a hex can no longer enter the database through the router", "writes are strict, reads are total: resolveHighlightColor maps anything unrecognised to 'default' in toAnnotation -- the single mapper that turns a row into an Annotation -- so milestone-1 hex rows still render. Editing a highlight rewrites the column, so rows converge on names without a migration pass", "readers resolve a name to var(--wr-highlight-<name>): PdfPageView, the markdown <mark>, and the card swatch all paint from the theme, so retheming repaints existing highlights", "HighlightPopover holds all three edits -- recolour, comment, delete -- because they are one act of reviewing a highlight; the card's bare Delete button folded into it", "the test renders the real AnnotationCard in jsdom and clicks a real swatch: the click runs the handler panels.tsx wires, the request crosses the real router into a real SQLite file, and the services are closed and reopened before the colour is read back"], "criteria_advanced": ["W11"], "artifacts_created": ["packages/shared-types/src/highlight-colors.ts", "packages/annotations/src/HighlightPopover.tsx", "tests/integration/highlight-color.test.ts"], "errors": ["the first swatch assertion queried the whole container, which also matched the card's own swatch -- scoped to the popover", "jsdom, react and react-dom had to be added to root devDeps (offline, from the pnpm store); @types/react-dom@18.3.5 is referenced by a workspace package but its tarball is not in the store, so 18.3.7 was used"], "next_action": "W12: Zotero import scoped to a named collection, additive across collections.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-w12-scoped-import", "iteration_hint": "Zotero import scoped to a named collection, additive", "started_at": "2026-07-25T16:25:00-0700", "finished_at": "2026-07-25T17:05:00-0700", "phase": "milestone-2", "actions_taken": ["import({collection}) resolves the name against the collection list before anything is written, so an unknown name imports nothing; the scope is the named collection plus every collection filed beneath it, because 'Past Projects' holding all its papers through children is the normal Zotero shape", "the item filter reads the recorded data.collections field rather than a collection-scoped endpoint: Zotero is not running on this machine right now, so /collections/<key>/items/top could not be verified live, and a route the fake API serves but the real one may not is exactly the fake data path the rules forbid. Noted as a later optimisation", "the whole collection tree is still mirrored on a scoped import -- collections are names and parents, not content, and a half-present tree would make the sidebar lie about where a scoped document sits", "ZoteroError gained NOT_FOUND and CONFLICT so 'no collection named X' and 'two collections named X' reach the user with a remedy instead of collapsing to INTERNAL", "additivity is asserted by import A, import B, then check A's five documents are still there -- a test that only checks B arrived would pass on a replace"], "criteria_advanced": ["W12"], "artifacts_created": [], "errors": ["the first draft of the test used externalReferences.findByEntity, which does not exist; listForEntity is the accessor"], "next_action": "Full verify_completion.py run, then the milestone-2 audit per docs/LOOP.md.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m2-audit-fixes", "iteration_hint": "milestone-2 audit: two majors found by mutation, both closed", "started_at": "2026-07-25T17:10:00-0700", "finished_at": "2026-07-25T17:36:00-0700", "phase": "milestone-2", "actions_taken": ["a mutation was sitting uncommitted in the working tree at session start: resolveMarkdownAnchor returned the stored offsets at confidence 1 when the quote was not found, silently moving a highlight onto whatever text now occupies those offsets. [W02] caught it and named the wrong text it landed on, so the test is load-bearing; reverted", "the verifier was already 92/92 at 4420cea, but its audit gate was satisfied by the milestone-1 audit at fa5672a -- W01-W12 had never been falsified. Two independent lenses were run against milestone 2", "W-1 (major, mutation-proved): [W11] built its own copy of the popover's edit handlers, so no-op'ing AnnotationsView's onChangeColor left all seven [W11] tests green. The criterion is that the colour is changed *from the popover*. The wiring moved to renderer/annotation-actions.ts -- free of React and dockview so jsdom can import it, which is what had pushed the test into duplicating it -- and both panel and test now drive it. The same mutation now fails", "W-2 (major): nothing anywhere called zotero:import, so the channel's zod contract and the handler forwarding `collection` were asserted by nothing; the handler could drop the scope and every [W12] test would still pass. services.ts gained an injectable zoteroFetch (the precedent is extractPdf, injectable so pipeline tests need not parse a real PDF) and tests/integration/zotero-import.test.ts drives the channel over the real router. Dropping the scope now fails 3 of its 4 tests", "W-3 (minor): packages/graph/src is imported by graph-panel.tsx but sat outside RENDERER_SOURCE_ROOTS, so the forbidden-import rule never reached it. Added -- a strengthening; the package was already clean", "reports/AUDIT.md now covers both milestones, naming the commit the auditors read (4420cea) rather than HEAD, with the four open minor findings and the reason each is not blocking"], "criteria_advanced": [], "artifacts_created": ["apps/desktop/src/renderer/annotation-actions.ts", "tests/integration/zotero-import.test.ts", "reports/audit-m2-tests.md", "reports/audit-m2-security.md"], "errors": ["the first draft of the router test invented db.library.listDocuments(); the real accessor is library.list({limit}).items with externalReferences.listForEntity('document', id)", "the first security-lens auditor died mid-response before writing its report; relaunched with a tighter brief and told to write the file early"], "next_action": "Re-run scripts/verify_completion.py at this commit; if 92/92 with HEAD pushed, emit the completion promise.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-ux-defects-from-real-use", "iteration_hint": "Six defects a user found by looking at the app while the suite was 92/92 green", "started_at": "2026-07-25T18:00:00-0700", "finished_at": "2026-07-25T19:30:00-0700", "phase": "milestone-2", "actions_taken": ["root-caused each report against the running app before changing anything: drove the real Electron build under Playwright, screenshotted every surface, and measured -- 1.34:1 contrast on the markdown body, 1112->832px reader collapse on highlight, 25 of 71 real Zotero PDFs warning 'Ensure that the standardFontDataUrl API parameter is provided'", "the markdown reader used var(--wr-surface, #fbfaf7), a token nothing defined, so its background fell back to a paper literal while var(--wr-text) found the dark chrome's #d7dae0 and did not fall back with it. Added a paper/ink scale and moved the reader onto it; [UX02] now fails on any --wr-* a stylesheet uses that nothing defines", "electron.vite.config.ts copies pdfjs-dist standard_fonts/ and cmaps/ next to the renderer bundle and pdfjs.ts points at them; without both, a PDF referencing the standard-14 fonts without embedding them renders in substituted metrics and a CID font maps to the wrong glyphs", "the selection toolbar was a flex row taking 39px of the scroller and committing a highlight force-opened the annotations sidebar taking 280px more. The toolbar is positioned now and annotating changes no layout; [M11] opens the sidebar itself and keeps every assertion about what it lists", "library:listDocuments takes a source filter; the sidebar issues 'zotero' and 'corpus' as two queries under two headings, while wikilink targets are still built from both", "verified real Zotero snapshots render faithfully -- across a 28-snapshot library the CSS is inlined (795 <style>, 1954 style=), the fonts are inlined (418 of 418 data:), the images are inlined (284 data:), and every remote reference is an ad, a beacon or a tracking pixel. [UX06] covers that single-file shape, which [W03]'s multi-file fixture does not"], "criteria_advanced": ["UX01", "UX02", "UX03", "UX04", "UX05", "UX06"], "artifacts_created": ["tests/e2e/reading-surface.spec.ts", "tests/e2e/pdf-fidelity.spec.ts", "tests/e2e/zotero-snapshot.spec.ts"], "errors": ["the first [UX05] asserted on console output and passed with the font URL pointed at a directory that does not exist -- PDF.js reports a missing font from the worker, which page.on('console') never sees. It asserts on the response and on document.fonts now, and was mutation-checked both ways", "asked the user to choose between collapsing duplicate library rows on a false premise: the two 'Early Data Exposure' rows are not one item with two attachments but two separate Zotero items, a preprint and a webpage. Disambiguated by type rather than collapsed"], "next_action": "Re-run verify_completion.py at HEAD; if 92/92 with a clean tree, emit the completion promise.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-carryover-c01-c03", "iteration_hint": "milestone 3 carry-over: pick collections, choose the notes folder, label the activity bar", "started_at": "2026-07-25T23:10:00-0700", "finished_at": "2026-07-25T23:50:00-0700", "phase": "milestone-3", "actions_taken": ["migration 003 adds a settings table (key -> JSON) with db.settings; the repository returns unknown on purpose and every reader parses it with the zod schema it already has, so a hand-edited row fails where it is read", "C01: zotero:listCollections, zotero:getImportScope and zotero:setImportScope; ZoteroImporter.import takes collections[] and filters the one item fetch against the union of their keys, so several picks are one import and a paper filed twice is seen once. zotero:import with no scope of its own falls back to the remembered picks -- absent means 'use the picks', an explicitly empty list means the whole library", "the picker falls back to the collections the last import mirrored when Zotero is not running, and says so: Zotero serves its local API only while open, so a live-only picker is empty exactly when someone opens the app to read", "C02: NotesFolder owns the four-step change -- remember, swap the allow-list, purge the notes from the folder no longer in use, re-import. SwappableRoots is the allow-list object every path check holds, so a change reaches rrfile://, the pipeline and the importer at once rather than whichever was built first", "the purge is a hard delete (documents.purge cascades; links and external_references have no FK and are cleared in the same transaction). A tombstone would bring the rows back the moment anything listed deleted documents. It also runs at startup, which is what clears the twelve stranded corpus rows that answered every click with 403 Forbidden", "the renderer gets the folder's *name*, never its path, and corpus:chooseFolder takes no payload -- a channel that accepted a path would be an arbitrary-directory read wearing a preference's clothes. A test asserts no response contains the folder's path", "C03: the activity bar's four glyphs carry their names as rendered text; the bar widened from 48px to 68px. The test measures a layout box and a computed font size, because 'the string is in the DOM' is true of font-size: 0"], "criteria_advanced": ["C01", "C02", "C03"], "artifacts_created": ["packages/database/src/migrations/003_settings.ts", "packages/database/src/repositories/settings.ts", "apps/desktop/src/main/notes-folder.ts", "apps/desktop/src/main/import-scope.ts", "tests/integration/notes-folder.test.ts", "tests/e2e/import-scope.spec.ts"], "errors": ["the build broke with 'Unterminated string literal' in out/main/index.js: electron-vite appends its CommonJS shim after the last match of a static-import regex over the whole bundle, and a log string ending in the bare word 'import' followed by another string literal matches it, so the shim landed inside a string. Reworded, with the reason recorded at the call site", "LibraryView returned an empty state before rendering anything when the library had no rows, which would have hidden the collection picker and the folder control on exactly the install that needs them. The controls render above the lists now"], "next_action": "Q01-Q04: the queue. Questions with a status, a hand-chosen order that survives restart, a discard reason, and typed edges to documents and annotations.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-queue-q01-q03-q04", "iteration_hint": "the queue: questions with a stored status, a stored order, a kept discard reason, typed edges", "started_at": "2026-07-26T00:00:00-0700", "finished_at": "2026-07-26T00:20:00-0700", "phase": "milestone-3", "actions_taken": ["migration 004 adds `questions`. `ordinal` is the hand-order and nothing derives it -- sorting by created_at or importance would replace a judgement about what to do next with a fact about when something was typed", "the CHECK (status <> 'discarded' OR discarded_reason IS NOT NULL) is deliberate: discarding is not deleting, the reason is the residue worth keeping, and the schema refuses a reasonless discard so no path -- repository, IPC or raw SQL -- can produce one. The test proves all three paths refuse it", "questions.reorder(ids) takes a *subset*: it collects the ordinals those ids already occupy, sorts them, and hands them back out in the new order, so a drag inside one filtered list cannot disturb the questions interleaved around it", "Q04 uses the existing links table with sourceType 'question' -- no second relationship mechanism. question:attach exists only so both endpoints are checked to exist before the edge is written; an edge to a document not in the library is a broken link the moment it is created, and the test asserts the refusal", "question:update refuses status: 'discarded' rather than accepting it with a null reason: routing the transition through a channel with no field for a reason would lose it", "'question' joins LinkableEntityType and EntityResolver.describe, so a question resolves in references, peek and the graph like anything else"], "criteria_advanced": ["Q01", "Q03", "Q04"], "artifacts_created": ["packages/database/src/migrations/004_questions.ts", "packages/database/src/repositories/questions.ts", "tests/integration/questions.test.ts"], "errors": ["the Q01 order assertion was first written over questions in insertion order, which would have passed on an implementation with no stored ordinal at all; rewritten to reorder into an arrangement no field sorts into"], "next_action": "Q02: the queue panel in the renderer -- hand-ordered by drag, order surviving restart (E2E).", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-queue-q02", "iteration_hint": "the queue panel: a grip, a drag, and an order that outlives the process", "started_at": "2026-07-26T00:20:00-0700", "finished_at": "2026-07-26T00:40:00-0700", "phase": "milestone-3", "actions_taken": ["renderer/queue-panel.tsx: a questions sidebar behind a new activity-bar button. The list is rendered in the order the main process returned and nothing in the view ever re-sorts it -- a drop sends the new id order to question:reorder and then takes back what the database says it is, so the view never keeps an arrangement that was not accepted", "reordering has two ways in: pointer-drag from the grip, and ArrowUp/ArrowDown while the grip has focus. A grip you can only drag is a grip some people cannot use, and both paths go through the same moveWithin and commit", "the order on screen is mirrored in a ref as well as in state. Committing from inside a setState updater would have sent the reorder twice under StrictMode, because React is free to run an updater more than once; computing the next order first and handing it to setQuestions keeps the updater pure", "the E2E drags with a real pointer over CDP rather than calling the channel: the criterion is that a person can arrange the queue, and a spec that invoked question:reorder would pass with no grip on the screen. Three questions are dragged into an order that is neither creation order nor its reverse nor alphabetical, then a second Electron process is launched over the same library", "mutation-checked: making questions.reorder a no-op fails both [Q02] tests and the [Q01] stored-order test"], "criteria_advanced": ["Q02"], "artifacts_created": ["apps/desktop/src/renderer/queue-panel.tsx", "tests/e2e/queue.spec.ts"], "errors": ["the first drag helper aimed at the near quarter of the target row, which is short of its midpoint, so dragging *downwards* never crossed the threshold that swaps two rows. It aims past the midpoint in the direction of travel now", "openQueue clicked the activity button blind, which closed the sidebar in the second process: the workspace layout restores the sidebars that were open, so the panel was already up. It toggles only when hidden"], "next_action": "J01-J03: the journal -- a dated markdown entry, a calendar that collapses runs of unlogged days, and an entry linked to the question it advances.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-journal-j01-j03", "iteration_hint": "the journal: a day, a calendar that folds the empty stretches, and an entry that names what it advanced", "started_at": "2026-07-26T00:40:00-0700", "finished_at": "2026-07-26T00:58:00-0700", "phase": "milestone-3", "actions_taken": ["migration 005 journal_entries is keyed by the ISO date rather than a minted id: there is exactly one entry per day, and a link to 'what I did on the 20th' should mean that day and survive the entry being cleared and written again", "the CHECK on markdown refuses the empty string, and journal.write() with blank markdown deletes the row. 'No entry' and 'an empty entry' are the same fact; a calendar able to tell them apart would be showing a difference that does not exist. Asserted three ways: the channel answers null, the row is gone, and a hand-written INSERT of '' throws", "J02 is a pure, clock-free function in the renderer -- calendarCells takes today as an argument, so the test can stand anywhere in the year. The two rules with the off-by-one in them are asserted from both sides: three idle days stay three bubbles and four fold into one marker, and today never folds and splits a run, which can drop a fold below the threshold on both sides", "J03 reuses links with sourceType 'journal' and type journal-entry-advances-question. journal:advancesQuestion checks both ends exist first: an edge from a day nobody wrote on, or to a question not in the queue, is broken the moment it is written", "the panel exists because the criteria are integration-level and would be satisfied by a feature no one can reach: JournalView renders the calendar from calendarCells, saves on blur, and offers the question picker only once the day has an entry. A short e2e drives it, because a save that never fires is invisible to a test that calls the channel"], "criteria_advanced": ["J01", "J02", "J03"], "artifacts_created": ["packages/database/src/migrations/005_journal.ts", "packages/database/src/repositories/journal.ts", "apps/desktop/src/renderer/journal-calendar.ts", "apps/desktop/src/renderer/journal-calendar.test.ts", "apps/desktop/src/renderer/journal-panel.tsx", "tests/integration/journal.test.ts", "tests/e2e/journal.spec.ts"], "errors": ["the [J03] refusal test used a 22-character placeholder id, which the router rejects as INVALID_REQUEST before the handler ever looks it up -- so the test was asserting the id format, not the existence check. Ids are <prefix>_<26 chars>; both placeholders are well-formed now"], "next_action": "A01-A13: the librarian. A02 (the write boundary) first, so everything after it is built against an enforced boundary rather than a promised one.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-a02-write-boundary", "iteration_hint": "the write boundary first: everything after it is built against an enforced rule, not a promised one", "started_at": "2026-07-26T01:00:00-0700", "finished_at": "2026-07-26T01:10:00-0700", "phase": "milestone-3", "actions_taken": ["apps/desktop/src/main/agents/workspace.ts: AgentWorkspace, one root, and the only path by which anything writes on the librarian's behalf. Every write resolves through it; there is no second door for accept to use later", "containment is decided twice because the string test is not the test that matters. `notes/x.md` where `notes` is a symlink out of the root is inside the workspace lexically and outside it after open(2), so the second check runs realpath on the deepest ancestor that exists and re-appends what does not, which resolves every link the OS would walk without requiring the leaf to be there", "the refusal is logged at warn with the path that was asked for, so a run that keeps trying to escape is visible rather than merely thwarted", "runDirectory mints <root>/.runs/<id> through the same resolve, so a run id carrying ../ is refused like any other escape. list() skips .runs: a proposal is in the workspace but is not yet part of what the workspace holds", "tests/integration/agent-workspace.test.ts drives five different escapes against a real temp filesystem with real planted symlinks -- climbing, absolute, symlinked directory, symlinked existing file, and a NUL byte -- and asserts both that nothing landed outside and that the attempt was logged"], "criteria_advanced": ["A02"], "artifacts_created": ["apps/desktop/src/main/agents/workspace.ts", "tests/integration/agent-workspace.test.ts"], "errors": [], "next_action": "A01: spawn a headless claude with --system-prompt-file and stream stream-json progress, built on AgentWorkspace.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-a01-runner", "iteration_hint": "a headless claude, watched while it works -- and a fixture that is a recording, not a guess", "started_at": "2026-07-26T01:10:00-0700", "finished_at": "2026-07-26T01:32:00-0700", "phase": "milestone-3", "actions_taken": ["probed the installed CLI rather than trusting the doc: --system-prompt-file exists in 2.1.220 and validates the file before anything else happens, so a missing prompt fails without a network call. The help text lists only --system-prompt and --append-system-prompt; the -file forms are real but appear only inside the --bare description", "recorded the fixture instead of writing one. One real `claude --output-format stream-json` run of the librarian's own prompt over a two-document corpus written to disagree with itself. It found the contradiction, cited both with [[wikilinks]] and wrote one note -- which is A06-A08 behaving before either was built. 47 events covering every kind the runner must survive: system/init, thinking_tokens, hook_started, assistant text/thinking/tool_use, user tool_result, rate_limit_event, result/success", "two mechanical redactions and no others: home paths, and the init event's slash_commands/skills/plugins inventories, which describe the laptop it was recorded on rather than the stream contract. tests/fixtures/agents/README.md says so", "agents/stream.ts parses what it recognises and returns null for the rest. An unknown event is a line from a newer CLI, not an error -- enumerating what to ignore is a list that goes stale. One assistant line can carry several blocks, so parseStreamLine returns an array: a two-tool-call turn that reported one call would be a silent undercount", "agents/prompt.ts makes the remit data. Each capability is one line appended only when enabled, so switching directions off removes the sentence rather than adding one that argues against it -- which is the only form A09 can be asserted in", "agents/runner.ts spawns with --system-prompt-file (replaces, where --append- would leave Claude Code's instructions in front of ours), --output-format stream-json (a --print run is a black box indistinguishable from a hang), --tools limited to the crawl set, and --strict-mcp-config with no config, so there is no retrieval anywhere in the path by construction", "the runner writes its system prompt through AgentWorkspace, not with writeFile. Giving the runner a privileged path of its own would have put an exception in the boundary on day one", "tests/fixtures/agents/fake-claude.mjs replays the recording as a real child process and writes the argv it was handed to disk, so the spawn is asserted rather than assumed -- a runner that silently dropped --system-prompt-file would still have streamed. It chunks at 3000 bytes to land splits mid-line, because one recorded event is 20 kB of file content"], "criteria_advanced": ["A01"], "artifacts_created": ["apps/desktop/src/main/agents/prompt.ts", "apps/desktop/src/main/agents/stream.ts", "apps/desktop/src/main/agents/runner.ts", "tests/fixtures/agents/librarian-stream.jsonl", "tests/fixtures/agents/fake-claude.mjs", "tests/fixtures/agents/README.md", "tests/integration/librarian-run.test.ts"], "errors": ["A09 and A11 now have passing tagged tests that cover only their first halves. Recorded as 'partial' in MILESTONE_STATUS.json so the next session does not read a green verifier as a finished criterion: A09 still needs a direction proposal to be dropped when the capability is off, and A11 still needs the agent to be handed whole documents"], "next_action": "A03 (agents off by default + disclosure) and the wiki view / proposal boundary that finishes A09 and A11.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-a11-wiki-view", "iteration_hint": "what the librarian is handed: the whole wiki, whole documents, and no way to ask for less", "started_at": "2026-07-26T01:32:00-0700", "finished_at": "2026-07-26T01:52:00-0700", "phase": "milestone-3", "actions_taken": ["agents/wiki-view.ts materialises the database as markdown a headless claude can Glob, Grep and Read its way through, following [[wikilinks]] by entity id. A document's text is every chunk of its latest revision joined in chunk_index order, with page boundaries kept as HTML comments so a citation can still name a page", "materialise() takes no arguments, and that is the assertion: there is no query, no limit and no relevance, so there is nothing to rank with. The A11 test checks the arity as well as the content", "the third A11 test reads the agent modules' own source and refuses embed/vector/cosine/topK/bm25/rerank and any import of @wr/search, because the regression arrives as somebody adding an import rather than as somebody editing an assertion. Comment lines are stripped first -- prose about not doing retrieval is the whole point of those files", "a document with no extracted text says so in words rather than rendering as an empty page. 'Not extracted yet' and 'nothing in it' are different facts, and an agent that cannot tell them apart will write a connection out of an absence", "the view is sealed r-x/r-- after every rebuild. --add-dir would otherwise make it writable to the agent, and a saved web page is hostile input: this way the refusal is in the kernel, below any tool, and does not depend on the agent having read the prompt", "sealing forced a public remove(): a read-only directory refuses to have its entries unlinked, so an ordinary rm -rf cannot clean the view up. #clear unseals before deleting, which is also what recovers a tree left behind by a crash"], "criteria_advanced": ["A11"], "artifacts_created": ["apps/desktop/src/main/agents/wiki-view.ts", "tests/integration/wiki-view.test.ts"], "errors": ["the first run of every wiki-view test failed in teardown, not in the body: rmSync could not unlink inside the sealed tree. Sealing a directory means owning its deletion, which is why remove() is public rather than a detail"], "next_action": "The proposal boundary: A04 citations, A06-A08 shapes, A12 coverage, A09's second half.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-proposal-boundary", "iteration_hint": "a schema in the prompt is a request; a schema at the boundary is a guarantee", "started_at": "2026-07-26T01:52:00-0700", "finished_at": "2026-07-26T02:07:00-0700", "phase": "milestone-3", "actions_taken": ["agents/proposals.ts is the gate between what a run staged and anything a person is shown. Every promise the criteria make about a proposal is decided here, on the way in, because that is the only place it can be guaranteed", "A04 resolves the union of the front matter's ids and the body's [[wikilinks]], so a citation the agent only wrote in prose is checked too -- which is where most of them are. One id that resolves to nothing refuses the proposal whole: a note asserting a source that is not there is worse than no note, so there is no partial form", "A09's second half is a capability check that runs before anything is resolved. The prompt line is a request; a run that suggests a direction anyway is exactly the case the flag exists to survive", "the entity kind comes from the id's own prefix, never from the front matter naming a table -- a proposal that could name its own table could choose which lookup it got. A journal entry's id is its date, which is why that is not a pure prefix table", "the front-matter reader understands exactly two forms, key: value and key: [a, b]. A general YAML parser would be a large surface pointed at text an agent wrote after reading a saved web page, and the extra grammar buys nothing", "a value is a list only when bracketed. Splitting an unbracketed value on commas truncated every title with a comma in it at the first clause, which is most of the interesting ones", "17 tests, each staging a file a well-behaved run would never write: an invented but well-formed doc id, a connection with one end, a contradiction with one side, evidence with nothing against, a direction after the capability came off"], "criteria_advanced": ["A04", "A06", "A07", "A08", "A09"], "artifacts_created": ["apps/desktop/src/main/agents/proposals.ts", "tests/integration/proposals.test.ts"], "errors": ["mutation-checked A04 rather than trusting it: making #resolve synthesize a citation when the database has none fails exactly the three refusal tests and nothing else. The criterion warns this one passes against no implementation, and it does"], "next_action": "A05: accept writes markdown with [[wikilinks]] into the workspace and a note row with source='librarian'; reject writes nothing. Finishes A12.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-accept-and-schedule", "iteration_hint": "the accept is where a person decides it is true; the schedule is why nothing writes when there is nothing to write", "started_at": "2026-07-26T02:07:00-0700", "finished_at": "2026-07-26T02:30:00-0700", "phase": "milestone-3", "actions_taken": ["migration 006 adds agent_runs and agent_proposals. proposal_count records nothing as a number rather than leaving it inferred from an absence of rows: a pass over unchanged material should produce nothing, and 'ran and found nothing' has to be distinguishable from 'never ran' or the schedule cannot tell whether it is due", "a CHECK refuses an accepted proposal with no workspace_path. Accepting IS writing the file, and a row claiming acceptance with nothing on disk would be a note the researcher believes is in their wiki and is not", "agents/librarian.ts: pass() stages and harvests, accept() is the only code path that writes into the workspace body. Accepting also mints a markdown document with source='librarian' and librarian-note-cites edges to every citation, so the note is an ordinary document -- openable, annotatable, and in the graph. The source column already carried what was needed: a write is legal iff it lands in the workspace, which is a path question, not a table question", "the note's coverage is written twice on purpose -- into the front matter, which a later pass routes on, and as [[wikilinks]] under a heading, which is what makes the graph real and what a person clicks", "agents/schedule.ts splits the policy from the timer. decidePass is pure, so every boundary is reachable directly; a scheduling rule tested through a real timer is one you can only watch fail twice a day", "a batch of imports shortens the interval and does not fire a pass. Nine papers is not a batch and one certainly is not: the work is cumulative, and a pass per arriving paper writes a note about each in isolation, which is the opposite of a connection", "tests assert the directory rather than the status column -- a row saying 'rejected' while a file sits on disk is the failure worth catching. E2E re-run after the migration: 34 passed"], "criteria_advanced": ["A12"], "artifacts_created": ["packages/database/src/migrations/006_agents.ts", "packages/database/src/repositories/agents.ts", "apps/desktop/src/main/agents/librarian.ts", "apps/desktop/src/main/agents/schedule.ts", "apps/desktop/src/main/agents/schedule.test.ts", "tests/integration/librarian-accept.test.ts"], "errors": ["A05 is an E2E tag in the verifier, so the three passing [A05] vitest tests do not satisfy the gate -- they prove the accept path, and a Playwright spec driving real buttons is still owed. Recorded as partial rather than left to look green", "A13's logic is fully tested but LibrarianScheduler is constructed nowhere outside its own test, so nothing in the running app schedules a pass. Partial by wiring, not by coverage"], "next_action": "Wire agents/ into services.ts, then A03 (off by default + disclosure), A05 (proposals panel) and A10 (a citation navigates) as E2E.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-wire-the-librarian", "iteration_hint": "seven tested modules and nothing constructing them: the wiring is what makes 'off' a fact about a running app", "started_at": "2026-07-26T01:05:00-0700", "finished_at": "2026-07-26T01:25:00-0700", "phase": "milestone-3", "actions_taken": ["services.ts builds workspace, view, runner, reader, LibrarianService and LibrarianScheduler as services.agents. Construction is deliberately free of consequence -- no directory made, no process spawned, no timer armed -- which is what lets the container be identical whether or not agents are enabled, and makes A03 a statement about behaviour rather than about which branch ran at startup", "the librarian's workspace joins the FIXED roots in SwappableRoots, not the swappable slot: an accepted note is an ordinary document opened through rrfile:// like any other, and unlike the notes folder its location is not a choice the researcher makes", "nine agent:* handlers, all through the one router. agent:status and agent:disclosure are the two an interface asks before anything is enabled, and neither may materialise the view or arm a timer -- the integration test asserts the wiki directory does not exist after both have been called", "startIfEnabled() reads the switch at launch so index.ts does not have to. With agents off no timer exists at all, which is stronger than a timer that keeps deciding not to run", "the run task now names the wiki by absolute path. It said ../../wiki, which from <workspace>/.runs/<id> resolves to <workspace>/wiki and not to the view's actual root -- an agent that cannot find the wiki produces an empty pass indistinguishable from a wiki with nothing new in it", "onEvent carries the run id now, so agent:progress can be published from the pass without the caller having to know an id that pass() mints internally", "settings.ts dropped its own AgentDisclosure interface for the one in shared-types: the schema at the boundary is the authority, and two declarations of the same shape drift"], "criteria_advanced": ["A13"], "artifacts_created": ["tests/integration/agent-channels.test.ts"], "errors": ["wrote the A05/A10 channel tests against agent:run over the recorded transcript first, and they failed with zero proposals. The recording predates both the proposals/ directory and the front matter the task now asks for, and it cites the ids of the library it was recorded against -- so it can prove the spawn and the stream but cannot stand in for a pass. Staged the proposals through the real workspace and the real reader instead, the way librarian-accept.test.ts does"], "next_action": "The renderer: a librarian sidebar with the disclosure, the switch, the capabilities and the proposals, then the E2E specs A03, A05 and A10.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-librarian-panel-and-e2e", "iteration_hint": "the disclosure is the interface: what it would send, before the switch that sends it", "started_at": "2026-07-26T01:25:00-0700", "finished_at": "2026-07-26T01:45:00-0700", "phase": "milestone-3", "actions_taken": ["renderer/librarian-panel.tsx puts the decision on screen in the order the decision is made: the disclosure first and not behind a triangle, then the switch, then what it may do, then what it has proposed. The main process already refuses to enable without the acknowledgement, so this is the same rule stated where it can be read rather than only enforced", "a citation is a button, not text, because it is a place. It navigates through the workbench like any other internal link, so a cited highlight opens its document scrolled to its page", "A03's strongest assertion is not on screen at all: after launching and opening the panel, <agentRoot>/wiki does not exist. The view is materialised at the start of a pass and nowhere else, so its absence means nothing was scheduled, started, or warmed up by looking at the panel", "the disclosure's document count in the E2E is documents + corpus pages, and getting that wrong is what caught the real behaviour: the wiki is the whole app, and a disclosure naming only the Zotero half would be the more dangerous kind of wrong", "A05 asserts the directory, not the row -- a proposal reported accepted with nothing on disk is a note the researcher believes is in their wiki and is not. Rejecting is asserted as the workspace being byte-identical to what the accept left behind", "A10 cites a highlight on page 2 so that 'opened the document' and 'opened it at the location' are distinguishable outcomes: the assertion is that pdf-scroll's scrollTop equals that page's own offsetTop, which page 1 cannot satisfy", "WR_AGENT_ROOT joins WR_DATABASE_PATH and WR_MARKDOWN_ROOT as main-process configuration, so the suite points a temporary library at its own agent workspace the way a second installation would", "README no longer says agents are unimplemented. It now says what the librarian sends, on whose credentials, that it only proposes, and that with agents off no schedule is armed and no copy of the wiki is made"], "criteria_advanced": ["A03", "A05", "A10"], "artifacts_created": ["apps/desktop/src/renderer/librarian-panel.tsx", "tests/e2e/librarian.spec.ts", "tests/e2e/support/librarian.ts"], "errors": ["seeded the highlight anchor with {x,y,width,height}; NormalizedRect is {x1,y1,x2,y2} and the repository parses its own row back, so it failed at create rather than at read"], "next_action": "The independent audit of milestone 3. reports/AUDIT.md still records 4420cea, the milestone 2 commit, and the verifier only checks ancestry -- so it passes today without milestone 3 having been read. Do not emit the promise before it has.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-gates-green-audit-owed", "iteration_hint": "the verifier says complete and it is wrong about exactly one thing", "started_at": "2026-07-26T01:45:00-0700", "finished_at": "2026-07-26T01:50:00-0700", "phase": "milestone-3", "actions_taken": ["verify_completion.py reports 117/117 on a clean pushed tree at 81f6530, and prints MILESTONE COMPLETE", "did not emit the promise. The audit check is satisfied by ancestry: reports/AUDIT.md names 4420cea, which is an ancestor of HEAD, and the file's own title is 'milestones 1 and 2'. Every line of milestone 3 was written after it, so the gate is green on an audit that read none of it", "NEXT_ACTION.md now opens with that, because a fresh session reading 'MILESTONE COMPLETE' from the verifier would reasonably believe it"], "criteria_advanced": [], "artifacts_created": [], "errors": [], "next_action": "The independent audit of milestone 3 per docs/LOOP.md, pointed at the librarian: A03's off-means-off claim, agent:accept as the one channel that writes outside the database, the workspace root now being a fixed rrfile:// root, and a mutation sweep over the single-test A05 and A10 tags.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-u08-deleted-nodes", "iteration_hint": "a soft delete the graph query never heard about", "started_at": "2026-07-27T20:40:00-0700", "finished_at": "2026-07-27T20:52:00-0700", "phase": "milestone-3", "actions_taken": ["graph.ts filters both ends of every edge against the three tables that soft-delete -- annotations, notes, documents. The traversal mints its nodes from the edges it reads, so an edge that outlives its endpoint is what was drawing the node for a deleted highlight; filtering the node afterwards would have left the edge", "degree() takes the same filter. Degree is what tells the view a node continues past the bound, so counting edges into deleted entities promises neighbours that expanding can never produce", "the deletion filter is in the query and not in graph-panel.tsx because graph:neighbourhood has more than one consumer, and a renderer-side filter leaves the stale node in all the others", "seedAnnotatedNote() builds note -> annotation -> document so the document is reachable only through the highlight: deleting the highlight then has to take the document with it, which distinguishes a real traversal fix from hiding one node"], "criteria_advanced": ["U08"], "artifacts_created": [], "errors": ["wrote the third case against a note:delete channel that does not exist -- notes are soft-deletable in the repository but no channel deletes them. Drove notes.softDelete directly and said so in the test, since the filter is in the query and holds for every route to deleted_at"], "next_action": "U01/U02: there is no close command at all -- no wr.closeTab, no Ctrl/Cmd+W binding, so the keystroke falls through to Chromium and closes the window.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-u01-u05-u07-usability", "iteration_hint": "the keystroke never reached the app; the chrome outgrew the document", "started_at": "2026-07-28T10:50:00-0700", "finished_at": "2026-07-28T12:15:00-0700", "phase": "milestone-3", "actions_taken": ["U01's real cause was not the missing command but Electron's default menu: Window -> Close owns Cmd+W and a menu accelerator is consumed before the renderer's keydown listener ever runs, so a wr.closeTab binding alone would still have closed the window. main/menu.ts installs a template with no close role, before the first window; menu.test.ts asserts no item on any platform binds Cmd/Ctrl+W, and checks the edit roles the note editor depends on are still there", "wr.closeTab and wr.closeGroup are bound unconditionally on purpose. A `when` clause that stopped matching once the last tab closed would hand the keystroke back to Chromium, which is the exact failure -- so with nothing open the command runs and does nothing, and the window stays", "U03 was reach, not painting: mutating the tab max-width away put a close control at x=1681 in a strip ending at 1441, 240px off screen. The test asserts reach before it asserts truncation, so it fails on the criterion's own property rather than on the technique that buys it", "the four left sidebars now share one slot. toggleSidebarState/normaliseSidebars/openLeftSidebar live in packages/workbench/src/layout.ts, and deserializeWorkspace applies the rule too -- a workspace saved with all four open collapses on restore instead of reproducing the stacked layout after a restart", "App.tsx renders a single <LeftSidebar> rather than four conditional siblings, so the markup cannot stack them even if the state rule were broken. revealInLibrary goes through normaliseSidebars because it shows the library rather than toggling it", "U04's second test is what keeps the first honest: it proves the reader's width actually responds to the sidebar closing, so 'unchanged across four swaps' in the sibling test means something rather than measuring an element that never moves", "#subjectOr lets a command say what would make it work. The graph button now reports 'Open a document or select a highlight first' instead of 'no entity to act on', which was true and useless (U05)", "U07's reason is rendered text beside the disabled button, not a title attribute -- same argument as C03's activity-bar labels: an explanation you must hover to discover is one most people never find. The test asserts it is visible, adjacent, and disappears once the precondition is met"], "criteria_advanced": ["U01", "U02", "U03", "U04", "U05", "U07"], "artifacts_created": ["apps/desktop/src/main/menu.ts", "apps/desktop/src/main/menu.test.ts", "tests/e2e/tabs.spec.ts", "tests/e2e/sidebars.spec.ts"], "errors": ["wrote U03's click against a hover-revealed control: dockview hides an inactive tab's x with visibility:hidden, and Playwright's click will not reveal it. Hovering first is what a person does, so the test hovers", "U04's activity loop started on the library, which is the sidebar already open, so the first click closed it rather than switching. Reordered to end at the library so every click is a genuine switch", "read the reader's boundingBox immediately after the sidebar unmounted; dockview relayouts from a ResizeObserver a frame later. Polled instead"], "next_action": "U06 -- a highlight's comment written and read back from the reader itself. The popover already edits comments and [W11] covers it; what is missing is the way in, and PdfPageView paints highlights aria-hidden and pointer-events:none on purpose so text selection keeps working. Then the milestone-3 audit.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-u06-comment-from-the-reader", "iteration_hint": "the popover existed; nothing in the reader pointed at it", "started_at": "2026-07-28T13:30:00-0700", "finished_at": "2026-07-28T14:45:00-0700", "phase": "milestone-3", "actions_taken": ["U06 was never about the editor. HighlightPopover already wrote comments and [W11] already covered it -- what was missing was a way in from the page, so the only place a comment could be written was the annotations sidebar", "PdfReaderView hit-tests the click coordinates against painted.byPage rather than making the overlay clickable. The overlay is pointer-events:none deliberately -- the text layer needs every pointer event, and a highlight that swallowed clicks would make the text under it unselectable, so a passage could never be highlighted twice. Hit-testing costs the text layer nothing and uses the relocated rectangles, so a highlight opens from where it is now rather than where it was stored", "MarkdownReaderView delegates one onClick and reads data-annotation-id off the <mark> under the pointer. render.tsx gains the attribute and no handler, so it stays a pure function of the source, and a highlight split across several <mark> runs needs no handler per run", "both readers return early when the selection is not collapsed. The click that ends a drag must not open whatever the pointer stopped over, or selecting a passage overlapping an existing highlight pops the editor every time", "ReaderHighlightEditor is shared by the PDF and markdown panels and drives createAnnotationEdits -- the same definition the sidebar drives. Writing a second copy of those handlers is exactly what let all seven [W11] tests stay green over a dead panel", ".wr-reader-popover floats bottom-centre like .wr-selection-bar: in the flex column it would take height from the scroller and reflow the page being read, which [UX03] forbids", "the E2E never opens the annotations sidebar and asserts it stays hidden throughout, and scopes the popover to .wr-reader-panel -- the sidebar renders the same component, so an unscoped locator would have passed on the affordance the criterion says is missing"], "criteria_advanced": ["U06"], "artifacts_created": ["tests/integration/reader-highlight-activation.test.ts"], "errors": ["skipped the red run: the first attempt was `pnpm test:e2e -- --grep`, which pnpm swallowed, so it ran the whole suite instead of one test. Killed it and proved the test load-bearing by mutation afterwards instead", "first mutation -- deleting onClick={handleClick} -- did not compile, since the handler then had no reader. A mutant that fails the build proves nothing about the test. Made a hit report a miss instead, which compiles and fails the popover assertion", "read the earlier suite's \u2718 marks with 17-27m durations as real regressions in [L02]/[W01]/[UX05]. They were the killed run contending for the machine: the full suite is 45 passing in 1.9m. A failing Playwright test is very slow here, so a long duration in a log means a failure, not a hang"], "next_action": "The independent audit of milestone 3 per docs/LOOP.md. reports/AUDIT.md names 4420cea, a milestone-2 commit, and is titled 'milestones 1 and 2' -- nobody has read milestone 3, and the verifier's audit gate is satisfied by ancestry alone. Point it at the librarian: A03 off-means-off, agent:accept as the one channel writing outside the database, the workspace root as a fixed rrfile:// root, and a mutation sweep over the single-test A05 and A10 tags.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-audit-finding-1-path-leak", "iteration_hint": "the audit found the renderer had been receiving paths all along", "started_at": "2026-07-28T15:10:00-0700", "finished_at": "2026-07-28T15:30:00-0700", "phase": "milestone-3", "actions_taken": ["the milestone-3 audit exists as reports/audit-m3-security.md, written against f6fbede and left untracked by the previous session. 15 findings, 1 critical and 6 major. Committed it so the findings are visible rather than sitting in an untracked file", "finding 1, the critical one: agent:progress published absolute filesystem paths to every webContents and librarian-panel painted them on screen, which makes CLAUDE.md's 'the renderer never receives or builds a filesystem path' false. Claude Code's Read takes an absolute file_path, so the leak is the normal case, not an edge", "the leak had a second channel the audit did not name: message prose. Fixture line 43 is the model saying 'Current working directory is `/private/tmp/wr-fix/run`'. Fixing only tool targets would have left the invariant false and the next audit would reopen it, so withoutFilesystemPaths runs over both free-text fields", "withoutFilesystemPaths lives in main/paths.ts, whose header already claims to be the second half of the no-paths guarantee. A path inside a root the run was given becomes relative to it -- which is what the topic's own doc at ipc.ts:754 always promised -- and anything outside keeps only its basename", "the regex uses a lookbehind so `and/or` is not read as the path `/or`, and splits trailing sentence punctuation off the match so prose ending in a path does not turn the full stop into part of the basename", "AgentServices.progressRoots names the roots once. Both publishers -- the scheduler in services.ts and agent:run in handlers.ts -- read it, because a publisher that forgets the roots does not fail, it silently emits absolute paths"], "criteria_advanced": [], "artifacts_created": ["tests/integration/agent-progress.test.ts", "reports/audit-m3-security.md"], "errors": ["none this checkpoint; the RED run failed on all four new assertions and listed 9 leaking details from the real recorded run, including /root, /home and the model's own cwd"], "next_action": "Audit findings 2 (the materialised wiki is never removed in production), 3 (agent:accept TOCTOU mints two documents), 4 (agent:run's race window spans materialise), then the three test-honesty majors 5, 6 and 7. Then write reports/AUDIT.md at the resolved commit -- it still names 4420cea, a milestone-2 commit.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-audit-findings-2-3", "iteration_hint": "off did not undo on; one intention made two documents", "started_at": "2026-07-28T15:30:00-0700", "finished_at": "2026-07-28T15:42:00-0700", "phase": "milestone-3", "actions_taken": ["finding 2: WikiView.remove() had no production caller, so switching agents off left every document's full text, every highlight and comment, every question and journal entry sealed under userData -- surviving the switch and the uninstall. README.md:16 and the disclosure's withhold line are both read by someone deciding whether they can change their mind, and neither was true of switching back off. agent:enable now awaits view.remove() when disabling, after stopping the timer and cancelling runs", "the finding-2 test runs a real pass first and asserts the copy exists before switching off, so it fails if remove() is called on a wiki that was never there", "finding 3: two clicks on Accept dispatch two concurrent invokes and both pass the pending check, which sits two awaits before the state change. The proposal row survives on its own (WHERE status='pending'); documents.create does not, and documents_slug_idx is not unique, so the second insert lands and upsertByPath repoints the one file row at it -- a librarian document with citation edges and no file, in the library and the graph, unopenable", "LibrarianService.accept now shares the in-flight promise per proposal id rather than refusing the second caller: a double click is one intention and both callers should be told the same thing. A later accept still fails the status check", "the race test went through three shapes before it was worth keeping. As a plain Promise.allSettled pair it reproduced 3 times in 6 -- a guard exercised half the time is one a later change can delete with nothing going red. A 2-party barrier was deterministic but deadlocked against the fix, because with the guard there is no second write to arrive: a harness that hangs when the bug is gone encodes the bug. The kept shape holds every writer and races the second arrival against a timeout"], "criteria_advanced": [], "artifacts_created": [], "errors": ["the first barrier released the held writer too early -- it won the race and registered its document before the second caller's write finished, so the mutation survived and the test proved nothing. Caught it by removing the guard and watching the test still pass, which is the only reason it was found", "the second shape deadlocked the suite for 20s on a vitest timeout. Both directions now checked explicitly: 3/3 green with the guard, 3/3 red without it"], "next_action": "Finding 4 (agent:run's race window spans materialise -- runner.busy goes true only after the spawn), then the test-honesty majors 5, 6 and 7. Then write reports/AUDIT.md at the resolved commit.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-audit-finding-4", "iteration_hint": "not running, for the whole of the expensive part", "started_at": "2026-07-28T15:43:00-0700", "finished_at": "2026-07-28T15:48:00-0700", "phase": "milestone-3", "actions_taken": ["finding 4: the one-pass-at-a-time guard read runner.busy, which reads the map the spawn populates. materialise() runs before the spawn and is the long part -- it writes every document's full text -- so the guard was false for exactly the window it was meant to cover. Two agent:run invokes inside it both passed, both rebuilt the wiki on one root with one rm -rf'ing while the other wrote and sealed, and both spawned a claude", "the flag belongs to LibrarianService, not the runner: a pass is not only its child process. LibrarianService.busy is true from the moment pass() is entered until it settles, and pass() refuses a second entry with PassAlreadyRunningError", "both readers of the stale flag were fixed together -- the agent:run guard in handlers.ts and observe() in services.ts, which is what decidePass reads. Fixing only the handler would have left the scheduler able to start a pass on top of a manual one", "the test asserts the run count, not the error: a second pass that got as far as starting has already recorded itself and already rebuilt the wiki under the first. It is deterministic in both directions because agentRuns.start runs synchronously before the first await"], "criteria_advanced": [], "artifacts_created": [], "errors": [], "next_action": "The three test-honesty majors 5, 6 and 7, then reports/AUDIT.md at the resolved commit.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-audit-findings-5-6-7-9", "iteration_hint": "three tests that agreed with their criteria by accident", "started_at": "2026-07-28T15:49:00-0700", "finished_at": "2026-07-28T15:56:00-0700", "phase": "milestone-3", "actions_taken": ["finding 5: every A02 test exercised AgentWorkspace, which the spawned claude never calls -- it writes with its own tools. The class header says 'there is no other path' and that is a claim about the app, not the agent. fake-claude now attempts a write into every --add-dir root it was given and records the errno; the test asserts the kernel refused all of them and that no file landed", "ENOENT is deliberately excluded from the accepted error codes: a wiki that was never materialised would refuse those writes too, and a test that cannot tell 'sealed' from 'absent' is the exact defect class this audit exists to find", "finding 6: A13's quiet-pass test ran the recorded transcript, whose child does write a finding -- into the run-directory root, where harvest does not look. 'Found nothing' and 'wrote it where nobody reads' have the same observable from the assertions, so the test agreed with the criterion by accident. WR_FAKE_CLAUDE_QUIET makes the pass genuinely quiet, and a new assertion fails if any markdown the child wrote is left anywhere in the run directory. Verified against the old behaviour: it catches the historical defect", "finding 7: the seam between what an agent writes and what the harvester reads was uncovered -- every proposal in every test was staged by code that computed .runs/<id>/proposals itself, so both ends were the test's arithmetic. The child now writes to ./proposals/ relative to the cwd the runner gave it, which is what the task tells it to do, and the real ProposalReader is left to find it. The recorded argv is asserted to still carry the ./proposals/ instruction, so a change to either end goes red", "finding 9: docs/SECURITY.md had no milestone-3 content and two false lines. Added threat-model rows for prompt injection through a hostile saved page and for the child process, four invariant rows, and four gaps -- including that the child's outer boundary is a third party's permission model that no test here asserts", "both new tests were mutation-checked: unsealing the wiki makes the child's escape succeed and the A02 test red; pointing harvest at a different directory makes the seam test red"], "criteria_advanced": [], "artifacts_created": [], "errors": ["the first run-directory assertion failed on system-prompt.md, which the runner writes before the spawn and is not the child's. Excluded it by name rather than loosening the assertion to a substring match"], "next_action": "Write reports/AUDIT.md at the resolved commit, then run scripts/verify_completion.py.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m3-audit-report-and-completion", "iteration_hint": "the audit existed untracked; the verifier would have said complete without it", "started_at": "2026-07-28T15:57:00-0700", "finished_at": "2026-07-28T16:06:00-0700", "phase": "milestone-3", "actions_taken": ["reports/AUDIT.md named 4420cea and was titled 'milestones 1 and 2'. The verifier's audit gate is satisfied by ancestry alone, so it would have passed on a milestone-2 audit -- which is exactly what the previous session's NEXT_ACTION warned about. The milestone-3 section now leads the file and names f6fbede, the tree the auditor actually read", "the audited commit is deliberately not HEAD, same convention as milestone 2: what it names is the tree that was read, and the commits after it are the fixes", "the findings table records all fifteen with severity and resolution, the seven open minors carry the reason each was left, and the traces that ended in 'this is safe' are kept -- A03's off-means-off, the accept path's traversal analysis, A11's no-retrieval check", "verify_completion.py exits 0 at 125/125 with a clean tree and HEAD on origin/main"], "criteria_advanced": [], "artifacts_created": ["reports/AUDIT.md milestone-3 section", "reports/audit-m3-security.md (committed)"], "errors": [], "next_action": "Milestone 3 is complete. Do not start milestone 4. The seven minor findings are open by choice; 11 (a child ignoring SIGTERM wedges the librarian) is the only one that breaks the feature outright.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-armed-and-packaged", "iteration_hint": "the fixes were green for three days and the researcher never had them", "started_at": "2026-07-28T18:35:00-0700", "finished_at": "2026-07-28T18:55:00-0700", "phase": "milestone-4", "actions_taken": ["the researcher reported Cmd+W quitting the app and tab titles hiding their close control -- both U01 and U03, both green at 125/125. Neither was a code defect: /Applications/wiki-reader.app was the Jul 25 package and the fixes landed Jul 28. The installed main bundle had zero occurrences of setApplicationMenu; the repo build had one. Confirmed the source is genuinely fixed by running tabs.spec.ts (3 passed) and menu.test.ts before repackaging", "pnpm package, then installed by moving the running bundle aside rather than writing over it, so the live process kept its inodes and the open window survived. Verified the new bundle carries both fixes: setApplicationMenu in main, max-width 220px plus ellipsis on .dv-default-tab-content in the renderer CSS, ordered after dockview's own rules", "CLAUDE.md now lists pnpm package and says the researcher runs the bundle, not the tree -- a green criterion is not a delivered fix", "armed all 21 milestone-4 tags and added docs/MILESTONE4.md to REQUIRED_DOCS. CLAUDE.md pointed at MILESTONE3.md and said not to build milestone 4; it is auto-loaded every session, so it was the first thing to correct", "added N08: a question's notebook is reached from the queue and names its question. The researcher's first question about notebooks was how they are accessed, which is U05 and K01/K02 again -- the door is the part that gets skipped", "strengthened the audit gate. Reachability alone made the milestone-3 audit satisfy the milestone-4 gate, because a commit stays an ancestor of HEAD forever. AUDIT.md must now carry Audited-milestone: <n> matching the highest-numbered criteria doc in docs/. Requiring the audited commit to be the newest would have been wrong in the other direction: closing an audit's findings necessarily commits after the audit ran"], "criteria_advanced": [], "artifacts_created": ["/Applications/wiki-reader.app (repackaged from 77fb545)"], "errors": [], "next_action": "Build milestone 4 in the order docs/MILESTONE4.md gives: notebooks (N01-N08), then library curation (B01-B04), then graph and discoverability (G, K).", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-notebooks-data-layer", "iteration_hint": "hypotheses are what evidence attaches to; everything else on the page is front matter", "started_at": "2026-07-28T20:30:00-0700", "finished_at": "2026-07-28T20:52:00-0700", "phase": "milestone-4", "actions_taken": ["migration 007: questions gains body/description/cover_file_id, plus question_tags and a hypotheses table. body defaults to empty and the section template is never stored -- question:notebook reads blankNotebook() for an empty row, so a blank page looks like the template without the app having written prose on the researcher's page. [N01] asserts the stored row is still empty", "evidence is an ordinary typed edge in links (<document|annotation>-<supports|opposes>-hypothesis) and hypothesis joined LinkableEntityType. That buys endpoint resolution, broken-link marking and the graph for one enum member; an evidence table would have been a second relationship mechanism to keep in step with all three", "packages/document-model/src/notebook.ts parses sections off the markdown AST the app already uses, so a ## inside a code fence is text and not a section. That case is the one that decides the implementation, and it is the [N02] test with teeth", "cover is a document_files id, never a path. [N03] asserts the temp dir and the filename appear nowhere in the JSON that crosses to the renderer, so an implementation handing over a path fails rather than passing quietly", "[N05] asserts citations come back resolved -- the highlight's own words and the location that opens them -- which an implementation storing ids and echoing them cannot produce. Same trap as A04"], "criteria_advanced": ["N01", "N02", "N03", "N04", "N05"], "artifacts_created": ["packages/database/src/migrations/007_notebooks.ts", "packages/database/src/repositories/hypotheses.ts", "packages/document-model/src/notebook.ts", "tests/integration/notebook.test.ts", "packages/document-model/src/notebook.test.ts"], "errors": [], "next_action": "Build the notebook panel: nothing in the app opens a page yet, so N01's 'edited in-app' is half delivered and N08 (the door from the queue) is what proves it. Then N06 and N07.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-notebook-panel-and-door", "iteration_hint": "the door is the part that gets skipped, which is why N08 exists at all", "started_at": "2026-07-28T20:52:00-0700", "finished_at": "2026-07-28T21:02:00-0700", "phase": "milestone-4", "actions_taken": ["the notebook is a Dockview tab, not a sidebar: it is a page you work on rather than a list you consult. Front matter, the markdown body, and the claims with their evidence, in one round trip -- question:notebook already returns all three, and a page whose hypotheses arrive on a second request flickers", "the question in the queue is the door. The title is now a button running COMMAND_IDS.openNotebook through the command registry, like every other panel move; panels never open each other directly", "the E2E caught a real bug the integration tests could not: panelSubjectKey fell through to descriptor.kind for a notebook, so opening a second question revealed the panel showing the first -- indistinguishable from the page failing to load. Keyed by question id now, and held by a [N08] unit test in panel-targets.test.ts as well", "the tab names its question. titleFor cannot know it (the title lives behind an IPC call), so the panel sets its own tab title once the page loads, and titleFor answers 'Notebook' for the moment before that", "prose saves on blur and the page says 'Saved'. A page that silently discards the last paragraph is worse than one that never had an editor"], "criteria_advanced": ["N08"], "artifacts_created": ["apps/desktop/src/renderer/notebook-panel.tsx", "tests/e2e/notebook.spec.ts"], "errors": [], "next_action": "The desk board: N06 (hand-placed cards, arrangement survives restart) and N07 (a dropped file becomes a card without leaving the researcher's disk). Store a position only once a card has been dragged -- a default position is not a decision.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-desk-board", "iteration_hint": "a card is an edge; a position is only what a hand chose", "started_at": "2026-07-28T21:02:00-0700", "finished_at": "2026-07-28T21:18:00-0700", "phase": "milestone-4", "actions_taken": ["migration 008 adds card_positions(link_id PK -> links ON DELETE CASCADE, x, y). No cards table: a card is the question-references-* edge that already exists, so putting something on the board and relating it to the question are the same act, and link:delete takes it off with the position cascading. A second table naming the same relationship would be the first thing to drift from the graph and the reference query", "a position is written only at the end of a drag. question:notebook returns position: null for a card nobody has moved and the board lays those out with defaultSpot(index), storing nothing -- so the default can be improved later without moving cards somebody thinks they placed. The [N06] integration test asserts COUNT(*) FROM card_positions is 0 after an attach, which is the assertion that fails against the obvious implementation", "the E2E drags a card in one Electron process, closes it, and asserts the same data-x/data-y in a second one -- and that the card that was never dragged is still data-placed=false after the restart. Both halves in the same restart", "question:placeCard checks the edge belongs to this question before writing: a position is meaningless away from the board it was chosen on", "the board carries data-wr-drop-question and the panel already subscribes to a notebook:changed topic, both unused until N07 publishes. reloadBoard() refreshes only the cards, because a card arriving must not replace a half-typed body with what was last saved"], "criteria_advanced": ["N06"], "artifacts_created": ["packages/database/src/migrations/008_desk_board.ts", "packages/database/src/repositories/board.ts", "apps/desktop/src/renderer/desk-board.tsx", "tests/e2e/board.spec.ts"], "errors": [], "next_action": "N07: the preload owns the drop (webUtils.getPathForFile) and must forward the path on a channel the renderer cannot address -- wr:invoke would let a compromised renderer name any file. Dropped paths need admitting to the allow-list one file at a time, which B02 then reuses.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-dropped-files", "iteration_hint": "the path must not exist in a world the page can read", "started_at": "2026-07-28T21:18:00-0700", "finished_at": "2026-07-28T21:35:00-0700", "phase": "milestone-4", "actions_taken": ["the preload handles the drop itself: it shares the DOM but not the renderer's JS world, so webUtils.getPathForFile can turn a dropped File into a path that never exists where the page can read it. The board carries data-wr-drop-question and the preload reads the question id off the ancestor", "the path travels on wr:drop, NOT wr:invoke. The scouted plan said to reuse the existing channel; it would have been a hole -- the renderer can invoke any channel in the contract, so a channel taking a path lets a compromised renderer name ~/.ssh/id_rsa, have it added to the library and read it back over rrfile://. wr:drop is not exposed on the bridge, and [N07] asserts the renderer's attempt is refused. The handler still lives in router.ts", "not copying the file has a price and it is paid explicitly: rrfile:// refuses everything outside the roots, so adding a file admits that ONE path (SwappableRoots.admit + isAdmittedFile, remembered in settings and restored at startup). Admitting the containing folder was one line shorter and would have handed over everything beside the paper -- the integration test asserts the sibling is still refused, across a restart", "the E2E caught a real bug the integration tests could not see: setPointerCapture on pointerdown retargets the compatibility mouse events at the card, so the title button never heard the click and no card could ever be opened. A drag now begins on movement (3px) rather than on contact, and nothing is captured or prevented before that", "the preload's dragover/drop listeners are scoped to drags carrying files. A listener that cancelled every dragover would have quietly decided what happens to Dockview's own tab drags", "[N07] asserts the file did not move (same inode) and that no second copy exists anywhere in the workspace, then opens the dropped PDF from the board in a second process -- which can only work if the admission was remembered"], "criteria_advanced": ["N07"], "artifacts_created": ["apps/desktop/src/main/local-files.ts", "tests/integration/local-files.test.ts"], "errors": [], "next_action": "Library curation: B01 (removal recorded as a tombstone so a re-import does not resurrect it), B02 (reuses LocalFileLibrary.add plus dialog.showOpenDialog), B03 (annotations and links survive a removal), B04 (hash zotero.sqlite before and after).", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-library-curation", "iteration_hint": "a removal that the next import undoes is not a removal", "started_at": "2026-07-28T21:35:00-0700", "finished_at": "2026-07-28T21:52:00-0700", "phase": "milestone-4", "actions_taken": ["migration 009 adds removed_at to external_references, and that is the whole of B01: the table the import already consults to answer 'have I seen this key?' now answers 'yes, and it was removed on purpose'. ZoteroImporter.writeDocument reads it before the version check AND before force, and importItem returns before tags, collections, attachments and the extraction job -- an import that skipped only the documents row would re-link the PDF to a document nobody can see", "removal is a soft delete in one transaction: documents.deleted_at, tombstones on every provider key the document carries (its files too), and its search entries dropped. The annotations, the links and the desk-board position are the researcher's work and are not Zotero's to take away; library:listRemoved and library:restoreDocument are what make 'recoverable' true rather than technically true", "B04 writes a real SQLite database where Zotero keeps one and hashes it before and after a whole curation run. Size and mtime are asserted as well as the bytes, and that no -wal or -journal sidecar appeared: an opened database leaves traces even when nothing is written", "B02 has two ways in because the E2E suite runs in background mode, where a modal dialog would wedge an unattended process. The drop is the one Playwright can drive with a File the OS actually produced -- wr:drop now takes questionId: null for the library, marked by data-wr-drop-library -- and the dialog half (library:addFiles, dialog.showOpenDialog refused in background) is covered under vitest with the chooser injected. Stubbing the dialog over CDP would have asserted the stub", "adding a file that was previously removed restores it. The library row is idempotent by path, so without that the drop would add a file to a library that goes on not showing it, which reads as a broken drop", "the sidebar grew the controls the criteria imply: Remove on each library row, a From disk section with Add files... and a drop hint, and a Removed section that appears only when something is in it. ListRow takes an action rendered *beside* the row, because a button inside a button is markup the browser repairs by moving it somewhere else"], "criteria_advanced": ["B01", "B02", "B03", "B04"], "artifacts_created": ["packages/database/src/migrations/009_library_curation.ts", "tests/integration/library-curation.test.ts", "tests/e2e/library.spec.ts"], "errors": [], "next_action": "The graph, G01-G06. Decide where pan/zoom and the graph settings live (workspace_layouts.panel_state_json is per-panel and already exists) before writing either. G03's trap: a display name written through to documents.title is silently overwritten by the next import.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-graph-view", "iteration_hint": "a panel id dies with its tab, so it cannot be what a view survives on", "started_at": "2026-07-28T21:56:00-0700", "finished_at": "2026-07-28T22:20:00-0700", "phase": "milestone-4", "actions_taken": ["the decision NEXT_ACTION.md asked for is made and written down: the graph's settings and its viewport persist differently because they are different kinds of thing. Spacing, labels and depth are a preference about reading graphs in general -> one application-wide settings row. Pan and zoom are a fact about one paper's neighbourhood -> keyed by 'seedType seedId', 64 most recently moved. workspace_layouts.panel_state_json cannot answer G01 at all: it is keyed by panel id, and the panel id is gone the moment the tab the criterion closes is closed", "depth was REMOVED from LinkGraphPanelSchema and from openLinkGraph's arguments rather than left beside the setting. Two authorities for the same number drift the moment either is changed alone, and which one wins would have depended on whether the panel was restored or opened fresh", "the panel pans and zooms for real: a native wheel listener with {passive: false} (React registers wheel passively on the root, so onWheel cannot preventDefault and the panel scrolls instead of zooming), pointer-anchored so what is under the cursor stays there, and a drag that starts on a node is deliberately not a pan so node clicks still open documents", "the viewport write is debounced 150ms and flushed on unmount, because a wheel gesture is dozens of viewports and only the last is ever read -- and because G01 closes the tab straight after the gesture", "a bug caught before it shipped: the wheel effect keyed on a ref ran while the canvas was still unmounted (the panel shows a loading state until the neighbourhood arrives) and would never have attached a listener. The element is state now, not a ref", "the panel keeps the old graph on screen while it re-queries, so changing depth adjusts the view rather than blanking it and drawing a second one", "G02 asserts each setting by its effect and not by its record: depth by the hop count the ANSWER came back with, spacing by the neighbour's transform actually moving, labels by the count of drawn labels. Both criteria were mutation-checked -- a null viewport from graph:getView fails G01, and storing the depth while pinning the query at 1 fails G02", "the 64-viewport bound is asserted where E2E cannot reach it: tests/integration/graph.test.ts drives 65 seeds through the real router and asserts the oldest is evicted, that re-panning an old one makes it recent again, and that a zoom the panel could not come back from is refused at the boundary and stored nowhere"], "criteria_advanced": ["G01", "G02"], "artifacts_created": ["packages/database/src/repositories/graph-view.ts", "tests/e2e/graph.spec.ts (G01, G02)", "tests/integration/graph.test.ts (the graph view)"], "errors": ["pnpm lint failed after the E2E test was written and passed before it: no-useless-assignment rejects `let x = ''` filled inside a try and read after it. Declared `let x: T | undefined` instead. Run lint after writing tests, not only after writing source"], "next_action": "G03: a node display name that does not write through to documents.title -- assert the title after setting a name AND after a re-import. Then G04 (icon from a local image over rrfile://), G05 (card art off by default, one host, second request cached, README names both exceptions), G06 (compound nodes -- the work is the query returning parentage).", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-node-names", "iteration_hint": "the title is Zotero's field, and a name written there reverts without saying so", "started_at": "2026-07-28T22:20:00-0700", "finished_at": "2026-07-28T22:30:00-0700", "phase": "milestone-4", "actions_taken": ["migration 010 adds graph_node_names(entity_type, entity_id, display_name), keyed by the pair and with no foreign key, because a node is anything the graph can draw -- a highlight is as renameable as a paper and has no title field to write through to at all", "GraphNode grows displayName BESIDE title rather than replacing it. The view draws the name and still knows what the thing is called, so the <title> tooltip says the real one and a renamed node does not hide what it is", "the names for one neighbourhood are read with an IN over the node ids already in hand, not as a whole-table select -- same bound the traversal follows", "the test does the thing the criterion is actually about: a real Zotero import supplies the title, the node is renamed, and then a FORCE re-import runs, which is the run that rewrites every field Zotero owns. Mutation-checked -- adding a documents.update to the handler fails it on the title assertion", "a rename has a way in, or it would be a channel nobody can reach: the graph toolbar has a Name field bound to the seed node, placeholdered with the document's own title so an empty field reads as 'called what it is called'. It commits on Enter or blur, not per keystroke, and the architectural test asserts the panel calls graph:setNodeName and never document:update"], "criteria_advanced": ["G03"], "artifacts_created": ["packages/database/src/migrations/010_graph_node_names.ts"], "errors": [], "next_action": "G04: a node icon from a local image, served over rrfile://. LocalFileLibrary.add already admits one path and mints a document; an icon is that plus a reference from the node \u2014 likely migration 011 adding icon_document_id beside display_name. Then G05, G06, K01-K03.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-node-icons", "iteration_hint": "an image reference is a file id, because that is the only kind the renderer can be handed", "started_at": "2026-07-28T22:30:00-0700", "finished_at": "2026-07-28T22:50:00-0700", "phase": "milestone-4", "actions_taken": ["migration 011 puts the icon in graph_node_icons rather than as a column on graph_node_names: display_name is NOT NULL there, so a node with a picture and no name would need that constraint dropped, and SQLite drops a constraint only by rebuilding the table -- copying every row of something the installed library already carries to save one indexed lookup. Names and pictures are also given up separately, so clearing one is a DELETE rather than a column nulled beside a value somebody still wants", "the icon is a document_files id and never a path, exactly as a notebook's cover_file_id is. The image is an ordinary local file the library was given, so it already has a row, and rrfile://<file id> is the only image reference the renderer can be handed", "graph:setNodeIcon refuses two things and neither is a formality: a file id the library does not hold (a broken picture from the moment it is set) and a file whose mime type is not an image (a PDF behind an <image> element is a way of asking for a document's bytes that has nothing to do with illustrating anything)", "getting the image IN is a drop, not a channel: graph:iconChoices lists what the library already holds, so the renderer chooses between pictures it can already draw and never names a file. The integration test asserts a path in the fileId slot is refused by the contract, and that no graph: channel takes anything path-shaped", "the E2E proves the bytes actually travelled: a real 16x16 PNG (tests/fixtures/node-icon.png, generated as real PNG bytes) is dropped on the library from outside every allowed root, chosen in the toolbar, and the node reports data-icon-loaded=true -- which React sets from the <image> element's own load event, so it is true only once Chromium fetched over rrfile://. The file id is read out of the database the app is writing, never scraped from the DOM", "the picker only ever offers images: db.files.listImages joins documents and excludes removed ones, asserted by removing the picture from the library and finding it off the list", "the drop mechanism is now one helper (tests/e2e/support/drop.ts) instead of three copies; board.spec.ts and library.spec.ts call it too, and the whole 57-spec E2E suite is green"], "criteria_advanced": ["G04"], "artifacts_created": ["packages/database/src/migrations/011_graph_node_icons.ts", "tests/fixtures/node-icon.png", "tests/e2e/support/drop.ts", "tests/e2e/graph.spec.ts (G04)", "tests/integration/graph.test.ts (a node's picture)"], "errors": [], "next_action": "G05: card art off by default, one allow-listed host, the main process fetches and caches to disk, and the SECOND request must not leave the machine. README names both network exceptions. Decide where a cached icon's document_files row lives -- a library row per fetched icon would be noise. Then G06 (compound nodes) and K01-K03.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-card-art", "iteration_hint": "the second exception is bounded by counting how often the app leaves the machine", "started_at": "2026-07-28T22:50:00-0700", "finished_at": "2026-07-28T23:10:00-0700", "phase": "milestone-4", "actions_taken": ["cardArt:fetch takes a card's NAME, never a URL. The host is one constant built in the main process, so 'one allow-listed host' is a property of the code rather than a promise about the caller -- an otherwise identical {url} channel would have been a request-forgery hole aimed out of the main process", "the switch is refused until cardArt:disclosure has been acknowledged, enforced in card-art.ts and not in a component, the same order and the same reason as agent:enable. Turning it off never needs the ceremony; turning it back ON afterwards does not either, because asking twice teaches people to click past", "the fetch is injected and the test counts its calls, because the count IS the criterion: three requests for the same art, one of them after an application restart, leave the machine exactly once. A cache that lived in memory would answer the second and not the third", "a reply is refused by an allow-list of four content types, not by an image/ prefix test -- image/svg+xml passes that test and carries script, and rrfile:// would then serve executable content behind an <img>. The check happens BEFORE any byte is written, so a refused reply never spends time in the directory rrfile:// serves from; the test asserts the cache directory is still empty afterwards", "every cached picture hangs off ONE document with source='card-art' rather than one document per picture. documents.list, count, countCreatedSince and listImages exclude that source -- count in particular, because it is the number the librarian's own disclosure tells the researcher it is about to send", "the cache root is created eagerly in createServices, before SwappableRoots is built: allowedRoots resolves through symlinks and keeps a not-yet-existing directory LEXICALLY, so on macOS a card-art root made later resolves to /private/var/... while the root naming it says /var/... and rrfile:// refuses every picture in it", "README.md and docs/SECURITY.md now name both exceptions; the local-first sentence was a promise with an unlisted exception until they did"], "criteria_advanced": ["G05"], "artifacts_created": ["apps/desktop/src/main/card-art.ts", "tests/integration/card-art.test.ts", "packages/shared-types: CardArtStatusSchema, CardArtDisclosureSchema, four cardArt: channels"], "errors": [], "next_action": "G05 has no way in from the renderer: the graph toolbar needs the disclosure-then-switch affordance (copy the Librarian panel's shape) and a field that asks for art by card name. Then G06 (compound nodes) and K01-K03.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-card-art-affordance", "iteration_hint": "a network feature nothing points at is worse than a feature nothing points at", "started_at": "2026-07-28T23:10:00-0700", "finished_at": "2026-07-28T23:20:00-0700", "phase": "milestone-4", "actions_taken": ["the graph toolbar has the control in the order of the decision: off shows only 'Card art\u2026', reading the disclosure is what makes the switch appear under the prose, and on is a field that takes a card's name", "the architectural test asserts the panel contains no 'https://' and not the host itself -- and it caught a comment of mine that named the host, which is exactly the drift it is for: a component with its own copy can go on naming a host after the code has stopped meaning it", "the disclosure is fetched on demand rather than with the status, so opening a graph is one request when the answer is almost always 'still off'", "the disclosure draws over the graph rather than in the toolbar row: it is several sentences and a settings bar that grew to hold them would push the graph down every time somebody read it", "57 E2E green, 618 vitest green, verifier 142/148 -- G05 now passes there"], "criteria_advanced": ["G05"], "artifacts_created": ["apps/desktop/src/renderer/graph-panel.tsx (CardArt control)"], "errors": [], "next_action": "G06: compound nodes. GraphNode grows a parentId and the traversal already knows an annotation's document (EntityResolver.describe returns documentId); the work is in the query returning parentage, not in drawing. Then K01-K03, then the milestone-4 audit.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-compound-nodes", "iteration_hint": "a highlight belongs to its paper, so the query says so and the drawing follows", "started_at": "2026-07-28T23:15:00-0700", "finished_at": "2026-07-28T23:40:00-0700", "phase": "milestone-4", "actions_taken": ["GraphNode grew parent: {entityType, entityId} | null, set in GraphRepository.neighbourhood from the description the node already resolves to (EntityResolver.describe returns documentId) and ONLY when that document is in the same bounded answer -- a parent the node cap dropped is reported as no parent, because the view cannot box something it was not sent and Cytoscape throws on a container missing from the elements", "@wr/graph turns parentage into Cytoscape's own: createGraph drops an unknown or self parent, layoutPositions skips children when building the rings and orbits them round their container instead, and groupBoxes derives each rectangle from the FINAL positions -- the panel applies spacing after the layout, so a box computed before that is a rectangle the contents have moved out of", "a highlight is no longer placed by its hop count: the far paper's highlight is two hops from the seed and would have been drawn a whole ring away from the paper it is part of, which is precisely the inference G06 exists to remove", "the panel draws the boxes behind the edges and annotates every edge with the group each end sits in (data-source-group/data-target-group/data-crosses-groups), so 'edges cross between groups' is a distinction in the drawing rather than a label on every line", "the E2E highlights both corpus pages by real DOM-Range selection over their prose, then RESTARTS the app before graphing: highlighting selects the highlight and the graph opens on what is selected, and nothing in the app ever clears selectedAnnotationId, so a same-session graph seeds on the annotation", "containment is asserted geometrically -- each box encloses its own paper and highlight and neither swallows the other paper -- from data-x/data-y in the graph's own coordinates, with every id read out of the database the app is writing", "the wikilink edge id is read in the SECOND launch: the corpus is re-derived at every start, so a link id kept from the first window names a row that has been replaced. Document ids are stable and are not"], "criteria_advanced": ["G06"], "artifacts_created": ["packages/graph/src/index.ts (parentage, child orbits, groupBoxes)", "packages/shared-types/src/domain.ts (GraphNodeParentSchema)", "packages/database/src/repositories/graph.ts (parent in the neighbourhood answer)", "apps/desktop/src/renderer/graph-panel.tsx + shell.css (group boxes, edge group attributes)", "tests/e2e/graph.spec.ts [G06], tests/integration/graph.test.ts [G06] x2, packages/graph/test/graph.test.ts [G06] x4"], "errors": [], "next_action": "N09-N11 and B05: added to docs/MILESTONE4.md and armed in the verifier by the last two commits, which wrote no code. The journal is still a sidebar; N09 moves it into the workspace, N10 gives it every day since the project began, N11 makes the day's entry a block notebook over one markdown document. Then K01-K03, then the milestone-4 audit.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-journal-page", "iteration_hint": "a day's thinking is not a filter, and a project's first day is not its first entry", "started_at": "2026-07-28T23:36:00-0700", "finished_at": "2026-07-29T00:02:00-0700", "phase": "milestone-4", "actions_taken": ["N09: the journal is a workspace page. New panel kind `journal` (descriptor carries nothing on purpose -- a page opens on today, and the day you were reading is a reading position, not layout), singleton so the calendar moves the one page between days, COMMAND_IDS.openJournal in place of toggleJournalSidebar, and the activity button is lit by an open journal panel rather than a sidebar boolean", "the journal left SidebarState entirely: LEFT_SIDEBARS is three now, and a workspace persisted with `journal: true` simply loses the key on the way through zod", "the page is a grid -- the day's entry takes the width, the calendar sits in a 220px margin -- and [N09] measures it against an open PDF reader rather than against a number", "N10: the calendar starts at journal.projectStart(), the earliest of this database's own creation day (MIN(schema_migrations.applied_at), converted to a LOCAL day) and any older entry. `firstDate` is gone from journal:loggedDates; a start derived from the entries is null on a journal nobody has written in, which is exactly the case the criterion is about", "[N10] E2E gives the workspace a past through seedJournalEntry() before launch, because the calendar offers no way to reach a day before its own start: one entry twelve days back, then the eleven-day run is expanded and every day between is asserted present, and writing on a middle day leaves today untouched", "the block model's pure core landed with 8 tests: parseBlocks/serializeBlocks round-trip, an unterminated fence keeps every character, an image with a caption beside it stays prose", "60 E2E green, 633 vitest green, verifier 146/152"], "criteria_advanced": ["N09", "N10"], "artifacts_created": ["packages/workbench/src/layout.ts (JournalPanelSchema; journal out of SidebarState)", "apps/desktop/src/renderer/journal-panel.tsx (JournalPanel, the page layout)", "packages/database/src/repositories/journal.ts (projectStart)", "apps/desktop/src/renderer/journal-blocks.ts + journal-blocks.test.ts", "tests/e2e/journal.spec.ts [N09] [N10], tests/integration/journal.test.ts [N10]"], "errors": [], "next_action": "N11: replace the day's single textarea with the block list over journal-blocks.ts, and derive the commands margin from the day's code blocks. Then B05, K01-K03, then the milestone-4 audit.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-block-notebook", "iteration_hint": "blocks are a view over the day's markdown, and the commands margin is a view over the blocks", "started_at": "2026-07-29T00:02:00-0700", "finished_at": "2026-07-29T00:12:00-0700", "phase": "milestone-4", "actions_taken": ["N11: the day's entry is a block notebook. The single textarea is gone; the page renders parseBlocks(markdown) as text/code/image blocks, each clicked into its own editor, and every commit serializes the WHOLE day and re-parses what the main process answered with -- so the stored markdown is the authority and a block edited into a fence becomes a code block without a reload", "text and image blocks render through renderMarkdown from @wr/markdown-reader (React elements, never an HTML string); an image resolves over rrfile:// and the window's img-src allows nothing else, so a day can carry a figure without the app reaching the network", "the commands margin is DERIVED from the day's code blocks -- clicking one opens the block it came from, and a copy button puts it on the clipboard. A stored {cmd, desc} list (what the reference notebook has) would be a second copy of the same fact", "no `+ image` affordance: nothing on the page can put bytes on the machine, so an insert button for one would be pretending. Image blocks render; creating one needs the wr:drop path, which N11 does not ask for", "block keys are a counter, not the index: blocks are re-parsed after every write, and keying by index makes React reuse the textarea of a block that has become a different one", "a selectedRef guards both loadDay and commit -- clicking another day blurs the editor, so a write and a day switch are in flight at once and the slower one must not drop yesterday's blocks onto today's page", "[J01], [J03] and [N10] now drive blocks instead of the textarea; 61 E2E green, 633 vitest green, verifier 146/152"], "criteria_advanced": ["N11"], "artifacts_created": ["apps/desktop/src/renderer/journal-blocks.ts + journal-blocks.test.ts (8 tagged tests)", "apps/desktop/src/renderer/journal-panel.tsx (block list, commands margin)", "apps/desktop/src/renderer/shell.css (.wr-blocks, .wr-block, .wr-commands)", "tests/e2e/journal.spec.ts [N11]", "state/DECISIONS.md (2026-07-29 \u2014 the journal is a page, and a day is a view over one document)"], "errors": [], "next_action": "B05: a per-collection import action in ZoteroScopePicker. zotero:import already takes { collection }; the open question is how the E2E observes a real import with Zotero not running and the endpoint fixed at 127.0.0.1:23119. Then K01-K03, then the milestone-4 audit.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-journal-delivered", "iteration_hint": "a criterion re-specified after it was built stays green, because the verifier matches the tag and not the sentence", "started_at": "2026-07-29T00:12:00-0700", "finished_at": "2026-07-29T00:20:00-0700", "phase": "milestone-4", "actions_taken": ["packaged and installed: /Applications/wiki-reader.app now carries N01-N11, B01-B04 and G01-G06. The previous bundle was RENAMED rather than deleted, because the researcher's instance (PID 69266) is running out of it and keeps its inodes until restart", "found while scoping B05: docs/MILESTONE4.md re-specified B01 in commit 16ab5ac -- 'importing its collection again brings it back', 'whatever recorded the old behaviour goes, list included' -- AFTER the tombstone was built in 9441870, and nothing was rebuilt. The four [B01] tests still assert 'a removed document is not brought back by the next import', the sidebar still has a Removed section and library:restoreDocument still exists. verify_completion.py matches on the tag, not the description, so it is all green", "B01 moved out of criteria_verified and into criteria_remaining with a known_defects entry: it is not met as the criterion is now written", "B05's approach decided and written down: a loopback fixture server on an ephemeral port (Node http over fixtureFetch, children relocated onto the temp Zotero dir) with a new loopback-validated WR_ZOTERO_ENDPOINT, and the observable is B01's own round trip -- remove a document, import its collection in one action, find it back"], "criteria_advanced": [], "artifacts_created": ["/Applications/wiki-reader.app (replaced)", "state/NEXT_ACTION.md (the B01 finding and the B05 plan)"], "errors": [], "next_action": "B01+B05 together, then K01-K03, then the milestone-4 audit.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-removal-round-trip", "iteration_hint": "a removal is not a blacklist: the collection it came from is the way back", "started_at": "2026-07-29T00:20:00-0700", "finished_at": "2026-07-29T01:05:00-0700", "phase": "milestone-4", "actions_taken": ["B01 rebuilt to the criterion that replaced it. The tombstone stays -- something has to record 'not now' while the highlights stay on the document -- but what LIFTS it changed: ZoteroImporter passes a removed item over on a whole-library run (force included) and RESTORES it on a run scoped to a collection, because every item surviving the scope filter was asked for by name. Reading 2 of the two the criterion allowed: reading 1 (any import clears it) means the nightly sync resurrects everything removed that morning", "the restore is the full write path, never the unchanged short-circuit: the Zotero version says nothing changed upstream, and what changed is here -- the row was hidden, the search entries dropped, the files' provenance tombstoned. outcome 'restored' counts into ImportSummary.documentsRestored and queues index-fts, because a document you cannot find is only half back", "the blacklist went: library:listRemoved, library:restoreDocument, the Removed sidebar section, RestoreToLibrary and library.removed in the workspace store. The removal's status line now names the way back instead of naming a section", "B05: each ZoteroScopePicker row imports its own collection in one action. Ticking is a standing decision about scope and pressing Import is one import now, so they are two controls -- and the button never writes the remembered scope, which [B05] asserts. ImportFromZotero's runner became useZoteroImport(), so the ECONNREFUSED remedy has one home", "WR_ZOTERO_ENDPOINT, loopback-only (resolveZoteroEndpoint: parsed hostname, no credentials, http(s) only; a refused value is logged and the default stands). Zotero's port is a preference so this is honest production configuration -- and the E2E finally has a way to drive a real import: startZoteroApi() binds fixtureFetch to an ephemeral 127.0.0.1 port, never 23119, where a Zotero someone starts would collide and the test would read their real library", "[B05] is B01's round trip end to end: remove a filed document, press Import on its collection row, find it back under the SAME id. The collection to press is read from the database before launch, because the renderer never sees a collection id", "637 vitest green (53 files), 62 E2E green, typecheck and lint clean, verifier 147/152 -- K01-K03 and the milestone-4 audit remain"], "criteria_advanced": ["B01", "B05"], "artifacts_created": ["packages/zotero-adapter/src/importer.ts (ImportMode, outcome 'restored', documentsRestored)", "apps/desktop/src/main/zotero-endpoint.ts + zotero-endpoint.test.ts (3 tagged tests)", "tests/e2e/support/zotero-api.ts (the recorded API over a real loopback socket)", "tests/e2e/library.spec.ts [B05], tests/integration/library-curation.test.ts ([B01] x5)", "state/DECISIONS.md (2026-07-29 -- the removal round trip, and WR_ZOTERO_ENDPOINT)"], "errors": [], "next_action": "K01-K03, then the milestone-4 audit. K01 and K02 need a gesture decided first (how the other end of a link is named from a reader, and where a note made from here hangs); K03 should read the keybinding registry rather than a hand-written table.", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m4-removal-round-trip-delivered", "iteration_hint": "the researcher runs the bundle, so a green criterion is not delivered until the bundle is", "started_at": "2026-07-29T01:05:00-0700", "finished_at": "2026-07-29T01:20:00-0700", "phase": "milestone-4", "actions_taken": ["packaged and installed: /Applications/wiki-reader.app now carries B01's round trip and B05's per-collection import. The previous bundle was renamed to .wiki-reader-superseded-20260729-002922.app rather than deleted -- PID 69266 is running out of the old one and holds its inodes until the researcher restarts", "three superseded bundles are now on disk at ~347M each. Recorded in state rather than deleted: they are the researcher's, and one of them is the app they are using right now"], "criteria_advanced": [], "artifacts_created": ["/Applications/wiki-reader.app (replaced)"], "errors": [], "next_action": "K01-K03, then the milestone-4 audit.", "git_commit": null, "pushed": false, "state_version": 1}
{"at": "2026-07-29T01:58:00-0700", "milestone": "milestone-4", "criteria": ["K01", "K02", "K03"], "what": "The three surfaces that point at mechanisms already shipped: a link picker over link:create where the relationship is chosen and never defaulted, a New note from here that resolves the selected highlight before the document and writes note plus edge in one note:create, and a command list rendered live from the command and keybinding registries, reached from a status-bar button rather than a chord.", "tests": ["[K01] links the open document to another one, with a relationship that is chosen", "[K02] makes a note from the highlight under the reader, linked to it", "[K03] shows every keybound action, and its key, without needing the key"], "gates": {"typecheck": "pass", "lint": "pass", "unit": "645 pass", "e2e": "65 pass"}, "notes": "@wr/workbench became a root devDependency so [K03] asserts against DEFAULT_KEYBINDINGS itself rather than a hand-written sheet. WorkbenchHost grew showCommands, promptDocumentLink, createDocumentLink and createNoteFrom. Every criterion in docs/MILESTONE4.md is now green; only the milestone-4 audit remains."}
{"at": "2026-07-29T01:25:00-0400", "milestone": "milestone-4", "criteria": [], "what": "The milestone-4 audit -- four independent lenses over fde3e38..c072375 -- and its three majors fixed, each confirmed by reverting the fix and watching the new test fail. (1) zotero:import falls back to the remembered picks, so with any standing scope ticked the plain Import button was a scoped run and lifted every removal inside it; ScopeOrigin now distinguishes a collection named in this action from the standing picks, and only 'named' restores. (2) Nothing had ever drained an index-fts job, so a restored document and all its annotations stayed unfindable forever while [B01] asserted the queue row as evidence; SearchIndexer.reindexDocument, an index-fts drain in pipeline.ts, and the local-file restore path enqueuing one. (3) artUrl asks for format=image, which Scryfall answers WITH a redirect, so redirect:'follow' let the reply choose the host; redirects are now followed by hand with the allow-list on every hop, scheme and port included, bounded at 3, and cards.scryfall.io is named in the disclosure, README.md and docs/SECURITY.md.", "tests": ["[B01] a plain import with a standing scope leaves a removal alone", "[S-cardart] a redirect off the allow-list is refused"], "gates": {"typecheck": "pass", "lint": "pass", "unit": "650 pass", "e2e": "65 pass (before fixes)"}, "notes": "The prior session was killed before committing; this session verified the tree (typecheck 0, lint 0, 650 unit) and checkpointed it. Eleven minors remain open, listed in reports/AUDIT.md and experiment_state.json. Next: pnpm package, replace /Applications/wiki-reader.app, then verify_completion.py."}
{"checkpoint_id": "cp-m4-complete", "iteration_hint": "milestone 4 closed: bundle delivered, verifier green", "started_at": "2026-07-31T19:15:00-0700", "finished_at": "2026-07-31T19:40:00-0700", "phase": "milestone-4", "actions_taken": ["pnpm package (exit 0), superseded the installed bundle and installed the 2026-07-31T19:24 build carrying K01-K03 and the three audit fixes", "verify_completion.py: 152/152 in 128.4s, exit 0, MILESTONE COMPLETE"], "criteria_advanced": [], "artifacts_created": ["/Applications/wiki-reader.app (replaced)"], "errors": [], "next_action": "no milestone 5 yet; only the minor audit findings remain", "git_commit": null, "pushed": false, "state_version": 1}
{"checkpoint_id": "cp-m5-p01-p05", "iteration_hint": "milestone 5: the notebook is the paper (P01-P05) and 'question' retires", "started_at": "2026-07-31T19:50:00-0700", "finished_at": "2026-07-31T20:35:00-0700", "phase": "milestone-5", "actions_taken": ["migration 012 rekeys journal_entries to (notebook_id, date), rewrites journal link endpoints to <notebook>:<date>, adds questions.journal_start; existing days are adopted rather than dropped", "journal channels all name their notebook; journal:advancesQuestion becomes journal:advancesNotebook; notebook:directory added", "a directory page lists every notebook and opens either door (page or journal); the activity bar gains Notebooks and the queue becomes 'What next'", "a picture dropped on a day's blocks is added where it lies and written into the day's markdown by the main process, over a new data-wr-drop-journal target", "clicking a block resolves the click with caretRangeFromPoint and maps it back to markdown source with sourceOffsetFor; focus set in the ref, not autoFocus", "'question' retired from every researcher-facing string, including the blank page's first heading; asserted by reading the screen"], "criteria_advanced": ["P01", "P02", "P03", "P04", "P05"], "artifacts_created": ["packages/database/src/migrations/012_notebook_journals.ts", "apps/desktop/src/renderer/notebook-directory.tsx", "tests/e2e/notebooks.spec.ts"], "errors": [], "next_action": "F01-F03, H01-H04 (H01 hides a bug: saved-page highlighting is broken today), D01/D02. Then audit milestone 5, package, install.", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "657 pass (53 files)", "e2e": "70 pass (20 files)"}}
{"checkpoint_id": "cp-m5-f01-f03", "iteration_hint": "milestone 5: the wiki is a place, the view is focused (F01-F03)", "started_at": "2026-07-31T20:30:00-0700", "finished_at": "2026-07-31T21:00:00-0700", "phase": "milestone-5", "actions_taken": ["graph:overview added: the only unseeded graph channel, nodeLimit required rather than defaulted, ranked by degree, reports totalNodes beside elidedNodes; added to the [W10] empty-request loop along with graph:focus", "graph:focus added: one file, its highlights in reading order and the files it reaches, with two separate caps so neither half starves the other; a neighbour reached only through a highlight is flagged throughAnnotation", "overviewPositions (golden-angle spiral by rank) and focusPositions (two bands, inner ring always inside the outer) added to @wr/graph and proved headlessly", "two new panel kinds: wiki (a page, singleton, stateless) and focus (one tab, re-seated on every file)", "RESEATED_PANEL_KINDS: a reveal plan now carries a descriptor for panels whose descriptor is their subject, and applyPlan applies it - readers deliberately excluded so they keep the zoom and position they have earned", "wiki-panel.tsx and focus-panel.tsx over a shared graph-canvas.tsx; both added to the source-scanning test that keeps every graph surface off the link tables", "activity bar gains Wiki and Focus; both commands are in the registry and therefore in the palette (no keybinding - D01 designs the scheme)", "eslint renderer-boundary rule and docs/ARCHITECTURE.md corrected: @wr/graph is shared, not main-only"], "criteria_advanced": ["F01", "F02", "F03"], "artifacts_created": ["apps/desktop/src/renderer/wiki-panel.tsx", "apps/desktop/src/renderer/focus-panel.tsx", "apps/desktop/src/renderer/graph-canvas.tsx", "tests/e2e/wiki.spec.ts", "tests/e2e/support/corpus.ts"], "errors": [], "next_action": "H01-H04 (H01 hides a bug: saved-page highlighting is broken today) and D01/D02. Then audit milestone 5, package, install.", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "676 pass (53 files)", "e2e": "73 pass (21 files)"}}
{"checkpoint_id": "cp-m5-h01-h04", "iteration_hint": "milestone 5: links that hold on (H01-H04)", "started_at": "2026-07-31T21:00:00-0700", "finished_at": "2026-07-31T21:40:00-0700", "phase": "milestone-5", "actions_taken": ["H01 root cause: the archive frame is sandbox=\"\" with an opaque origin and no script, so a selection in it cannot reach the app by any open route - the anchoring layer was already green under [W05], the way in was never built and cannot be built by copying the markdown reader", "selection taken from Chromium's context-menu params in the main process and published as webpage:selection (document id + words, never the frame url); the sandbox, the served CSP and the request blocking are unchanged, recorded in state/DECISIONS.md and one row of docs/SECURITY.md", "document:getSnapshotText added; the article panel builds an HtmlAnchor with readerMode 'original' stated rather than read off the descriptor (which still claims 'readability', a rendering this app does not build), and lists each highlight beside the page with whether it still resolves", "H02: promptEntityLink/createEntityLink take EntityRefs, store.linkDraftSource replaces linkDraftSourceId, linkTypesFor(source,target) is the one vocabulary the picker offers and the command checks", "new link type annotation-references-document - not annotation-belongs-to-document, which every highlight already carries and which create() returns idempotently, so reusing it would report success for a link never written", "H03: LinksRepository.findForDocument runs scopeClause with no type filter and names the near endpoint; derived edges with both ends inside are omitted as bookkeeping; new ledger panel, reseated like focus, with the file and each highlight linkable from it", "link:create/link:delete now publish library:changed with a new 'link' reason so the ledger and every graph surface redraw", "H04: the picker's second tab is WikiPanelBody + FocusPanelBody with an optional picking prop and a third SceneNode action, 'pick' - a file node or one of its annotations, with the crawl kept to reach another paper's highlights", "[L09]'s 'a highlight resolves to its document' case became two stronger cases: the highlight stays the highlight, and the reader's strip asks for the file by name"], "criteria_advanced": ["H01", "H02", "H03", "H04"], "artifacts_created": ["apps/desktop/src/renderer/ledger-panel.tsx", "tests/e2e/ledger.spec.ts", "tests/integration/highlight-links.test.ts"], "errors": [], "next_action": "D01/D02 - the keyboard scheme across every milestone-5 surface, and a help page rendered from the registries. Then audit milestone 5, package, install.", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "682 pass (54 files)", "e2e": "76 pass (22 files)"}}
{"checkpoint_id": "cp-m5-d01-d02", "iteration_hint": "milestone 5: fast in the hand (D01, D02)", "started_at": "2026-07-31T21:40:00-0700", "finished_at": "2026-07-31T22:05:00-0700", "phase": "milestone-5", "actions_taken": ["the keyboard became a scheme rather than eleven unrelated chords: four families chosen by the verb - go to a page (Cmd+Shift+<first free letter of its name>), go to a file (Cmd+P), follow the links on what you are reading (the function row), make something from here (Cmd+Alt+<letter>); panes and retracing keep the conventions every app shares", "each rule in DEFAULT_KEYBINDINGS declares its family, and ResolvedKeybinding carries it: inferring the family from the modifiers is free and wrong, because Cmd+Shift+W closes a group and shares its modifiers with every page chord", "WorkbenchHost.notebookInHand(): openNotebook and openJournal demanded an argument a keystroke cannot carry, so neither could be bound; the host answers (the notebook you are on, else any open, else the first in hand order) and the activity bar's Journal button now runs the same command with no args", "wr.goToFile + the file palette (Cmd+P): filter, arrows, Enter - the keyboard's way into a reader, since a document is one of thousands rather than one of a dozen", "wr.openReading (Cmd+Shift+R): the way back out of the pages, deliberately not #subject so a pointer resting on a citation cannot change where 'back' means", "new help panel kind, singleton and stateless; rendered from commands.all() and the new keybindings.all(), grouped by declared family, with row counts on the element so a hand-written page cannot pass [D02]", "help reachable from the status bar beside Commands, printing its own chord - the page that explains the scheme must not be reachable only through the scheme", "[P01] 'no surface calls a notebook a question' extended to read the help page, now the app's fullest inventory of its own vocabulary"], "criteria_advanced": ["D01", "D02"], "artifacts_created": ["apps/desktop/src/renderer/help-panel.tsx", "tests/e2e/keyboard.spec.ts"], "errors": ["one unit run out of five reported a single failure and printed no name for it; four consecutive clean runs since, and nothing in the diff is timing-dependent - recorded rather than explained"], "next_action": "Not code: audit milestone 5 into reports/AUDIT.md (header must claim milestone 5), run scripts/verify_completion.py, then pnpm package and swap /Applications.", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "691 pass (54 files)", "e2e": "78 pass (23 files)"}}
{"checkpoint_id": "cp-m5-unify", "iteration_hint": "unification sweep: use the abstractions that exist, delete what nothing calls", "started_at": "2026-07-31T22:00:00-0700", "finished_at": "2026-07-31T22:50:00-0700", "phase": "milestone-5", "actions_taken": ["graph-panel.tsx was the surface graph-canvas.tsx was extracted from and the only one never folded back onto it: it kept its own scene size, zoom bounds, rounding, pointer-to-viewBox arithmetic, wheel/drag handlers and node rendering; useSceneGestures is now controlled (it reports a move and says nothing about where the value is kept) and useSceneView is that hook plus local state, which is what the wiki page and focused view wanted", "SceneViewportGroup writes the viewport transform and its three data-* from one value, so a surface cannot pass its own assertions while drawing something else; SceneViewport was GraphViewport with the same three fields and now is it", "wiki-panel built a headless Cytoscape instance per redraw and read one number off it that nothing used; focus-panel returned a model nobody destructured", "graph.spec.ts folded onto tests/e2e/support/corpus.ts as NEXT_ACTION said to; library.spec.ts had a byte-identical openLibrary", "eight exported names with exactly one occurrence in the tree deleted (PEEKABLE, CHANNEL_NAMES, planTargetsReader, entityKindOf, keystrokesEqual, PARENTABLE_ENTITY_TYPES, serializeHtmlAnchor, serializeMarkdownAnchor); isAttachment was a ninth and was dead because importer.ts inlined its predicate", "makeHighlight + SelectionBar: three readers each wrote the same seven steps behind the Highlight button and had already drifted; useReadingPosition: the PDF and markdown readers wrote the same debounce, comment and all", "Overlay + useCloseOnEscape: three overlays each carried the sheet, the scrim and the window-capture Escape listener; both palettes concatenated the library's three ingestion paths by hand", "GraphRepository prepared the same 'live links touching this entity' statement twice - now #edgesTouching, compiled once, which matters at once per frontier node; the Link->GraphEdge projection was written out at both queries", "defaultSidebars() parses SidebarStateSchema instead of two hand-written literals; normaliseSidebars/toggleSidebarState derive from LEFT_SIDEBARS instead of restating it", "LibraryRow, LedgerRows, DirectoryRow: three list sections, three ledger accounts and two directory lists each repeated a row verbatim, test ids included", "tests/integration/support/workspace.ts replaces eight copies of the harness class (366 lines); local-files keeps its own and now says why", "packages/database/src/repositories/graph.ts held a literal NUL byte in a template, so git treated it as binary and grep skipped it; written as an escape, same character", "CLAUDE.md and README layout tables were missing packages/graph and packages/markdown-reader and still called html-reader a milestone-2 stub"], "criteria_advanced": [], "artifacts_created": ["tests/integration/support/workspace.ts"], "errors": [], "next_action": "Unchanged: audit milestone 5 into reports/AUDIT.md (header must claim milestone 5), run scripts/verify_completion.py, then pnpm package and swap /Applications.", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "691 pass (54 files)", "e2e": "78 pass (23 files)", "verify_completion": "165/167 - the two failures are the milestone-4 audit header and a dirty tree, both pre-existing"}}
{"checkpoint_id": "cp-m5-design-pass", "iteration_hint": "designer's pass: drive every surface with Playwright, fix what is small, report what is not", "started_at": "2026-07-31T22:50:00-0700", "finished_at": "2026-07-31T23:20:00-0700", "phase": "milestone-5", "actions_taken": ["every surface screenshotted from the running app under WR_BACKGROUND=1 rather than read off the code: empty workspace, library, directory, notebook page, journal day, What next, librarian, wiki, focused view, a file's link graph, help, command list, all three readers, annotations, search", "a highlight on markdown or a saved web page was labelled 'Anchor broken' the moment it was made: only the PDF reader publishes resolutions, and AnnotationList collapsed 'a reader looked and failed' (null) with 'no reader looked' (absent), so the sidebar struck through a highlight visible on the page - describeAnchorHealth grew a fourth state, unknown, which shows no badge; the same bug at smaller scale silenced PDF pages that had not rendered yet", ".wr-graph__settings did not wrap, so in a narrow panel - the normal shape of the focused view - Hops, Spacing, Labels and Reset view sat past the panel edge, unreachable and unannounced", "the What next queue's six grid columns in a 280px sidebar left the title column one glyph wide and every notebook wrapped a letter per line down the panel; the row is a wrapping flex row and the status plus its two actions travel as one block", "the search panel showed a field and then nothing at all, which reads as a panel that failed to load; it now says what it searches and what a result does, and 'nothing matched' suggests what to try", "the journal's Advances heading stood over empty space while Commands beside it explained itself; it now says what would appear there and which of the two reasons it is empty for", "two-word buttons broke in half when squeezed (New/notebook, New note on/highlight, Card/art) and 'Put on the board' was clipped by a select claiming the whole row", "the empty workspace was one faint sentence pointing at a sidebar that may not be open; it offers the three ways in as the same registered commands a chord runs, each with its chord printed", "eight larger gaps written up as open-ended proposals in reports/DESIGN_GAPS.md - concept, not implementation, and none of them milestone-5 scope"], "criteria_advanced": [], "artifacts_created": ["reports/DESIGN_GAPS.md", "packages/annotations/src/anchor-health.test.ts"], "errors": [], "next_action": "Unchanged: audit milestone 5 into reports/AUDIT.md (header must claim milestone 5), run scripts/verify_completion.py, then pnpm package and swap /Applications.", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "695 pass (55 files)", "e2e": "78 pass (23 files)"}}
{"checkpoint_id": "cp-m5-vision-alignment", "iteration_hint": "alignment pass: where the criterion passed and the vision did not", "started_at": "2026-07-31T23:00:00-0700", "finished_at": "2026-07-31T23:45:00-0700", "phase": "milestone-5", "actions_taken": ["every surface driven again from the running app under WR_BACKGROUND=1, this time reading the milestone-4 and milestone-5 criteria against what the researcher said the app is for rather than against their own wording", "a highlight over a sentence containing a [[wikilink]] was never painted: render.tsx looked for the quote inside one run of plain text, and a chip, a bold word, inline code or a hard-wrapped source line all break the run - so on a wiki page the highlight was in the sidebar, in the focused view and in the database while the page in front of the researcher showed nothing; painting is per block now (flatten to atoms, fold the way normalizeText folded the quote, match, rebuild with marks - a text run is cut, a chip is wrapped whole)", "a notebook and its journal were two tabs spelled the same: the journal titled itself '<notebook> - today' and a tab strip truncates the tail, so both read 'Does spacing beat massin...'; it is 'Journal - <notebook> - <day>' now, the shape 'Focus - <file>' and 'Links - <file>' already use", "neither the notebook page nor its journal pointed at the other, though the directory row has both doors; the journal's owner line is the command that opens the page and the page has a Journal door, both through the registry", "a notebook made after 5pm in California said it started tomorrow: 'started' sliced ten characters off a UTC instant, the trap JournalRepository.start documents on the other side of the wire; localDay in journal-calendar.ts is now the one answer for both", "the wiki, the focused view and the link graph were filed under the Links category, so the help page listed the three places the milestone is about between Copy Internal Link and Find Incoming Links; they are a Graph category now", "seven larger misalignments written up under a Vision alignment heading in reports/DESIGN_GAPS.md: the wiki draws files and never highlights; nothing carries reading into a notebook; a hypothesis cannot be given evidence by hand; the ledger cannot see a highlight until something else already has; the reader never says a highlight has become structure; the notebook's page is the smallest region on its own page; the graph has no way to find a node by name"], "criteria_advanced": [], "artifacts_created": ["tests/integration/markdown-highlight-painting.test.ts"], "errors": [], "next_action": "Unchanged: audit milestone 5 into reports/AUDIT.md (header must claim milestone 5), run scripts/verify_completion.py, then pnpm package and swap /Applications.", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "701 pass (56 files)", "e2e": "78 pass (23 files)"}}
{"checkpoint_id": "cp-m5-audit-closed", "iteration_hint": "close the milestone-5 audit: 12 majors, each fixed with a test that fails when the fix is reverted", "started_at": "2026-07-31T23:50:00-0700", "finished_at": "2026-08-01T01:05:00-0700", "phase": "milestone-5", "actions_taken": ["read the four lens reports (reports/audit-m5-{notebooks,graph-views,links,security}.md) and reproduced every major before touching it", "the retired word: IpcCallError no longer prefixes the user-facing message with the channel name (the channel is a field), and nine notFound('question') sites say notebook - a missing notebook used to read 'question:notebook: question not found' on the page milestone 5 renamed", "the notebook directory re-reads on reveal (api.onDidVisibilityChange) and on journal/library/notebook changes; it was a useCallback([]) behind one useEffect and Dockview detaches rather than unmounts, so the shelf was as it had been when the tab was left", "migration 013 projects every anchor's position.start as annotation_anchors.text_start, and the focused view's ring orders by (page_index, text_start, created_at, id) - page_index is NULL for markdown and saved pages, so the ring had been creation order with a random ULID tiebreak and [F02] failed about one run in three", "the focused view's 'where it leads' is one grouped SQL query with no internal ceiling, so elidedNeighbours is exact: FOCUS_EDGE_LIMIT had cut the edges before ranking and under-reported a 3,000-neighbour file by a thousand", "graph:overview reads its edges once over the drawn set through an indexed temp table (EDGES_PER_NODE had decided which lines a complete-looking map had), takes an edgeLimit, and answers with totalEdges/elidedEdges; the wiki page shows the two elisions separately", "the overview ranking joins the deleted-endpoint set once instead of six correlated existence checks per row and groups each half along an index (272->122 ms at 1,000 files / 100,000 links; 2,750->230 ms for the whole answer on the dense corpus), counts only the kinds the page draws, and the panel no longer redraws for a highlight - which that change makes provably safe", "useSceneView(subject) returns the viewport to rest when the scene's subject changes, so a crawl no longer draws the next file wherever the last one was left", "locateNearest returns null when the quote is not in the text; HtmlAnchor.position is optional and a saved-page anchor with no verbatim match records no offsets and no context (it had been recording context cut from the top of the page, and never searching past hint+4000)", "LIVE_EDGE moved to repositories/live-edge.ts and the ledger query uses it, so a deleted highlight's link is not listed as a live connection while graph.focus drops it", "the article panel says how to highlight a saved page (the context menu is the only route out of the archive, and every other reader raises its bar on mouseup)", "tab activation runs one host rule for every reader kind and drops a highlight belonging to the file just left; the three highlight-activation handlers and annotation navigation set the file alongside the highlight", "each fix was mutation-checked: the fix reverted, the new test watched to fail, the fix restored (11 of 12 individually; the H01 hint's assertion names an element that exists only with it)", "reports/AUDIT.md gained the milestone-5 section at the top with Audited-commit f93477062c288983c6112b63f1240ae351799858 and Audited-milestone: 5; the four earlier sections keep no such tokens"], "criteria_advanced": [], "artifacts_created": ["packages/database/src/migrations/013_anchor_text_start.ts", "packages/database/src/repositories/live-edge.ts", "apps/desktop/src/renderer/ipc.test.ts", "reports/AUDIT.md (milestone-5 section)"], "errors": [], "next_action": "python3 scripts/verify_completion.py driven to 0, then pnpm package and swap the bundle in /Applications.", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "712 pass (57 files)", "e2e": "81 pass (23 files)"}}
{"checkpoint_id": "cp-m5-ship", "iteration_hint": "milestone 5 verified at 167/167 and the researcher's bundle replaced", "started_at": "2026-08-01T00:35:00-0700", "finished_at": "2026-08-01T01:10:00-0700", "phase": "milestone-5", "actions_taken": ["pnpm package under WR_BACKGROUND=1: electron-builder 25.1.8, electron 33.4.11, darwin arm64, exit 0 (logs/package-20260801.log)", "/Applications/wiki-reader.app renamed to /Applications/.wiki-reader-superseded-20260801-004638.app rather than deleted - pgrep showed the researcher running PID 7789 out of it, so it holds its inodes until they restart - and the fresh bundle dittoed in; the swap is confirmed by the renderer hash changing from index-DG0g_QxL.js to index-Byp7koRy.js", "python3 scripts/verify_completion.py exited 0: 167/167 in 168.1s at 55a1140, 712 unit tests and 81 e2e specs green, audit header answering for milestone 5, no unresolved critical or major findings, tree clean and HEAD on origin/main", "state/NEXT_ACTION.md rewritten for the next context: milestone 5 shipped and nothing left in it to build, the swap procedure spelled out (rename, never delete), the open list unchanged, and two traps added - the verifier needs a clean pushed tree so it can only go green after the checkpoint commit, and a full run is ~170s", "state/experiment_state.json records the new installed_build and last_verified_commit 55a1140"], "criteria_advanced": [], "artifacts_created": ["/Applications/wiki-reader.app (2026-08-01T00:46 build)", "logs/package-20260801.log", "logs/verify-20260801-a.log"], "errors": [], "next_action": "Milestone 5 is done and shipped. Do not start milestone 6 - it has not been written. The open list is the minors, the design gaps and the milestone-3 security items.", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "712 pass (57 files)", "e2e": "81 pass (23 files)", "verify_completion": "167/167, exit 0", "package": "pnpm package exit 0; bundle installed at /Applications/wiki-reader.app"}}
{"checkpoint_id": "cp-m6-notebook-writes-the-paper", "iteration_hint": "S01-S03: the notebook page becomes the block editor, LaTeX renders from a vendored KaTeX, and a highlight becomes a linked excerpt", "started_at": "2026-08-01T02:20:00-0700", "finished_at": "2026-08-01T03:00:00-0700", "phase": "milestone-6", "actions_taken": ["S01: the journal's block editor was ~140 lines welded into JournalView; it is one component now (apps/desktop/src/renderer/blocks.tsx) over one pure module (block-source.ts, renamed from journal-blocks.ts), and both surfaces are thin owners of a markdown document - value in, onCommit(markdown) out, and the owner answers with what was stored", "S01: the notebook page is a grid the way the journal already was - the writing column is 1fr and grows, the front matter, the sections and the claims are a fixed 240px margin, and the desk runs along the bottom because it is a pointer-dragged surface and 240px is not one; the outline stopped being a grey line of inert text and became the list of what the document actually has in it", "S01: a picture dropped on the page becomes a block the way one dropped on a day does - data-wr-drop-notebook-page in the preload as a *sibling* of the board's attribute (closest picks the innermost, so nesting would have stolen the board's drops), a notebookPage target on wr:drop, picturesAsBlocks shared by both surfaces, and 'page-drop' on notebook:changed so the page re-reads its body rather than its cards", "S01: the milestone-5 audit's picture-drop bug is fixed where it can now only be fixed once - mergeAppend(baseline, mine, theirs) reconciles an unsaved block with a document that changed underneath it, taking the appended tail and keeping what was typed", "S02: katex 0.18.1 as an ordinary bundled dependency with output:'mathml', trust:false, throwOnError:false; the HTML string it returns is parsed with DOMParser and rebuilt as React elements against an allowlist of MathML tags and attributes, so render.tsx's promise that nothing there produces an HTML string still holds", "S02: $...$ and $$...$$ are tokenised in render.tsx's existing atom pass beside [[wikilinks]] - not remark-math, which would have pulled a second KaTeX and could not have made a formula an Atom, which it has to be or paintRanges cuts a <mark> through the middle of one", "S03: an excerpt is markdown - a blockquote plus one annotation:// link (packages/document-model/src/excerpt.ts) - inserted from the page through a two-step picker, and accompanied by a real question-references-annotation edge so the desk, the graph and the ledger see it", "S03: RenderOptions.internalLinks turns document:// / annotation:// / note:// links into chips that navigate; before this safeHref rendered them as inert <a href=\"#\">", "the [S03] link assertion was mutation-checked: with the chip click removed the test fails on the source never opening", "N01 and N08 rewritten to drive the blocks rather than the retired notebook-body textarea; what they assert has not moved"], "criteria_advanced": ["S01", "S02", "S03"], "artifacts_created": ["apps/desktop/src/renderer/blocks.tsx", "apps/desktop/src/renderer/excerpt-picker.tsx", "packages/markdown-reader/src/math.tsx", "packages/document-model/src/excerpt.ts", "packages/document-model/src/excerpt.test.ts", "tests/integration/markdown-math.test.ts", "tests/e2e/notebook-page.spec.ts"], "errors": [], "next_action": "Milestone 6 continues: E01-E03, V01-V04, I01, R01, O01. E01 should reuse excerptMarkdown rather than growing a second answer.", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "733 pass (59 files)", "e2e": "84 pass (24 files)"}}
{"checkpoint_id": "cp-m6-reading-flows-in", "iteration_hint": "E01-E03 and I01: reading reaches a notebook, a claim can be given evidence by hand, the ledger sees every highlight, and a notebook can be deleted", "started_at": "2026-08-01T03:00:00-0700", "finished_at": "2026-08-01T03:35:00-0700", "phase": "milestone-6", "actions_taken": ["E03: link:findForDocument answers with two arrays - the edges it always returned, and the file's own highlights read from `annotations` with a link count; the old page grouped entries by their near end, so a marked sentence with no edge produced no group and 'Link this highlight...' existed exactly where linking had already happened", "E03: the count beside a highlight is computed under the same predicates the entries are (LIVE_EDGE, no derived edge with both ends inside the file) and is unbounded by the entries' limit, so a group can never say '3 links' over two rows; the order is listByDocument's, down the page, so the ledger and the annotation sidebar agree about one file's highlights", "E01: a fourth button in the reader's strip on the same subject rule as Link (the marked sentence if one is selected here, else the file), a new sendToNotebook command at Cmd+Alt+S in the declared 'make something from here' family, and a picker that asks only which notebook - the relationship is already known, so asking for it would be asking the researcher to name something they cannot get wrong", "E01: it writes through question:attach, so a card *is* the question-references-... edge and the desk, the graph, the ledger and the references panel all see it; notebook:changed gained reason 'attach' so an open page grows the card without being reopened; discarded notebooks are not offered as a destination", "E02: linkTypesFor gained the two hypothesis branches - the milestone said only the picker could not see a claim, but createDocumentLink re-validates with the same function, so a supports/opposes edge could not have been written even with the button hard-coded; related-to is deliberately absent from what may be said to a claim, because it appears on neither the For line nor the Against line", "E02: a hypothesis has no row in documents or notes, so everythingInLibrary structurally could not reach one - hypothesis:list is its own channel and the claims are their own section in the picker, each labelled with the notebook that claimed it, matched by the same filter box", "E02 mutation-checked: with the linkTypesFor branch disabled the test fails on the relationship list reading 'related to'", "I01: question:delete refuses a notebook that has not been discarded and the control appears only on the discarded shelf beside Restore - the distinct act made structural rather than cosmetic, since enforcing it in the panel alone leaves a channel any future surface can reach past", "I01: a hard delete - the row, and by cascade its journal, claims and tags, and by hand every edge with the notebook, one of its claims or one of its days at an end, which takes the desk because a card is such an edge and card_positions cascades from links; links is polymorphic and has no foreign key to any of them", "I01: the papers and highlights those edges pointed at are never touched, asserted twice; the confirmation says what goes and what stays, cancelling destroys nothing, and the status line reports the act in what was lost", "I01 mutation-checked: with the link cleanup removed the test fails on the desk's card and its position surviving the notebook", "QuestionsRepository's class comment said 'a question is never deleted by this repository' and now says what the two acts are"], "criteria_advanced": ["E01", "E02", "E03", "I01"], "artifacts_created": ["tests/e2e/reading-into-notebooks.spec.ts", "tests/e2e/notebook-lifecycle.spec.ts", "apps/desktop/src/renderer/overlays.tsx (NotebookPicker, the picker's Claims section)"], "errors": [], "next_action": "Milestone 6 continues: V01-V04, R01, O01, then the milestone-6 audit. Build O01's guide early enough that each later criterion adds its entry as it lands.", "git_commit": "a53feb9", "pushed": true, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "734 pass (59 files)", "e2e": "87 pass (26 files)", "verify_completion": "174/181; the seven failures are V01-V04, R01, O01 and the milestone-6 audit header"}}
{"checkpoint_id": "cp-m6-seen-and-found", "iteration_hint": "V01-V04: highlights on the wiki with their words, the graph searched in place, every calendar day, and a zoom lever on a saved page", "started_at": "2026-08-01T03:40:00-0700", "finished_at": "2026-08-01T04:25:00-0700", "phase": "milestone-6", "actions_taken": ["V03: the researcher asked for all the days, so the journal draws weekday-aligned month grids and nothing folds; calendarCells and its seven [J02] cases are untouched and calendarMonths is that same description asked with the fold off, so the fold stays a parameter rather than a second copy of the layout", "V03: [N10] and [P03] no longer click a run marker open before reading the range - a strengthening, since both now assert every day is present with nothing to expand first", "V04: HtmlReaderView keeps its 1280px layout width (a page asked to be a phone drops its navigation) and the lever multiplies the *fit* instead; past 1x the frame is larger than the panel and the viewport scrolls sideways, which is a trade the researcher makes rather than one they are handed", "V04: data-snapshot-scale publishes the effective scale, because that attribute is how the webpage suite computes a click inside the frame; the lever lives on ArticleReaderPanelSchema.zoom, per panel like the PDF reader's, and survives restart through the ordinary layout save", "V04: [W03] and [H01] now ask the reading surface rather than the whole reader whether text was rendered beside the page - the lever is chrome, not a rendering of the document", "V01: graph:overview admits a highlight once something links it; DRAWN_KINDS excludes the annotation-belongs-to-document edge every highlight is born with, so 'has a degree here' is 'the researcher connected this sentence to something' and a paper's degree is still what it was joined to rather than how much of it was marked up", "V01: that same exclusion keeps the wiki's redraw rule true - making a highlight still cannot change the answer, so the page still does not re-rank the library for every marked sentence; the four comments that argued the old 'no highlights' decision now state the new one", "V01: the node carries a snippet, is drawn with a smaller filled disc and its own words in quotation marks, and sits beside its paper - overviewPositions takes the containment the answer already carries (parent), so containment is never inferred from a link type", "V01: totalNodes is a COUNT(*) OVER () window over the same pass that ranks, so the whole-library query still reads links once; the [F01] plan and clock assertions still hold", "V01: the [F01] integration case that asserted highlights are absent was rewritten to the reversed decision, and gained the control it needed - a second marked sentence nobody links, which must not be drawn", "V02: SceneFilter, matchesNeedle and centredOn in graph-canvas, used by the wiki page and the neighbourhood panel; what does not match is dimmed rather than removed, and the view pans onto the matches keeping the researcher's zoom", "V02: useSceneView grew panTo instead of letting a surface compute its own transform, so rounding and clamping stay where every other viewport change goes", "graph edges take no pointer events now: a line crossing a disc was taking the click meant for it, which a small highlight node sitting an arm's length from its paper hits every time"], "criteria_advanced": ["V01", "V02", "V03", "V04"], "artifacts_created": [], "errors": [], "next_action": "Milestone 6: R01 and O01 remain, then the milestone-6 audit header. O01's guide must cover the graph filter and the saved-page zoom lever even though neither is a command - they are panel controls, like the graph's depth and spacing.", "git_commit": "6b6e0e1", "pushed": true, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "737 pass (59 files)", "e2e": "92 pass (26 files)", "verify_completion": "178/181 at 6b6e0e1; the three failures are R01, O01 and the milestone-6 audit header"}}
{"checkpoint_id": "cp-m6-the-right-hand", "iteration_hint": "R01: right-clicking a thing offers the commands that make sense on it, read out of the registry", "started_at": "2026-08-01T04:35:00-0700", "finished_at": "2026-08-01T05:05:00-0700", "phase": "milestone-6", "actions_taken": ["R01: packages/workbench/src/menus.ts is the whole feature - a table of command *ids* per surface, and buildContextMenu reads the title, the category and the chord back out of the two registries at click time; there is no action anywhere in it, so a menu cannot offer something the help page has not heard of", "R01: COMMAND_IDS moved to its own module (command-ids.ts, re-exported from workbench.ts) because the menu table has to name a command without importing the module that defines the commands - a cycle whose losing side is undefined at load time", "R01: three ways an entry is kept off a menu, all answered by the registry or the target rather than by the table - the `when` clause does not hold, the target cannot supply an argument the command needs (`requires`), or the thing is the wrong kind for it (`forTypes`); a menu omits where the palette greys, because a menu is a claim about this thing under this pointer", "R01: seven surfaces wired - a library row (ListRow), a tab (dockview's own DockviewDefaultTab, wrapped not reimplemented, so U03's markup is untouched), a graph node (SceneNode, so all three graph surfaces get it at once), a highlight (AnnotationCard and the article reader's strip), a notebook (the directory and the queue), a block, and the reader's own chrome", "R01: three new Writing commands - Edit This Block, Add a Text Block, Add a Code Block - because a block had no registered action at all and a menu may not invent one; they carry the block that was right-clicked, so 'add' means *after this one*, which is the thing the insert strip at the bottom cannot say. No delete: an emptied block disappears on commit, so removing prose stays where the prose is visible", "R01: WorkbenchHost.runBlockAction is the boundary; block-surfaces.ts is a React-free registry of mounted writing surfaces, because the host is called from a command handler rather than a render and importing blocks.tsx from host.ts would close a cycle through workspace.tsx", "R01: the menu offers neither discard nor delete on a notebook - both are the queue's, guarded and in that order, and a menu that could reach past a guard would be the guard's second, unguarded door", "R01: composed with H01 rather than colliding - a right-click inside the archive frame is an event in a sandboxed nested browsing context that never crosses into this document, so the frame keeps the gesture that carries its selection out and the reader's chrome around it gets the menu; asserted both ways in the e2e", "R01: the load-bearing assertion reads the menu, then reads the help page, and insists every item has a row there - a menu item nobody can find on the help page is by definition the second list of actions the criterion forbids"], "criteria_advanced": ["R01"], "artifacts_created": ["packages/workbench/src/menus.ts", "packages/workbench/src/command-ids.ts", "packages/workbench/test/menus.test.ts", "apps/desktop/src/renderer/context-menu.tsx", "apps/desktop/src/renderer/block-surfaces.ts", "tests/e2e/context-menu.spec.ts"], "errors": [], "next_action": "Milestone 6: O01 is the last criterion, then the milestone-6 audit header. The guide must cover the three Writing commands, the graph filter and the saved-page zoom lever (neither of the last two is a command).", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "748 pass (60 files)", "e2e": "96 pass (27 files)"}}
{"checkpoint_id": "cp-m6-the-guide", "iteration_hint": "O01: a guide page generated against the registries, with vendored motion, and three mechanisms that stop it rotting", "started_at": "2026-08-01T05:05:00-0700", "finished_at": "2026-08-01T05:45:00-0700", "phase": "milestone-6", "actions_taken": ["O01: a Guide page (wr.openGuide, Cmd+Shift+U, status-bar button beside Help) that answers 'what is this app and how do I use it' where the help page answers 'which key does this'; packages/workbench/src/guide.ts holds fourteen chapters in the order a researcher meets the app, and each chapter owns only prose and *ids* - every title and chord it displays is read out of the command and keybinding registries at draw time, the same discipline menus.ts already follows", "O01: coverage is computed by guideCoverage against the live CommandRegistry on mount, not against COMMAND_IDS - a command no chapter names is `missing`, a chapter naming an unregistered command is `unknown`, and both fail packages/workbench/test/guide.test.ts *and* are drawn on the page in a warning band, so a gap is visible to whoever is looking at the app rather than only in CI", "O01: the e2e cross-checks that verdict the only honest way - it reads every data-command-id off the help page, which *is* commands.all() rendered (D02), and insists each one has a chapter; importing COMMAND_IDS into the test would have been an assertion about a constant", "O01: the second tier is the features that are not commands, which the milestone warned enumerating commands.all() would miss - PANEL_CONTROLS declares 24 of them (the graph filter and its hops/spacing/labels/reset/rename/picture, the wiki cap, the saved-page zoom lever, mark-a-sentence, the Zotero scope, local files, the notes folder, new notebook, the desk, the excerpt insert, the claim, discard, delete, drop-a-picture, the calendar, and the librarian's consent/run/proposals), each panel carries data-control=\"<id>\", and tests/integration/guide-controls.test.ts reads the renderer's source and asserts the two sets are equal in *both* directions", "O01: that scan also forbids a computed data-control={...} - nothing can be read out of source about one, so the check would silently stop meaning anything the first time somebody used a variable there", "O01: the third tier is every ContextMenuKind, read off CONTEXT_MENUS through a new contextMenuKinds(), so R01's right hand is taught rather than discovered", "O01: motion is fourteen inline SVGs animated by keyframes in apps/desktop/src/renderer/guide.css - no animation library, no video, nothing fetched; every drawing's static attributes are its *resting* state, so prefers-reduced-motion: reduce switches all of it off and leaves a diagram rather than a pile of shapes at the origin, and the e2e asserts both directions with emulateMedia plus that no stylesheet or attribute on the page has an http origin", "O01: the stagger is delay *classes* rather than :nth-of-type - in SVG the animated siblings are rarely the only elements of their type in their parent, so an nth-of-type stagger lands on the wrong shape the first time the artwork gains a <rect>; the match ring scales instead of animating `r`, which is a CSS property only in Chromium", "O01: the guide is the fourth mutually-checking reading of one authority - help prints the registries, the menus resolve ids against them at click time, the guide covers them, and the e2e ties menu -> help and guide -> help together", "CLAUDE.md now carries the rule as an enforced one: a feature is not done until the guide shows it, with the two files that fail otherwise named"], "criteria_advanced": ["O01"], "artifacts_created": ["packages/workbench/src/guide.ts", "packages/workbench/test/guide.test.ts", "apps/desktop/src/renderer/guide-panel.tsx", "apps/desktop/src/renderer/guide.css", "tests/e2e/guide.spec.ts", "tests/integration/guide-controls.test.ts"], "errors": [], "next_action": "Milestone 6's criteria are all green. What is left is the milestone-6 audit header in reports/AUDIT.md, then verify_completion.py, then pnpm package and the /Applications swap.", "git_commit": "402a567", "pushed": true, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "759 pass (62 files)", "e2e": "100 pass (28 files)", "verify_completion": "180/181 at 402a567; the one failure is the milestone-6 audit header (a separate work item)"}}
{"checkpoint_id": "cp-m6-unification-sweep", "iteration_hint": "unify the tree onto what already exists, favouring what milestone 6 touched; remove what it superseded", "started_at": "2026-08-01T05:40:00-0700", "finished_at": "2026-08-01T06:25:00-0700", "phase": "milestone-6", "actions_taken": ["graph: sceneKey, SceneEdge and SceneGroupBox join SceneNode in graph-canvas.tsx - keyOf was written out three times and is compared across positions, containment, Cytoscape's model and the filter's match set, so one spelling is load-bearing; a surface's own facts (the neighbourhood's groups, the focused view's holds/reaches) ride as `data` exactly as they do on a node", "display: collapseWhitespace + ellipsize in @wr/document-model/display.ts replace seven copies of the same truncator (two in @wr/database, one in excerpt.ts, four in the renderer); one contract - limit is the width of the answer, ellipsis included - and every string produced is byte-for-byte what it was, the two 'ellipsis is extra' call sites now asking for the width they were already drawing. Deliberately not normalizeText, which is versioned and carries every persisted anchor offset", "commands: the annotation card's Note button now runs COMMAND_IDS.newNoteFromHere instead of its own annotation:get + note:create + navigate, which was the same four calls and the same 40-character title as the command the palette, the keybinding and the menu all reach; annotationTextIn (store.ts) is the one loop that reads a highlight's words out of the open readers", "readers: ReaderFrame in panels.tsx is the panel element, the actions strip and the right-click the three readers each wrote out - the menu is a claim about the file, never about the selected highlight, and that decision is now made once", "normalize.ts: foldCharacters is the five steps normalizeText and normalizeTextPreservingParagraphs share before they disagree about line structure - those five are what NORMALIZATION_VERSION names", "chrome: Chord (overlays.tsx) replaces eight hand-written <kbd class=\"wr-kbd wr-kbd--inline\">, six of them behind the same undefined guard; describeResolvedLink (@wr/workbench/entity-links.ts) is the ledger's and the references panel's identical sentence; NewNotebookControl is one control drawn on two shelves, which the guide already treated as one feature", "tests: support/keys.ts (three copies of 'press the chord the app resolved'), support/archive.ts (two copies of the DOM-Range-inside-the-sandboxed-frame gesture, comment included), corpusPageId and commitLink into support/corpus.ts, SilentHost into packages/workbench/test/support/, FAKE_CLAUDE and sampleMarkdownAnchor into the integration harness. No assertion changed", "dead: todayIso folded into localDay (it was a rename of it); .wr-journal__text (the journal's pre-blocks textarea) and .wr-notebook__front (the notebook's pre-S01 front matter) deleted from shell.css"], "criteria_advanced": [], "artifacts_created": ["packages/document-model/src/display.ts", "apps/desktop/src/renderer/notebook-controls.tsx", "tests/e2e/support/keys.ts", "tests/e2e/support/archive.ts", "packages/workbench/test/support/silent-host.ts"], "errors": [], "next_action": "Unchanged: the milestone-6 audit header in reports/AUDIT.md, then verify_completion.py, then pnpm package and the /Applications swap. Left as findings, not touched: workers/indexing is a placeholder nothing imports whose criterion (M09) is delivered by @wr/text-extraction-worker plus main/pipeline.ts, but CLAUDE.md's layout table names it; seven IPC channels have a handler or a publisher and no caller (zotero:probe, corpus:wantedPages, library:listCollections, library:listTags, agent:cancel, indexing:progress, zotero:importProgress); DocumentAdapter is never implemented but is CLAUDE.md's stated architecture. The calendar's fold (calendarCells, CalendarRun, COLLAPSE_RUN) looks superseded by V03 and is not - J02 still gates on it.", "git_commit": "4795a5e", "pushed": true, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "759 pass (62 files)", "e2e": "100 pass (28 files)", "verify_completion": "not re-run; no criterion touched"}}
{"checkpoint_id": "cp-m6-design-pass", "iteration_hint": "designer's pass over milestone 6: drive every surface, fix what is small, re-read the undecided gaps", "started_at": "2026-08-01T06:00:00-0700", "finished_at": "2026-08-01T07:00:00-0700", "phase": "milestone-6", "actions_taken": ["every surface driven and screenshotted from the running app under WR_BACKGROUND=1 with a throwaway tour spec (deleted before committing): empty workspace, guide top to bottom, help, command list, library, markdown reader, annotations sidebar, wiki, ledger, PDF, saved page alone and beside a second panel, notebook directory, a new notebook's page, its journal, the queue, discard, the discarded shelf, the delete confirmation, and two context menus", "gap 4 fixed: Back and Forward in the status bar were always enabled and pressing Back on a fresh workspace did nothing and said nothing; the gap said the history had to become observable first, and it already was one layer up - Workbench writes canGoBack/canGoForward into ContextKeyService on every navigation and that service publishes changes, so the buttons now read the same context key their chords are gated on through a useSyncExternalStore, a disabled one says on hover why there is nowhere to go, and both print their chord", "gap 6 fixed: the wiki drew three sizes of disc and a line and explained none of them; a legend row under the controls names the hub, a file, a quoted marked sentence and a typed link, each swatch drawn with the same class and radius the canvas draws it with so a restyle moves the legend with it, followed by what a click does - and Show now says what it counts", "the half of gap 8 the researcher's 'render all days' did not cover: the Begins field carried the stored value, which is null until someone sets it, so a journal that plainly begins today read 'mm/dd/yyyy' with an unlabelled 2026-08-01 underneath; the field carries the resolved day now and the line under it says only whether that day was chosen or worked out - journal.spec.ts P03 asserts the control's value rather than the orphan label, which is the same fact read off the thing the researcher looks at", "reports/DESIGN_GAPS.md rewritten: the eight gaps the researcher decided on are a table of what closed them, the four still open (1, 5, 7, 13) each say what this pass learned about their real cost, and four new proposals were added (16-19)", "gap 1 investigated and deliberately left open: fitting the viewBox to the drawn bounds moves 'the middle of the picture', which centredOn, toViewBox, viewBoxScale and awayFromCentre all hard-code as 500/350 with three specs asserting on it - written into the gap so the next pass starts from the cost rather than rediscovering it", "gap 5 re-read and its remedy replaced: hiding row actions on hover contradicts a decision already recorded in shared-ui/styles.css, so the proposal is now the title spanning line one with the action at the right end of line two"], "criteria_advanced": [], "artifacts_created": ["reports/DESIGN_GAPS.md (rewritten)"], "errors": [], "next_action": "Unchanged: the milestone-6 audit header in reports/AUDIT.md, then verify_completion.py, then pnpm package and the /Applications swap.", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "759 pass (62 files)", "e2e": "24 pass across journal/wiki/shell/keyboard; full suite re-run by the verifier"}}
{"checkpoint_id": "cp-m6-vision-alignment", "iteration_hint": "drive milestone 6 again against the researcher's sentences rather than its criteria; fix the small, write the large down", "started_at": "2026-08-01T06:40:00-0700", "finished_at": "2026-08-01T07:20:00-0700", "phase": "milestone-6", "actions_taken": ["every milestone-6 surface re-driven from the running app under WR_BACKGROUND=1 with throwaway tour and probe specs (deleted before committing), measured rather than eyeballed: the notebook page fresh and at paper length, the excerpt picker, the block menu, the guide top to bottom, the wiki after two sentences were marked, a saved page and its lever, a fresh notebook's journal, and search over what the researcher wrote", "the writing surface collapsed once the paper outgrew the panel: .wr-blocks is a flex item whose explicit min-height replaces the automatic minimum size, so a 1812px page was squeezed into a 414px box, its blocks overflowed it, and the insert strip carrying + text, + excerpt and Save page was laid out a screen and a half above the end of the writing and painted across two paragraphs (measured: strip -906..-876 overlapping notebook-block-5 and -6). flex-shrink: 0, and both writing surfaces grow with what is written. S01 passes at four template headings, which is why the criterion never saw it", "Sections in the notebook's margin listed the paper's headings and did nothing with them; each is now a control that goes there, addressed by ordinal rather than slug because every block renders through its own renderMarkdown and two blocks reading '## Method' are each slugged `method` while notebookSections calls the second `method-1`. Registered as the panel control notebook.outline, so the guide names it - the rule working", "gap 17 fixed: the guide's COVERS row drew commands, panel controls and context menus as one kind of chip; three runs now, each with its word in front (Press, On the panel, Right-click), a menu chip dotted where a control is dashed", "gap 18 fixed: the wiki header promised highlights the map usually will not have - when no marked sentence is drawn it reads 'N files and notes' and says the sentence that explains what would put one there", "reports/DESIGN_GAPS.md gained a Vision alignment section - 20: search indexes everything the researcher read and nothing they wrote (a phrase from a notebook page and one from a journal day both answer 'Nothing matched' in the running app, while the same query over a paper answers with two passages); 21: a note is a file the researcher owns and the paper is a TEXT column with no export, print or way out; 22: the desk and the excerpt edges are a bibliography the page never prints; 23: a journal that began today draws a month grid with one box in it"], "criteria_advanced": [], "artifacts_created": [], "errors": [], "next_action": "Unchanged: the milestone-6 audit header in reports/AUDIT.md, then verify_completion.py, then pnpm package and the /Applications swap.", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "759 pass (62 files)", "e2e": "100 pass (28 files)", "verify_completion": "run after the checkpoint is pushed"}}
{"checkpoint_id": "cp-m6-audit-close", "iteration_hint": "close the milestone-6 audit: fix every critical and major, each with a test that fails when the fix is reverted", "started_at": "2026-08-01T07:25:00-0700", "finished_at": "2026-08-01T08:40:00-0700", "phase": "milestone-6", "actions_taken": ["graph:overview's annotation branch (V01) was joining the materialised, unindexed degrees CTE to documents, so SQLite drove from documents and walked every annotation-degree row per file: reproduced against the compiled repository on seeded libraries, 123 ms at 400 papers, 1,957 ms at 1,500 and 8,988 ms at 3,000 (4 citations and 20 highlights per paper, one highlight in ten linked). Nine seconds of the process that owns the database, re-run on every library:changed that is not an annotation, and link:create publishes one per link. The branch drives from degrees now and expresses the paper's liveness as a set membership: 87 ms at 3,000, 65 ms at 1,500, and the probe asserts the two SQL shapes return identical rows", "the [F01] perf guard that exists for exactly this query seeded zero annotations, so the branch was in neither its plan nor its clock, and its three plan assertions all pass on the 9-second query. It now marks 8,000 sentences and links one in twenty, asserts totalNodes is files plus the linked marks and not the other 7,600, and requires every lookup of degrees to constrain entity_id rather than entity_type alone (a SCAN g fails it the same way). Reverting the fix fails it in 1.5 s", "S02 put $...$ into the shared corpus renderer and left projectText spelling a formula with its delimiters while the renderer's atom is the TeX without them. Measured: a highlight whose quote is the document's own normalized text painted 0 marks, and in real Chromium a drag over a sentence containing a formula produced no Highlight button at all - captureSelection's indexOf answers -1 and SelectionBar is gated on that. An M02/M03/H02 regression invisible to milestone-6 specs, which only render a formula on a notebook page. The alternation that decides what an inline construct counts as is now @wr/document-model's and is shared by projectText and renderMarkdown, so there is one answer; [[Page#section]] was the same defect and went with it. The reader reads a selection touching a formula back out of the DOM in the document's spelling, a formula being atomic there for the same reason it is atomic when painting; the unrendered fallback carries data-tex too", "four new integration tests derive the quote from the document's own projection instead of spelling it, and a new E2E over a corpus page carrying $R = e^{-t/S}$ drags the paragraph, presses Highlight, asserts the mark wraps the MathML without opening inside it, and reads the stored quote back off annotation:listByDocument. Both halves were reverted separately and watched to fail, in jsdom and in Electron", "questions.delete had no unit or integration cover and its single E2E path could not fail for two of four claims: the hypothesis subquery is emptied by the cascade before the after-state is counted, and card_positions was counted through a join on links. Four repository tests now capture every edge and position by id before the act and ask about those ids after it, cover the target_type='journal' branch the spec omitted, and assert the library edge and a second notebook's rows survive; three mutations were watched to fail, including deleting the row before the links so the subqueries resolve empty", "E02's evidence never reached an open notebook page: the page listens to notebook:changed, link:create published only library:changed, hypothesis:attachEvidence published nothing at all, and the reload kept only cards while hypotheses - where supporting/opposing live - was discarded. notebooksTouchedBy sits beside documentsTouchedBy and both link writes plus attachEvidence announce through it with a new 'link' reason; the reload keeps only the draft. The [E02] spec opens the notebook first, stamps the mounted panel, and asserts the For line fills on the same mount; four integration tests read what the channels published, including that two library rows tell no notebook anything", "the guide declares graph.find on 'Every graph surface' and the focused view had none - the surface a dense paper's neighbourhood is crawled on, twenty-four marked sentences round one paper. It has the filter now, matching the middle, the marked sentences and the files at the edge, dimming lines with nodes and panning through the module that owns every viewport; and guide-controls enumerates the files that draw the shared scene, so a surface without a filter fails. New E2E [V02] on the focused view", "E03's fix is three things on a panel and none of them was asserted anywhere. A new E2E marks two sentences of a corpus page, links one, opens the ledger and reads the 'Marked in this file' heading, data-highlight-count, both groups, the quoted label and 'Nothing said about this sentence yet.' off the page, then links the unlinked one from the ledger and watches the group fill in place. Two mutations watched to fail", "IntegrationWorkspace records what the services published, so a test can ask what a channel announced instead of inferring it from what a view did; local-files.test.ts keeps its own recorder and the override still wins", "the milestone-6 audit section is at the top of reports/AUDIT.md and carries the only Audited-commit:/Audited-milestone: tokens; the milestone-5 header was reworded to 'Audited commit (milestone 5):' so it cannot answer for the gate. Eighteen minors from both lenses are recorded there and in state/experiment_state.json"], "criteria_advanced": [], "artifacts_created": ["reports/AUDIT.md (milestone-6 section)", "reports/audit-m6-writing.md", "reports/audit-m6-surfaces.md"], "errors": [], "next_action": "python3 scripts/verify_completion.py, then pnpm package and the /Applications swap.", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "773 pass (62 files)", "e2e": "103 pass (28 files)", "verify_completion": "run after the checkpoint is pushed"}}
{"checkpoint_id": "cp-m6-ship", "iteration_hint": "ship milestone 6: package, swap the /Applications bundle, run the verifier, hand off", "started_at": "2026-08-01T08:20:00-0700", "finished_at": "2026-08-01T08:50:00-0700", "phase": "milestone-6", "actions_taken": ["WR_BACKGROUND=1 pnpm package: typecheck, build and electron-builder all clean (log in logs/package-20260801.log), bundle at apps/desktop/release/mac-arm64/wiki-reader.app built 2026-08-01T08:28:45", "the recorded swap: /Applications/wiki-reader.app moved to /Applications/.wiki-reader-superseded-20260801-082853.app - never deleted, because the researcher may be running out of it and holds its inodes until they restart - and the fresh bundle dittoed in (352M). Seven superseded bundles are now on disk", "python3 scripts/verify_completion.py at 17aae397 with a clean tree: 181/181 in 209.1s, exit 0, 'MILESTONE COMPLETE'. Every milestone-1 through milestone-6 criterion, the security and architecture invariants, the audit gate (Audited-commit 4b0fd0a reachable from HEAD, Audited-milestone 6) and the git gates all pass", "state/NEXT_ACTION.md rewritten for whoever comes next: milestone 6 is closed and shipped, there is no milestone 7 document and SPEC past 6 is still later, and the sanctioned work is an improvement pass over the open DESIGN_GAPS entries (1, 5, 7, 13, 16, 19, 20-23) - each a proposal until the researcher writes a Descision: line. The milestone-6 narrative was condensed to what is load-bearing for writing near it: the guide's three-way coverage, the menus table, INLINE_CONSTRUCT_RE as one rule, graph:overview driving from degrees, notebooksTouchedBy, and the panel controls the guide will not find by enumerating commands", "state/experiment_state.json updated: gates carry 181/181 at 17aae397, installed_build records the 2026-08-01T08:28:45 bundle and the name of the superseded one, last_verified_commit is 17aae397", "verifier re-run after this checkpoint was pushed, since it requires HEAD to be present on origin/main"], "criteria_advanced": [], "artifacts_created": ["/Applications/wiki-reader.app (2026-08-01T08:28:45)", "logs/package-20260801.log", "logs/verify-20260801-a.log"], "errors": [], "next_action": "No milestone 7 document exists. Improvement pass over the open reports/DESIGN_GAPS.md entries and the recorded minors in reports/AUDIT.md; do not build past milestone 6.", "git_commit": null, "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "773 pass (62 files)", "e2e": "103 pass (28 files)", "verify_completion": "181/181, exit 0 (209.1s)"}}
{"checkpoint_id": "cp-m6-security-lens-close", "iteration_hint": "close the milestone-6 security lens: fix its four findings, watch each mutation fail, fold the report into AUDIT.md and ship", "started_at": "2026-08-01T08:55:00-0700", "finished_at": "2026-08-01T09:35:00-0700", "phase": "milestone-6", "actions_taken": ["major S1 -- math.tsx's allowlist is the milestone's one new parser of untrusted input, sitting in the app's own origin, and had no test that could fail: swapping renderMath's body for dangerouslySetInnerHTML={{__html: html}} passed all fifteen tests in markdown-math.test.ts and every other test in the tree (the file has no other caller), and adding href/style/id to ALLOWED_ATTRIBUTES changed nothing any test observed because trust:false means KaTeX never emits one. Fixed by pinning, with the implementation left alone: fifteen new cases assert both allowlists by exact contents with href/id/style/class/onclick/src named absent, drive the rebuild directly with markup KaTeX cannot produce (a refused tag drops its subtree; a refused attribute drops from an allowed tag), assert \\href/\\htmlId/\\htmlClass/\\htmlData/\\htmlStyle/\\includegraphics inert with no a/href/id/style/class/src under the formula, assert mathcolor (from \\textcolor) and mathbackground (from \\rule) stripped -- the attribute filter observed through the public path -- and walk renderMarkdown's return value as a React tree so no element carries dangerouslySetInnerHTML and the MathML is present as built elements. ALLOWED_TAGS, ALLOWED_ATTRIBUTES and the rebuild (elementsFromMathML) are exported for this; nothing else in the file changed", "mutation watch: the dangerouslySetInnerHTML swap fails 8 of the new cases, the added href/style/id fails 2, and both were restored from a copy rather than by git checkout (work in progress is not staged)", "minor s1 -- renderMath never set KaTeX's maxSize (default Infinity) while the allowlist admits every MathML length attribute, so $\\rule{99999em}{99999em}$ reached the DOM as mspace width/height 99999em, ~1.6M CSS px inside a reader panel, reachable from any markdown file and from a highlight quoted onto a notebook page. maxSize = MAX_USER_SIZE_EM (10 em); the hostile rule comes back 10em/10em and \\hspace{2em}/\\rule{2em}{1em} are untouched. Removing the option fails the first test", "minor s2 -- excerptMarkdown escaped the title (library metadata) and left the quote (document-controlled, out of a PDF or off the open web) raw markdown, so a highlight spelling '- [Ebbinghaus 1885](annotation://ann_...)' rendered a second attribution chip above the real one on the feature whose criterion is 'keeps its link to the source'. The quote is now escaped as text, at the characters that begin a construct and only where they can begin one: backslash/backtick/*/[/]/</~/| everywhere, #/-/+/=/>/: and '1.' at a line lead, and _ only when it is not between two word characters so file_9 keeps its underscore. Five new tests including the forged chip driven through renderMarkdown to exactly one annotation:// chip. Reverting the escape fails four", "minor s3 -- INLINE_CONSTRUCT_RE's wikilink target admitted [, its own opening delimiter, and backtracked quadratically (8,000 chars 385 ms, 32,000 5.6 s, 64,000 22 s) in the process that owns the database, on every block of every markdown file the importer reads. [ excluded there and in WIKILINK_RE, which must exclude the same characters or the links one collects are not the chips the other draws. Verified not to change what matches: both expressions run over all 364 source, markdown and state files in the tree agreed on every match, index and group, and the only shape that reads differently is [[[Page]], which now means [ followed by a link to Page. Four time-bounded tests over parseMarkdown; putting [ back fails three, the first in 22 s", "two residues recorded rather than fixed, in excerpt.ts's docstring, AUDIT.md and this file: a $...$ in a quote still draws as a formula because render.tsx runs the shared inline pass over mdast text values after markdown has consumed the \\$ (fixing it means a second parser, or making the projection and the fold disagree about escapes, which is the defect the audit's finding 3 was), and a bare https://... is autolinked by GFM with no punctuation to escape -- the anchor prints its own destination and will-navigate refuses it", "reports/audit-m6-security.md committed; AUDIT.md's milestone-6 brief now says three lenses, names the third report, and states that the security lens crashed during the audit and was re-run against b824ec5..b54f510, wider than the other two's b824ec5..4b0fd0a, so it read their seven fixes as part of the milestone. Audited-commit is left at 4b0fd0a, the commit all three cover", "WR_BACKGROUND=1 pnpm package clean (logs/package-20260801-b.log); the recorded swap: /Applications/wiki-reader.app moved to /Applications/.wiki-reader-superseded-20260801-091724.app, never deleted, and the fresh bundle (2026-08-01T09:16:59, 353M) dittoed in. Eight superseded bundles are now on disk"], "criteria_advanced": [], "artifacts_created": ["reports/audit-m6-security.md", "/Applications/wiki-reader.app (2026-08-01T09:16:59)", "logs/package-20260801-b.log", "logs/verify-20260801-b.log"], "errors": [], "next_action": "No milestone 7 document exists and the milestone-6 audit is now closed across all three lenses. Improvement pass over the open reports/DESIGN_GAPS.md entries and the recorded minors in reports/AUDIT.md; do not build past milestone 6.", "git_commit": "521d25e9", "pushed": true, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "796 pass (62 files)", "e2e": "103 pass (28 files)", "verify_completion": "181/181, exit 0 (209.2s)"}}
{"checkpoint_id": "cp-m7-notebook-one-document", "iteration_hint": "milestone 7 P06/P10/P12: retire the desk into the page, fold the margin into it, and give the page Cmd+S", "started_at": "2026-08-01T14:10:00-0700", "finished_at": "2026-08-01T14:55:00-0700", "phase": "milestone-7", "actions_taken": ["P06 -- the desk retires. `desk-board.tsx`, `BoardCardSchema`, `NotebookPage.cards`, `boardCards`, `question:placeCard`, `BoardRepository`, `db.board`, `data-wr-drop-question` and the `notebook.desk` panel control are all gone; migration 014 drops `card_positions`. What a card *was* -- a `question-references-\u2026` edge -- is untouched, and is now shown as a block appended to `questions.body`: a paper as `[Title](document://\u2026)` (`documentReferenceMarkdown`, new beside `excerptMarkdown` in `@wr/document-model`), a highlight as the excerpt `S03` already defined", "the migration is a pass in `createServices` (`main/desk-retirement.ts`), not SQL: what a landing block reads as is a markdown decision that already exists once. Idempotent twice over -- a `notebook.deskRetired` setting, and `appendNotebookBlocks` skipping a block whose internal link the page already carries, which holds even with the setting cleared", "`appendNotebookBlocks` (`main/notebook-body.ts`) is now the one way main writes prose into a page: the send (`question:attach`, which gained `landsAsBlock` for the excerpt picker that writes its own block), the drop, and the migration. It appends to `blankNotebook()` when the body is empty -- found by [N06]'s E2E, which saw four template headings replaced by one line", "the notebook's two drop targets became one. `receiveDrop`'s `questionId` branch is gone and the page's target takes everything: a picture becomes a figure, a paper becomes a reference plus its edge. `notebook:changed`'s reason enum is now ['drop','attach','link','deleted']", "P10 -- `<aside data-testid=\"notebook-side\">` is gone. Front matter, Sections and Hypotheses are sections of one scrolling `notebook-page`, in the order a paper is read, with a `notebook.jump` strip under the title. `.wr-notebook` is a flex column; the writing section is the only one that grows", "P12 -- `COMMAND_IDS.saveWriting` on `Cmd/Ctrl+S`, family 'Work the panes', deliberately unguarded by `!textInputFocus`. `BlockEditorHandle.save` / `BlockSurface.save` / `BlockActionRequest.action: 'save'`; save re-parses rows only when the store normalized something, so the caret stays. `main/menu.test.ts` carries the [P12] half a CDP keypress cannot see", "supersessions: `board.spec.ts` is `notebook-landing.spec.ts` (N06, N07, P06); E01 asserts blocks in the page; S01's layout claim is re-expressed against the scroller and the front matter; S03 asserts the edge in SQLite rather than on a second surface; the N06 integration suite and the I01 seeds lost `card_positions`; `removed.cards` is `removed.references`", "guide: `notebook.desk` -> `notebook.jump`, the `desk` motion is `send` and lands in a page, the send chapter says block rather than card, and the notebook chapter names `saveWriting`"], "criteria_advanced": ["P06", "P10", "P12", "N06", "N07", "E01"], "artifacts_created": ["apps/desktop/src/main/desk-retirement.ts", "apps/desktop/src/main/notebook-body.ts", "packages/database/src/migrations/014_desk_retired.ts", "tests/e2e/notebook-landing.spec.ts", "tests/e2e/notebook-one-page.spec.ts"], "errors": [], "next_action": "The rest of milestone 7: P07/P08/P09/P11, H05-H09, F04-F07, U09-U11, D03/B06/B07.", "git_commit": "544986c", "pushed": true, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "802 pass (62 files)", "e2e": "106 pass (29 files)", "verify_completion": "not run in this slice"}}
{"checkpoint_id": "cp-m7-blocks-and-journal-popup", "iteration_hint": "milestone 7 P07/P08/P09/P11: the block handles, the ready first block, the journal pop-up that expands, the figure that resizes", "started_at": "2026-08-01T15:00:00-0700", "finished_at": "2026-08-01T16:20:00-0700", "phase": "milestone-7", "actions_taken": ["P07 -- every block now carries a grip and a delete, drawn *outside* the block's own box because `offsetFromClick` reads `element.textContent` to place the caret (`P05`) and a glyph inside would shift every offset past it. The drag copies the queue's pointer discipline (capture on the handle, listeners on the window, `pointercancel` unbinds) and is deliberately not HTML5 drag-and-drop: the preload's file-drop listener watches `drop` on these very elements. `moveBlock` in `block-source.ts` is the pure half. `COMMAND_IDS.deleteBlock` supersedes the milestone-6 comment that decided against one, and is the `block` menu's own last group", "the write ticket. Deleting a block that is being typed in blurs first, so two writes are in flight at once; `BlockEditor` now numbers them and takes rows only from the newest answer, or the older one landing last brings the deleted block back", "P08 -- `BlockEditor.openWhenEmpty`, asked for by the journal and not by the notebook page (a day is arrived at in order to be written in; a page arrives with a template and a shape). The seed fires on an arrival -- a `surfaceId` never seeded, or a document emptied to no blocks -- never merely because a block was left blank, which would be a focus trap. A blank block left behind carries the invitation the empty state used to, so `BlockBody` gained a `placeholder`", "P09 -- `openJournal` calls the new `WorkbenchHost.promptJournal` and the journal comes up on the shared `Overlay` (`JournalPopup` in journal-panel.tsx, `store.journalPopup`, not part of the saved layout); `COMMAND_IDS.expandJournal` resolves the same plan the tab always used. One command per gesture and no second 'open as a pop-up' for a menu to choose between -- placement is the host's decision the way `applyPlan` decides split from reveal. `N09` re-anchors on both homes", "P11 -- a figure's width is one word (`w=320`) in markdown's own title slot; `IMAGE_ONLY` was widened to tolerate the slot, because `classify` runs on every keystroke and a resized image that stopped looking like a lone image would become a `text` block. `parseImage`/`withImageWidth` are pure and tested; `BlockBody` draws the figure itself (as it already drew code) rather than through the corpus renderer, which would print the slot as a tooltip. The bytes are untouched -- a resize is a fact about how the figure is drawn", "guide: `block.rearrange` and `block.resize` are new panel controls on 'Any writing surface', the notebook chapter names `deleteBlock` and both controls, and the journal chapter names `expandJournal` and says the day opens with a block in it", "ripples, all re-expressed rather than weakened: `N10` reads 'the day starts empty' as a blank open editor instead of the empty message; `R01`'s block menu gains `wr.deleteBlock` last; three notebook/context-menu specs dismiss the journal sheet before reaching the workspace behind it, and `P01`'s 're-reads the shelf' expands the journal into a page to get away from the directory and back", "`P09` carries its own tagged test as well as the re-anchored `N09`: the verifier indexes by tag, and a supersession that only re-titles an old test leaves the new criterion with nothing to point at. The two say different things -- `N09` is the layout claim (a sheet, then the full width of the page), `P09` is the continuity one (the journal is asked for from inside the reading, written in there, and carries what was written into the tab it expands to)"], "criteria_advanced": ["P07", "P08", "P09", "P11", "N09"], "artifacts_created": ["tests/e2e/blocks.spec.ts"], "errors": [], "next_action": "The rest of milestone 7: H05-H09, F04-F07, U09-U11, D03/B06/B07.", "git_commit": "d14309a", "pushed": true, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "811 pass (62 files)", "e2e": "111 pass (30 files)", "verify_completion": "188/204; P07 2, P08 1, P09 1, P11 1, N09 1 all pass"}}
{"checkpoint_id": "cp-m7-a-link-is-a-link", "iteration_hint": "milestone 7 H05-H09: the kind chooser goes, the picker opens a file out into its sentences, a link is deletable wherever it is drawn, and two drags make one", "started_at": "2026-08-01T16:25:00-0700", "finished_at": "2026-08-01T17:05:00-0700", "phase": "milestone-7", "actions_taken": ["H05 -- the relationship section is gone from the picker and the guard is gone from the command. `createDocumentLink` now defaults through `defaultLinkType(source, target)` (new, beside `PLAIN_LINK = 'related-to'` in `entity-links.ts`), which answers null for the one pair with no plain edge -- a claim -- so the picker still asks *which way the evidence cuts* there and nowhere else (`E02` would otherwise draw empty For/Against lines). `linkTypesFor` is untouched and still bounds an explicit type arriving from a caller that does know; the links table keeps its typed directed edges and everything that reads one goes on reading it", "`commitLink(window, linkType)` in tests/e2e/support/corpus.ts is now `commitLink(window, stance?)` -- one press, with the argument surviving only for a claim. Every graph/wiki/ledger spec got a click shorter, and the three ledger/wiki assertions that named `annotation-references-document` now name `related-to`, which is what the app writes", "supersession: `[K01]`'s test in tests/e2e/links.spec.ts is re-titled `[K01] [H05]` and its two load-bearing assertions are inverted -- Create is armed by the target alone, and `link-picker-types` is not on screen at all -- while the edge is still read back out of SQLite as `{type: 'related-to', origin: 'manual'}`, which is the half of K01 that survives. `workbench.test.ts`'s `[L09]` refusal case became two `[H05]` cases (plain link written; claim still refused); L09 itself is covered fifty times over in keybindings.test.ts", "H06 -- choosing a file in the picker's list opens it out into the sentences marked in it, on a stage that replaces the list rather than nesting under a row. `link:findForDocument` is the query, not `annotation:listByDocument`: one round trip for both halves, and each sentence arrives with how much has already been said about it, which is the signal the ledger groups by. `marks: null` is 'not answered yet' and `[]` is 'nothing is marked in it', said in words that keep the file as the target rather than presenting an empty list as an error. The file itself stays reachable from inside its own stage, and `← another file` goes back", "H07 -- one control (`UnlinkButton` in the new link-actions.tsx) and one command (`COMMAND_IDS.deleteLink` -> `WorkbenchHost.deleteEntityLink` -> `link:delete`), drawn on the ledger, on the references panel, and as a × on a chosen line on the wiki and on a file's neighbourhood. `link:delete` already announced through `announceLink`, so every open panel redraws with no new subscription and no panel knowing about another", "the canvas reversal, deliberately: `.wr-graph__edge` keeps `pointer-events: none` and an invisible `.wr-graph__edge-hit` band beside it takes the press (`pointer-events: stroke`, drawn beneath every node), because widening the visible stroke to make it hittable would change the picture in order to make it clickable, and the old comment's objection -- a line crossing a disc stealing its click -- is answered by paint order rather than by a rule. Two presses to delete: one to say which line, one to mean it. The focused view is not given a delete because its lines are aggregates (`GraphFocusNeighbour.connections`), not rows in `links`", "H08 -- a marked sentence dragged onto the reader beside it. Implemented once in `ReaderFrame`, which is both ends of the gesture: any element carrying `data-annotation-id` inside a reader is the handle (the attribute the PDF and markdown readers already paint), the panel under the pointer is the target, and a saved page's frame can receive without being able to start. Pointer events with a 6px threshold so a press that stays put is still the click that selects the mark; the in-flight drag lives in the store (not the saved layout) because the *other* reader has to know a sentence is in flight to say `data-taking-link`. A drop back on the file the sentence was marked in writes nothing. Nothing goes near `wr:drop`", "H09 -- press one disc, drag to another, let go. The gesture starts in `useSceneGestures`' existing early return on `.wr-graph__node`, which is what keeps a node's click working, and window listeners rather than pointer capture for the same reason: capturing on the `<svg>` would take the click with it. `SceneLinkDrag`/`SceneLinkLine` keep the arithmetic in graph-canvas.tsx, and the rubber-band line is drawn outside the viewport group because both its ends are already in the SVG's units. Both canvases run `createDocumentLink`, so a drag makes exactly the edge the picker makes", "guide: the `link` chapter is re-lede'd ('a link is just a link'), names `deleteLink`, and gains two panel controls -- `link.dragHighlight` on any reader and `link.dragNodes` on the graph surfaces -- each carried as a literal `data-control` on the frame and the two canvases", "tests/e2e/linking.spec.ts is new and carries H06, H07, H08 and H09: real pointer gestures at real coordinates, every claim about what was written read straight out of SQLite. `centreOfText` aims at the first client rect of a mark rather than the middle of its union, because a highlight over a wrapping paragraph has gaps where the average is"], "criteria_advanced": ["H05", "H06", "H07", "H08", "H09", "K01"], "artifacts_created": ["apps/desktop/src/renderer/link-actions.tsx", "tests/e2e/linking.spec.ts"], "errors": [], "next_action": "The rest of milestone 7: F04-F07, U09-U11, D03/B06/B07.", "git_commit": "1410e09", "pushed": true, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "812 pass (62 files)", "e2e": "115 pass (31 files)", "verify_completion": "193/204 at 1410e09; H05-H09 and K01 pass"}}
{"checkpoint_id": "cp-m7-one-graph", "iteration_hint": "milestone 7 F04-F07: the wiki fills the page and keeps its scale when docked, the focused view becomes a state of it, a highlight's label wraps, and the librarian leaves the sidebar", "started_at": "2026-08-01T17:10:00-0700", "finished_at": "2026-08-01T17:50:00-0700", "phase": "milestone-7", "actions_taken": ["F04 -- every graph canvas's `viewBox` is now the panel's own size in CSS pixels, and the scene's fit inside it is *held* rather than recomputed. `preserveAspectRatio=\"xMidYMid meet\"` over a fixed 1000x700 box was scale-to-fit, so dragging the wiki's tab to the side halved the panel and halved the map with it. `SceneFit`/`SceneCanvas`/`fitInto` in graph-canvas.tsx, captured on the first measurement with a real size (a hidden Dockview tab measures zero and is skipped) and released only by Reset view. Docking is then a smaller window onto the same drawing: same disc size, same distance between two discs, fewer of them inside the panel", "the two transforms are deliberately separate elements. `SceneViewportGroup` draws `.wr-graph__fit` outside the pan-and-zoom group it already published: multiplying them would make 'the panel got narrower' and 'you zoomed out' one number, and nothing could then tell a docked map from a small one. `centredOn` is unchanged and still centres on the scene's own middle, which is why every `V02` assertion about `data-pan-x + node.x` survived untouched", "`toViewBox`/`viewBoxScale` are one `toScene(fit, ...)`; pointer arithmetic reads the held fit instead of re-deriving a letterbox from the element. `useSceneGestures` returns `{svgProps, canvas, refit}` now, and `sceneCanvasProps(canvas)` writes the viewBox plus `data-view-width`/`data-view-height`/`data-fit` so a test reads the surface's own numbers rather than measuring pixels and hoping", "F05 -- the `focus` panel kind is gone. `WikiPanelSchema` carries `focusDocumentId` (null = the whole library), `wiki` joins `RESEATED_PANEL_KINDS`, and `WikiPanel` draws `WikiPanelBody` or `FocusPanelBody` from its descriptor. `openFocusView` writes a wiki descriptor; `openWiki` writes one with no file, which is how the whole library comes back through the same tab. `FocusPanelSchema`, `focusDescriptorFrom`, `FocusPanel` and the `focus` entry in `DOCKVIEW_COMPONENTS` are deleted; `titleFor` says `Wiki` or `Wiki - <title>`. The way back is a `wiki-whole` button on the focused state, running `openWiki` through the registry rather than writing the panel's own descriptor", "F06 -- a highlight's label wraps. `quoteLines` (greedy, 24 characters, at most 3 lines, ellipsis when the sentence goes on) and `<tspan>` lines under the disc; a title still gets one ellipsized line, because a name does not need reading. Supersession: `[V01]`'s test is re-titled `[V01] [F06]` and its label assertion is what moved -- more than one line, more than forty characters drawn, every line inside the column, and what is drawn is the start of the sentence in `data-snippet` rather than a shorter summary of it", "F07 -- the librarian has no sidebar. `librarian` is out of `SidebarStateSchema` and `LEFT_SIDEBARS` (a persisted workspace carrying it still deserializes: zod ignores the key), the `activity-librarian` button is gone, and `COMMAND_IDS.toggleLibrarianSidebar` is `COMMAND_IDS.openLibrarian` -> `WorkbenchHost.promptLibrarian` -> `store.librarianOpen`, drawn as `LibrarianPopup` on the shared `Overlay`. The way in is a `wiki-librarian` button on the wiki, because that is the picture the librarian proposes edges for. Following a citation closes the sheet, the way the journal's Expand does", "guide: the map chapter is re-lede'd around one surface with two states and says docking keeps the scale; the librarian chapter names `openLibrarian`; the three `librarian.*` panel controls say 'The librarian, opened from the wiki' instead of 'Librarian sidebar'. No new panel control -- both new buttons run commands", "ripples: `panel-targets.test.ts`'s focus block is now 'the wiki, whole and focused' and gained an `[F05]` case for the whole library re-seating the focused tab; `layout.test.ts`'s U04 fixtures lose the fourth sidebar and its restore case now carries `librarian: true` as the stale key it has to survive; `librarian.spec.ts` and `sidebars.spec.ts` open the librarian from the wiki"], "criteria_advanced": ["F04", "F05", "F06", "F07", "V01"], "artifacts_created": [], "errors": [], "next_action": "The rest of milestone 7: U09-U11, D03/B06/B07, then the audit and the swap.", "git_commit": "0958ac2", "pushed": true, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "813 pass (62 files)", "e2e": "118 pass (31 files, logs/e2e-full-f.log)", "verify_completion": "pending the checkpoint push"}}
{"checkpoint_id": "cp-m7-shell-obeys-the-hand", "iteration_hint": "milestone 7 U09/U10/U11: the chrome drags and folds, a search hit goes where it points, and delete goes to a bin that empties", "started_at": "2026-08-01T17:55:00-0700", "finished_at": "2026-08-01T18:40:00-0700", "phase": "milestone-7", "actions_taken": ["U10 -- `SearchPanel` maps a hit to its own entity by kind (`searchTarget` in panels.tsx): a chunk/document opens the file at the passage, an annotation opens the file with the sentence selected, a note opens the note. The dead `if (result.documentId === null) return;` is gone; a hit with nothing left to open says so in the status bar. Rows also carry a title saying where the click goes, and Cmd-click opens to the side.", "U09 -- `ChromeState` in `@wr/workbench/layout.ts` (sizes + minimized for `left`/`annotations`/`bottom`, `CHROME_BOUNDS`, `clampChromeSize`, `resizeChrome`, `toggleChromeMinimized`, `chromeExtent`) persisted as a defaulted `chrome` key on `SerializedWorkspaceSchema` -- the layout version is NOT bumped, so every saved workspace still restores. `ChromeResizer` (role=separator, pointer drag + arrow keys) and `MinimizeControl` in App.tsx; the annotations column gained its own close, which runs `COMMAND_IDS.toggleAnnotationSidebar` so the activity button cannot disagree with it. The notebook page's four sections fold by their headings (`SectionHead`, `data-control=\"notebook.fold\"`), and the jump strip unfolds whatever it goes to.", "U11 -- migration 015 adds `questions.trashed_at`. It is NOT a fourth status: a binned notebook is still `discarded` and still carries its reason, so `question:delete`'s precondition is untouched -- what changed is that `question:delete` now *bins* (returns the question), `question:restoreFromTrash` takes it back out, and `question:emptyTrash` is the only channel in the app that destroys a line of work. The queue draws three shelves; the confirmation moved from Delete (now reversible, one press) to Empty the bin (now the irreversible one). The directory hides binned notebooks.", "I01 supersession -- the E2E is re-titled 'deleting goes to the trash bin' and re-anchored: delete puts it in the bin, Put back returns it whole, and only emptying the bin destroys it (with the what-goes/what-stays confirmation and the counts message). The four integration tests move with it: one new one proves the round trip, the other three empty the bin before asserting what went.", "Guide -- three new panel controls (`shell.resize`, `shell.minimize`, `notebook.fold`) plus `notebook.emptyBin`; the `read` chapter covers the chrome, the `notebook` chapter the fold, the `aside` chapter the bin, and the `search` chapter now says a row goes to its own kind of place."], "criteria_advanced": ["U09", "U10", "U11", "I01"], "artifacts_created": ["tests/e2e/search.spec.ts", "tests/e2e/shell-chrome.spec.ts", "packages/database/src/migrations/015_trash_bin.ts"], "errors": [], "next_action": "The rest of milestone 7: D03/B06/B07, then the audit and the swap.", "git_commit": "e8a16a1", "pushed": true, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "817 pass (62 files)", "e2e": "121 pass (33 files, logs/e2e-full-u.log)", "verify_completion": "200/204 at 86c1fa0; U09, U10, U11 and the I01 supersession all pass. The 4 that fail are D03/B06/B07, which nobody has built yet, and the audit-milestone gate."}}
{"checkpoint_id": "cp-m7-shown-not-told", "iteration_hint": "milestone 7 D03/B06/B07: every command draws itself, the icon picker becomes a gallery of the set's art, and a demo library fills every surface in development", "started_at": "2026-08-01T18:45:00-0700", "finished_at": "2026-08-01T19:35:00-0700", "phase": "milestone-7", "actions_taken": ["D03 -- the guide's fourteen drawings moved out of guide-panel.tsx into apps/desktop/src/renderer/motions.tsx so two pages can draw them, and the help page now puts one beside every command. Which drawing is *computed*, never listed: `commandMotion({id, category})` in guide.ts reads the two things every registered command declares -- a small `MOTION_BY_COMMAND` table for the verbs a category cannot tell apart (delete vs create, close vs open, gather vs link, save) over a total `MOTION_BY_CATEGORY` fallback. A hand-written table of fifty-two would be a second registry and the first command added without a row in it would be a blank.", "D03 -- `COMMAND_MOTIONS` = the guide's set plus four a chapter never needs (`close`, `unlink`, `gather`, `save`), drawn in the same ink with keyframes in guide.css, so `prefers-reduced-motion`'s single `.wr-guide__motion * { animation: none }` covers both pages. `commandMotionCoverage` is reported on the page as `data-motion-complete`, the way the guide reports its own gaps. The help page's feature list became a card grid; `help-command-<id>`, `.wr-help__command-title` and `data-chord` are unchanged, so `[D02]` and `[O01]` are untouched.", "B06 -- `CardArtLibrary.gallery({offset, limit})`: the set listing (`setListingUrl`, `q=set:mh3 unique:art`, `order=name`) fetched once per installation through a *sibling* path with its own `application/json` gate and its own cache file, then the existing `artUrl` crop path per name. `#fetchAndKeep`'s IMAGE_TYPES gate is NOT widened -- that gate is what keeps a web page out of the directory `rrfile://` serves from. A crop that cannot be had is `iconFileId: null`, so one missing picture is a gap rather than a panel that will not open.", "B06 -- the renderer's card-name text field is gone; `card-art-gallery` (`data-control=\"graph.gallery\"`) is a horizontal scroller of `rrfile://` tiles with a More button, and pressing one runs the same `cardArt:fetch` by name (a cache hit, so nothing leaves the machine). The disclosure gained a line naming the set listing and one saying the whole card is never asked for. The local-image `<select>` (`graph.picture`) is untouched, so `G04` is unaffected.", "B06 -- the E2E seeds the card-art cache before the app starts (`tests/e2e/support/card-art.ts`), keyed with the application's own `setListingUrl`/`artUrl` through SHA-256, so the running app finds everything on its own disk and the suite never touches the network. A seed that spelled its own URLs would silently stop matching and the gallery would go quietly empty instead of failing.", "B07 -- `apps/desktop/src/main/demo.ts`. Six synthetic markdown papers written to a `demo` root beside the database and ingested by the *real* `MarkdownCorpusImporter`, which gained one option: `source`. Everything the demo makes is a document tagged `DEMO_SOURCE`, a notebook on the demo's own remembered list, or an edge with one of those at an end -- so clearing is one predicate rather than a bookkeeping table. Highlights are real `createMarkdownAnchor` anchors so they paint; the wikilinks between the papers are parsed rather than written, so the wiki has edges the demo did not author.", "B07 -- `demo:status`/`demo:fill`/`demo:clear` take nothing that could shape what is written, and refuse with CONFLICT unless `createServices({development})` said so (`!app.isPackaged`, decided once in main/index.ts). Two commands, `wr.fillDemoLibrary` and `wr.clearDemoLibrary`, in a new `Demo` category with its own guide chapter and its own motion. Filling twice is a no-op by construction. The library sidebar lists `demo` documents beside the notes, which is a fourth `library:listDocuments` call in workspace.tsx.", "database: `NotesRepository.purge` beside `DocumentsRepository.purge`, because a soft-deleted note comes back the moment anything lists deleted notes and the demo's note was never the researcher's to keep."], "criteria_advanced": ["D03", "B06", "B07"], "artifacts_created": ["apps/desktop/src/renderer/motions.tsx", "apps/desktop/src/main/demo.ts", "tests/e2e/help.spec.ts", "tests/e2e/card-art.spec.ts", "tests/e2e/demo.spec.ts", "tests/e2e/support/card-art.ts", "tests/integration/demo.test.ts"], "errors": [], "next_action": "Milestone 7's criteria are all built. Next: the audit in reports/AUDIT.md, then pnpm package and the bundle swap.", "git_commit": "pending", "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "833 pass (63 files)", "e2e": "128 pass (36 files)", "verify_completion": "pending the checkpoint push"}}
{"checkpoint_id": "cp-m7-unification-sweep", "iteration_hint": "unify the codebase after milestone 7: fold what the retired surfaces left behind, and put hand-rolled copies onto the abstraction that already existed", "started_at": "2026-08-01T19:45:00-0700", "finished_at": "2026-08-01T20:40:00-0700", "phase": "milestone-7", "actions_taken": ["graph surfaces -- `usePanToMatches` in graph-canvas.tsx is the filter-moves-the-view effect all three surfaces had written out (the joined-key change detector, the positions ref, two `eslint-disable react-hooks/exhaustive-deps` comments). The neighbourhood panel keeps the only part that was its own: a `panTo` that routes through `moveView` so the pan is saved against its seed (`G01`). `dataAttrs` replaces three copies of the `data-*` prefixing spread; `ScenePoint`/`ScenePositions` name the shape that was spelled out inline; the two hand-rolled `[...].filter(...).join(' ')` class joins go through `classNames` from @wr/shared-ui.", "panels -- `usePanelDescriptor(panelId, kind)` in workspace.tsx replaces nine copies of `descriptor === null || descriptor.kind !== 'X'`, three of them wrapped in a bespoke `graphDescriptorFrom`/`ledgerDescriptorFrom`/`wikiFocusFrom`. `PanelParams`/`DockPanelProps` move there too -- they were declared five times. `useReportFailure` replaces five identical `report` callbacks plus twelve inline `store.setStatus(describeError(failure).message, 'error')` in the graph, wiki, focused view and directory.", "cross-process duplicates -- `localDay` was in the renderer's calendar and in `JournalRepository.start`, each with a comment naming the other; it is `@wr/document-model/calendar.ts`. `ellipsize(collapseWhitespace(t), n)` was written out in `EntityResolver`, the graph repository and the excerpt picker; it is `shorten` in display.ts. `isWorking` was a private one-liner in the queue and another in the directory; it is beside `isInTrash` in @wr/shared-types.", "layout -- `SerializedWorkspaceSchema.sidebars` and `serializeWorkspace` each carried a copy of the four sidebar defaults the schema already declares, which is what `defaultSidebars()` exists to prevent; both go through `SidebarStateSchema` now.", "retirement residue -- `.wr-graph__node--link-target` (nothing has ever set it; the drag reports its target as `data-over` on the line) and `.wr-notebook__prose-head` (the notebook's margin became sections of the page) are gone. The two pop-ups that replaced sidebars shared eleven box properties written twice and differ in one. `GuidePanel`/`HelpPanel` were wrappers forwarding a `panelId` their stateless pages cannot use. `LibrarianView`'s `testId` was optional because a sidebar also drew it. Seven exports had no second caller.", "prose -- the comments that described a desk board as the present tense (what a menu can send, what Cmd+Alt+S makes, what the drop channel is for) say what is true now. `sendToNotebook` deliberately keeps `desk` among its search keywords: that is user-facing vocabulary, and changing it would be a behaviour change."], "criteria_advanced": [], "artifacts_created": ["packages/document-model/src/calendar.ts"], "errors": [], "next_action": "Unchanged: the audit in reports/AUDIT.md, then pnpm package and the bundle swap.", "git_commit": "91d869b", "pushed": true, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "833 pass (63 files) -- unchanged, no test edited", "e2e": "128 pass (36 files) -- unchanged, no spec edited (logs/e2e-1.log, logs/e2e-2.log, logs/e2e-3.log)", "verify_completion": "not re-run; no criterion touched"}}
{"checkpoint_id": "cp-m7-design-pass", "iteration_hint": "a designer drives the milestone-7 surfaces with the demo library seeded and fixes what is small and obviously right", "started_at": "2026-08-01T18:45:00-0700", "finished_at": "2026-08-01T19:30:00-0700", "phase": "milestone-7", "actions_taken": ["drove the app with a throwaway tour spec (WR_BACKGROUND=1, demo library filled, card-art cache seeded) through the one-document notebook, the journal pop-up and its page, the merged wiki, the librarian, the trash bin, the help page's command cards, the guide, the MH3 gallery, search and the link picker; the spec was deleted before committing", "the search page printed the FTS5 delimiters. `SearchPanel` handed `result.snippet` straight to the row, so every hit drew a private-use code point either side of the match -- no glyph, so a pair of tofu boxes. `SNIPPET_OPEN`/`SNIPPET_CLOSE`, a new `snippetSegments` and `stripSnippetMarkers` now live in `@wr/shared-types/snippet.ts` beside the schema of the field they describe (the renderer may not import main-only `@wr/search`), `search-service.ts` imports and re-exports them, and the row draws a `<mark>` per matched run styled to lift rather than fill", "a disabled button looked live everywhere but the link picker's footer -- eighteen controls, each answering the hover and then doing nothing. That footer rule is promoted to `.wr-button:disabled` in `@wr/shared-ui`, the icon and quiet variants get theirs, and the three `:hover` rules became `:not(:disabled)`", "`.wr-notebook__claim-status` is `width: auto`: `wr-input` is 100% wide because a field takes its line, and a four-word status select taking the page made the claim above it read as a caption", "an empty side of a claim said `For` / `Against` and nothing, which reads as a list that failed to load; `Citations` now says `Nothing yet` on a side nobody has argued", "`.wr-help__key > .wr-help__chords` is `justify-content: flex-end`: the column is wide enough for the widest chord, so left-aligned it sat `Cmd+S` a hundred pixels from what it does"], "findings": ["not a defect: inside `WR_BACKGROUND=1` a Playwright screenshot of a scrolling overflow container (the link picker's target list) rasterises as a smudge. Probed through the DOM -- 13px, `--wr-text`, opacity 1, 19.8px rows -- the pixels are the capture, not the app. Do not chase it.", "left for the researcher: the wiki's labels collide in the dense middle of the demo library; the journal pop-up is a fixed 720px whether the day is two lines or twenty; the keyboard section lists a command once per binding, so two chords for Search Library read as a duplicate row. All three are judgements, not defects.", "deliberately not done: a reading measure on the notebook page and the journal. The block editor runs prose to the full panel while the markdown reader holds 38rem beside it -- but `S01` is the recorded decision that the page takes the room, so this is the researcher's to rule on, not a pass's to quietly reverse."], "criteria_advanced": [], "artifacts_created": ["packages/shared-types/src/snippet.ts", "packages/shared-types/src/snippet.test.ts"], "errors": [], "next_action": "Unchanged: the audit in reports/AUDIT.md, then pnpm package and the bundle swap. The bundle in /Applications does not carry these five fixes yet.", "git_commit": "9f650c1", "pushed": true, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "836 pass (64 files)", "e2e": "128 pass (36 files, logs/e2e-design.log)", "verify_completion": "203/204 at 9f650c1 (logs/verify-design.log); the only failure is the audit-milestone gate"}}
{"checkpoint_id": "cp-m7-alignment-pass", "iteration_hint": "drive every milestone-7 criterion from the running app with the demo library seeded, and fix where the letter passed but the vision did not", "started_at": "2026-08-01T19:40:00-0700", "finished_at": "2026-08-01T21:05:00-0700", "phase": "milestone-7", "actions_taken": ["drove all twenty-two criteria through throwaway tour specs (WR_BACKGROUND=1, demo filled, card-art cache seeded) and photographed each surface: the one-page notebook and its blocks under the hand, the journal pop-up and its page, the link picker's two stages, the ledger, the wiki whole/docked/focused, the librarian, the trash bin, search, the help page's command cards, the guide, the MH3 gallery, and both in-app drags. The specs were deleted before committing", "a double-click opened the same paper twice. `Workbench.navigate` computed its plan against the workspace as it stood and `describeEntity` is a round trip, so two activations of one thing overlapped, neither saw the other's panel, and both opened one -- on the map the duplicate even landed in a different group, so it did not read as a repeat. `navigate` now hands an identical in-flight navigation its own promise and queues everything else, so a second press plans against what the first produced. Two tests in workbench.test.ts, both failing before", "a pressed disc left a rectangle behind. The node is a `<g>` carrying the disc *and* its label, so the shared `:focus-visible` outline drew round the union of the two: a box enclosing a circle and three lines of quoted sentence, over the map, after the press. `.wr-graph__node:focus` is `outline: none` -- the disc's own rim is what focus looks like here (`.wr-graph__node:focus-visible .wr-graph__disc`), so no keyboard affordance is lost", "the status bar recited the kind the picker had stopped asking about: `Linked to \u201cX\u201d \u2014 related to`, which is also not a sentence. `createEntityLink` names the type only when somebody chose it, which today is exactly a claim's side (`E02`)", "the label halo the previous pass left in the tree was a `stroke`, and stroke is ink: it widened the glyphs' bounding box, which is inside the node's own button, and moved that button's box down off its disc into the gap where nothing takes a press. `[F02]` and `[H04]` both failed on it deterministically. It is a `text-shadow` now -- paints, is not geometry, same picture", "`describeLocation` answered `document` for a markdown anchor above the first heading, so a search hit on a marked sentence drew that noun in the column where every row beside it drew a heading path. Empty is the honest answer and both callers already handle it", "carried in the two fixes the interrupted design pass left unverified: a chunk's search row takes the file's title from `documents` on the way out (the indexer deliberately does not write it into the chunk's weighted `title` column), and `document-references-document` -- the commonest edge in a markdown library -- has a label, so the ledger says `references this` instead of `document references document this`"], "findings": ["left for the researcher: the wiki's labels still collide in the dense middle -- the halo lifts a label off an edge or a disc but two labels at the same place still interleave, and fixing that means placing labels rather than discs; the journal pop-up is nearly the whole window with one paragraph in it, which makes `Expand into a page` mean less than it should; the rail has three doors onto graphs (Graph, Wiki, Focus) and the neighbourhood panel is still its own surface with its own controls, which is a reading of `F05` the criterion's letter allows and its sentence does not; the help page draws the same `demo` picture for filling and clearing, deliberately (the drawing is the whole cycle), but they are opposite acts", "deliberately not changed: the demo papers are listed under the library's `Notes` heading beside the corpus folder's own, which `workspace.tsx` explains as a decision -- a fourth heading would be permanent chrome for something temporary. The count and the folder name sitting together still read as a claim about where all nine came from"], "criteria_advanced": [], "artifacts_created": [], "errors": [], "next_action": "The audit in reports/AUDIT.md, then pnpm package and the bundle swap.", "git_commit": "pending", "pushed": false, "state_version": 1, "gates": {"typecheck": "pass", "lint": "pass", "unit": "838 pass (64 files)", "e2e": "128 pass (36 files, logs/e2e-align2.log)", "verify_completion": "pending the checkpoint push"}}
{"checkpoint_id": "cp-m7-audit-close", "iteration_hint": "close the milestone-7 audit: fix every critical and major across four lenses, fold them into reports/AUDIT.md, record the minors", "started_at": "2026-08-01T20:42:00-0700", "finished_at": "2026-08-01T21:40:00-0700", "phase": "milestone-7", "actions_taken": ["P06 -- retireTheDesk read a notebook's cards through findReferences' 500-row default and then wrote notebook.deskRetired, which is checked before anything else at every later start: a notebook with more than 500 cards lost everything past the cap for good, and `added` cannot tell a partial run from a complete one. FindReferencesOptions.limit now takes null (SQLite LIMIT -1) and the two main-process callers that have to be exhaustive pass it; the channel schema still caps a renderer at 2000, so nothing on the bridge can ask for it. A 520-card notebook lands all 520 blocks, once each.", "P12 -- registerBlockSurface's disposer set inHand to null, so Cmd+S answered 'open a notebook page or a journal day first' over an open notebook page. Reachable in one gesture inside P09's own layout: page, journal pop-up over it, close the pop-up. The registry keeps recency order now and hands the hand back to whatever is still mounted. Five unit tests plus the P12 spec driving that exact sequence.", "P09 -- Expand carried only the questionId, so a pop-up open on a past day expanded into a page showing today, and the test asserted only on today's blocks. The day is WorkspaceState.journalDays now, shared by the two views; deliberately not on the descriptor (JournalPanelSchema says why) and not persisted. The spec reads back four days, writes there, expands, and asserts the page's own date and tab.", "B07/C02 -- demo:clear and purgeOutsideRoot each had their own copy of 'destroy a document', and neither swept search_entries, which has no foreign key to documents: 23 rows and 5 live hits survived a clear, unremovable from inside the app. LibraryRepository.purge is the one implementation now and both call sites use it; notes are swept by id since they carry no document_id.", "H07 -- a derived edge could be deleted from four surfaces. A wikilink came back on the next corpus scan silently (replaceDerived runs over every walked file at every launch); the annotation-belongs-to-document containment edge did not come back at all, so one press permanently took a marked sentence out of its own file's graph. unlinkRefusal in @wr/shared-types is the one predicate, link:delete refuses on it, and every surface draws its control dead with the reason on it.", "H07/G01 -- the invisible 12-unit edge hit-bands swallowed the pan: a press on one did nothing, and on the wiki's own defaults they cover the scene several times over. The pan starts on a band now and captures the pointer only past the shared 6px threshold, so the click underneath still reaches the line.", "tests -- workers 1 -> 4 with a per-workspace --user-data-dir (the one thing four Electron processes shared), and the timeouts capped: use.actionTimeout 10s (never inherited from expect, and where two real failures spent 30 of their 31 seconds), expect 10s, per-test 60s, the shell wait 30s. verify_completion.py's run() gained a streaming mode with a watchdog, and both suites run a progress reporter beside the JSON one, so a kill leaves evidence instead of 86 misleading tag failures.", "docs/LOOP.md gained the three-rung gate ladder and the two mistakes that each cost a full run.", "reports/AUDIT.md carries the milestone-7 section at the top with the only Audited-commit:/Audited-milestone: tokens; the milestone-6 section now says 'Audited commit (milestone 6):'. The 23 minors from four lenses are in state/experiment_state.json."], "criteria_advanced": [], "errors": [], "next_action": "pnpm package and the bundle swap.", "gates": {"typecheck": "pass", "lint": "pass", "tests": "851 pass (66 files, 6.3s)", "e2e": "129 pass (36 files) -- 246s before the worker change, 64s after; --repeat-each=3 clean at 387/387 in 161s", "verifier": "204/204 pass in 72.3s (was 255.8s at the milestone-6 close)"}, "git_commit": "c914de1", "pushed": true}
{"checkpoint_id": "cp-m7-ship", "iteration_hint": "package milestone 7, swap the bundle in /Applications the recorded way, and verify", "started_at": "2026-08-01T21:20:00-0700", "finished_at": "2026-08-01T21:45:00-0700", "phase": "milestone-7", "actions_taken": ["WR_BACKGROUND=1 pnpm package at 7574ed4 (clean tree, HEAD on origin/main) -> apps/desktop/release/mac-arm64/wiki-reader.app, 366M, logs/package-m7.log; electron-builder 25.1.8, electron 33.4.11, darwin arm64, unsigned as configured", "the swap, the recorded way: mv /Applications/wiki-reader.app /Applications/.wiki-reader-superseded-20260801-212540.app, then ditto the fresh bundle in. Nothing deleted - the researcher may be running the old one and holds its inodes until they restart. Nine superseded bundles on disk now, 3.1G against 173G free, so the pile is still cheaper than taking a running app out from under someone", "verify_completion.py green at 7574ed4 with the bundle installed: 204/204 in 72.0s, no failures (logs/verify-m7-1.log)", "state/NEXT_ACTION.md rewritten for whoever comes next: milestone 7 is closed and shipped, so the next context waits for the researcher's feedback and writes docs/MILESTONE8.md from it - Supersessions list included, because that list is the only thing that stops an audit calling a deliberate replacement a regression - and builds nothing ahead of that doc"], "findings": ["the packaged bundle is 366M in the tree and 353M installed; ditto drops the extended attributes electron-builder leaves behind, which is why the two numbers differ and why ditto rather than cp is the recorded verb", "the researcher had the app open across the swap: a process launched 17:25 was still running the milestone-6 bytes out of the moved-aside bundle at 21:25, holding its inodes. Until they quit and reopen, feedback about 'the app' is feedback about milestone 6 - recorded in NEXT_ACTION.md so the next context asks about a restart before believing a surface is missing"], "criteria_advanced": [], "artifacts_created": ["/Applications/wiki-reader.app (2026-08-01T21:25, built from 7574ed4)", "logs/package-m7.log", "logs/verify-m7-1.log", "logs/verify-m7-2.log"], "errors": [], "next_action": "Wait for the researcher's feedback; write docs/MILESTONE8.md from it.", "gates": {"package": "pass (WR_BACKGROUND=1, no window shown)", "bundle": "installed 2026-08-01T21:25:40 as /Applications/wiki-reader.app", "verifier": "204/204 pass in 72.0s at 7574ed4 with the bundle installed; 204/204 in 72.5s again at a34d80b, the checkpoint commit (logs/verify-m7-2.log)"}, "git_commit": "a34d80b", "pushed": true, "state_version": 1}
{"checkpoint_id": "cp-u12-title-bar-band", "iteration_hint": "fix U12: the researcher's tabs render too high", "started_at": "2026-08-01T22:20:00-0700", "finished_at": "2026-08-01T22:45:00-0700", "phase": "milestone-7-found-by-using-it", "actions_taken": ["drove the running app first (WR_BACKGROUND=1, two PDFs open) and measured the strip rather than guessing: the tab strip's rect was y=0..35, the activity bar's first button y=8, the sidebar heading y=0 -- the whole top edge of the app was drawn inside the band macOS keeps for the traffic lights on a `hiddenInset` window, whose native title bar is still there and still takes every press. That is 'tabs offset upward, clipped, overlapping the title bar region'", "found the cause rather than the symptom: `.wr-activity` already read `env(titlebar-area-height, 8px)`, so the shell had asked the platform how tall the band is -- but `titleBarOverlay` was never enabled, `env()` was undefined, and the only reader of it silently took its own 8px fallback while every other region took nothing at all. A probe with the flag on returned 38px and a titlebar-area rect of x=78..1440, which is macOS answering", "`titleBarOverlay: process.platform === 'darwin'` in `main/index.ts` so the variable resolves; `.wr-shell` reserves the band once at the root with `padding-top: env(titlebar-area-height, 0px)` and takes the chrome's background so the band reads as furniture; `.wr-activity`'s duplicate padding-top reverted to a plain 8px. Zero on Windows and Linux is correct there -- the frame is already outside the page", "`[U12]` in tests/e2e/tabs.spec.ts reads the band off `navigator.windowControlsOverlay` -- the window's own answer, never the CSS that is the fix -- then asserts the shell reserved at least it, the strip starts below it and is whole inside the viewport, every tab is contained in the strip at full height, `elementFromPoint` returns the tab at its drawn centre, top edge and leading edge, and a `page.mouse` press at the first tab's own rectangle activates it", "falsified the test twice, each probe restored with python and never `git checkout`: with the padding removed it fails on `reserved >= band`, and with that assertion removed as well it fails on `strip.top >= band`. The darwin branch also fails if `titleBarOverlay` is turned off, which is the main-process half"], "criteria_advanced": ["U12"], "errors": ["`git checkout-index -f apps/desktop/src/renderer/shell.css` to undo a probe took the file back to HEAD and threw away the fix, exactly as the trap in NEXT_ACTION.md says it would. Re-applied from the edit text; every later probe was made and unmade with python"], "gates": {"typecheck": "pass", "lint": "pass", "tests": "851 pass (66 files)", "e2e": "130 pass (36 files, 60s at workers=4) -- run whole because the fix moves every coordinate in the shell down 38px", "verifier": "not run: it names H10, F08 and F09, which are not built"}, "next_action": "H10, F08, F09 -- the rest of milestone 7's found-by-using-it list. The /Applications bundle is still the 2026-08-01T21:25 build, so U12 is not yet in the researcher's hands.", "git_commit": "2428982192e655081d2ce5ed5f9dface55a82be6", "pushed": true}
{"checkpoint_id": "cp-h10-marks-in-the-bytes", "iteration_hint": "H10: a highlight made on a saved web page is painted on the page itself", "started_at": "2026-08-01T22:45:00-0700", "finished_at": "2026-08-01T23:00:00-0700", "phase": "milestone-7-found-by-using-it", "actions_taken": ["read the reader first: the archive is framed with `sandbox` and no tokens, so it has no script and an opaque origin, and the article panel listed its highlights *beside* the page with a comment saying painting one would need script in the frame. The spec note points at the other way round -- mark the snapshot at serve time in the main process -- so the transport is bytes, not messages, and the frame gains nothing", "made the extraction carry its own provenance rather than writing a second scanner: `extractHtmlTextWithSource` in @wr/document-model returns the text plus, per UTF-16 unit, the half-open range of the HTML it came from, and `extractHtmlText` is now that function's `.text`. `normalizeTextWithSource` is the mapped twin of `normalizeText`, step for step, off the same fold tables -- which are now data (ZERO_WIDTH_CHARS, UNICODE_SPACE_CHARS, PUNCTUATION_FOLD_TABLE) with the regexes derived from them, so one alphabet answers for one NORMALIZATION_VERSION", "`markSnapshotHtml` (packages/document-model/src/html-mark.ts) turns resolved offsets into <mark> runs cut at every tag boundary, wraps a character reference whole, merges only across gaps holding no angle bracket, clips overlapping highlights instead of nesting them, and injects one inline <style> -- which the CSP served with an archive already allows and which needs no relaxation of anything. It refuses outright when the mapped text is not identical to normalizeText(extractHtmlText(html))", "`paintSnapshotHighlights` in main/protocol.ts resolves every html anchor of the addressed file with readerMode 'original' and snapshotHash = file.contentHash -- the same call the panel makes -- and serves the marked copy; the archive on disk is never touched. Only a snapshot *entry* page is painted (`isSnapshotEntry`), never a resource inside it, and an entry page is now `cache-control: no-store` because its bytes are a view of the database as much as of the file", "HtmlReaderView takes `marks` (an opaque revision) and `focusedMarkId`. The query is a cache-buster and nothing else: what is painted is decided where the bytes are. A ref holds the revision the frame first loaded with, so a page opened with highlights already on it does not reload a moment after appearing -- the first load is already painted", "[H10] in tests/e2e/webpage.spec.ts drives it end to end: select in the frame, right-click, Highlight, then assert one <mark> whose text is the quoted words, on the paragraph, with the palette's own background, and `document.title` still the page's own -- the inline script that renames it still did not run. Restart, reopen, still painted", "[H10] x5 in tests/integration/snapshot-protocol.test.ts for the refusals: the page saved again without the sentence paints the *other* highlight and nothing where the lost one was, an anchor claiming the readability rendering paints nothing, a resource inside the snapshot is never painted, and a reopened database serves the same marks", "guide: the `mark` chapter now says a saved page raises no bar, that the gesture is a right-click, and that the mark lands on the page itself"], "findings": ["the alignment has to be exact or it must not happen at all, so the map is verified against `normalizeText` itself before a single tag is inserted -- an unmarked page is a disappointment, a mark on the wrong sentence is a claim about what the researcher read", "a mark that crossed a tag would be a mark the browser is entitled to repair somewhere else, so runs are cut at every boundary in the source's own character data: a sentence spanning an <em> is three <mark> elements and one element id", "`no-misleading-character-class` rejects a zero-width joiner inside a character class, which is why ZERO_WIDTH is built as an alternation from its table rather than as `[...]`", "marking is text-transparent: <mark> is inline and <style> is a raw-text element, so `normalizeText(extractHtmlText(marked))` is unchanged and every other anchor on the page still points where it did. Asserted, not assumed", "[UX07] (a saved page is not reloaded when the workspace re-renders around it) still passes -- the frame URL only changes when the painted set does"], "criteria_advanced": ["H10"], "artifacts_created": ["packages/document-model/src/html-mark.ts", "packages/document-model/src/html-mark.test.ts"], "errors": [], "gates": {"typecheck": "pass", "lint": "pass", "tests": "866 pass (67 files)", "e2e": "131 pass (37 files, 60s at workers=4). Run whole rather than by tag: `protocol.ts` is on the path of every `rrfile://` response, not only an archived page\u2019s", "verifier": "not run: it names F08 and F09, which are not built"}, "next_action": "F08 (the graph lays out by force -- nodes push apart and none overlap at rest), then F09 (focusing reframes and de-emphasizes; nothing is hidden).", "state_version": 1}
{"checkpoint_id": "cp-f08-f09-force-and-focus", "iteration_hint": "F08 and F09: the graph lays out by force, and focusing stops hiding the map", "started_at": "2026-08-01T23:00:00-0700", "finished_at": "2026-08-01T23:40:00-0700", "phase": "milestone-7-found-by-using-it", "actions_taken": ["measured Cytoscape's own force layout before writing anything: `cose` headless over 150 nodes ran in ~155ms but returned different positions on two identical runs (it draws from Math.random), and with `styleEnabled: false` it has no node sizes at all -- which is precisely the half of F08 that says 'none overlap'. So the ranked spiral survives as the deterministic *seed* and `forcePositions` in @wr/graph relaxes it: repulsion every pair, a spring with a rest length along every link, gravity, linear cooling, then a separation phase that only ever moves two discs apart", "the separation is what makes 'none overlap' a promise rather than a tendency, and it needed three fixes found by measurement, not by reading. (1) The forces must run with no walls: clamping during them stacked a crowded library into a boundary layer against the frame, and the separation cannot dig out of that because the direction it must push is the direction the wall is -- 292 overlapping pairs at 300 nodes became 0 by moving the containment to a single pass afterwards. (2) A pair that cannot take its half of a separation hands the remainder to the other end. (3) An axis-aligned check before the square root, because almost every pair on a 300-node map is nowhere near touching and this phase looks at every pair 400 times: 807ms -> 497ms", "`frameInto` (not `fitInto`, which is graph-canvas's) frames an unpinned arrangement with one translation and one scale over everything at once, so the relations the forces found survive exactly. The room each disc needs comes off the box *before* the scale is chosen: folding it into the span overflows whenever the scale is under one, which drew a hub fourteen units past the bottom of the scene -- half off its own panel, with the point a hand aims at outside the canvas. `[H04]` caught it by failing to click a node in the picker's map", "a held node (V01) is carried by its holder at exactly the offset the seed gave it and never relaxed; the holder's radius is widened to cover the whole ring instead, and the satellite ring widens with the number of sentences on it. That is what keeps `[V01] [F06]`'s 'a highlight is drawn at its paper, under 60 units away' true through a force layout", "F09: `FocusPanelBody` now asks `graph:overview` as well as `graph:focus` and draws the rest of the library round the two bands, dimmed, clickable, and crawlable. The band is *pinned* in the relaxation, so F02's geometry -- every highlight nearer the middle than every connected file, the box holding the file and what it says and nothing it reaches -- is untouched by construction rather than by luck", "`--faded` is deliberately a second dimming, not the filter's: `--dimmed` says 'not what you typed' and `--faded` says 'not what the view is about'. A node can be both and the two multiply. One class for both would have made V02 and F09 unable to disagree, and V02's 'nothing is dimmed once the box is cleared' would have been false the moment focus drew a corpus", "`[F08]` in tests/e2e/wiki.spec.ts reads every disc's published position and its drawn `r` and asserts no pair overlaps, no disc hangs off the scene, the two pages the startup scan joined by a wikilink are nearer each other than the average pair, and the map comes back identical after being asked for at another size and back", "`[F09]` asserts every file in the library is on the focused view, that one which is neither the subject nor a neighbour is `data-role=context`, `data-faded=true` and computed fainter than the centre while still `data-match=true`, that the focused disc is within two pixels of the middle of the panel, that F02's bands still read, that nothing on the view overlaps, and that clicking out into the dimmed corpus re-centres the view there with the file just left behind still drawn", "guide: the map chapter says the layout is by force and that focusing hides nothing, and gains a step for walking back out into the dimmed library"], "findings": ["the fit a panel holds (F04) is captured once and released only by Reset view, so 'the focused disc is at the centre of the canvas' is only true in pixels while the panel's own box has not changed. After a crawl the header says a different number of things and the panel is a few pixels taller, so that assertion is made in the scene's own units instead -- same claim, no dependence on a fit that is deliberately stale", "the textbook `d^2/k` attraction has no length it is happy at: with few edges it drew every linked pair until the two discs touched, which would have left `[F04]`'s 'two discs are more than 20px apart' resting on the separation minimum. A spring with a rest length -- slack below it, pulling only above -- puts a linked pair at ~85 scene units on this corpus against ~530 for the average pair", "nothing in the layout reads a clock or a random number, so `expect(forcePositions(a,b,c)).toEqual(forcePositions(a,b,c))` holds and the E2E can compare every disc across two redraws"], "criteria_advanced": ["F08", "F09"], "artifacts_created": [], "errors": [], "gates": {"typecheck": "pass", "lint": "pass", "tests": "874 pass (67 files)", "e2e": "133 pass (37 files, 60s at workers=4). Run whole rather than by tag: the layout moves every coordinate on three graph surfaces and the link picker's map is one of them", "verifier": "not run in this checkpoint -- it *is* the full e2e run, and the ladder forbids both at once"}, "next_action": "python3 scripts/verify_completion.py, then the independent audit per docs/LOOP.md, then swap /Applications (still the 2026-08-01T21:25 build).", "state_version": 1}
{"checkpoint_id": "cp-drive-the-four-found-by-using-it", "iteration_hint": "drove U12, H10, F08 and F09 in the built app with the demo library seeded, and fixed what the screenshots showed", "started_at": "2026-08-01T23:40:00-0700", "finished_at": "2026-08-02T00:20:00-0700", "phase": "milestone-7-found-by-using-it", "actions_taken": ["drove the built app (WR_BACKGROUND=1, throwaway tour spec since deleted) through every arrangement the four criteria live in: one tab, five tabs, two groups, three groups, the annotations column open, the wiki as a page and docked; a saved page marked and then read at half width; the wiki at 60/150/300 with labels on and off; the focused view docked, widened and crawled. Screenshots read by eye rather than asserted on", "U12 miss: an overflowing tab strip lost 11px of every tab to a scrollbar. Dockview asks for a 3px overlay bar and Chromium honours neither half of the request -- `overflow: overlay` is gone, and `::-webkit-scrollbar` is ignored wherever `scrollbar-width` is declared -- so the fifth tab in a group brought a classic bar into being and that group's tabs were drawn shorter than the group's beside it. `scrollbar-width: none` on `.dv-tabs-container` in shell.css; the strip still scrolls and dockview still brings the active tab into view", "H10 miss: the highlight chips in the strip beside a saved page are painted in the highlight's own colour -- a paper colour -- and drew their quote in `--wr-text`, which is chosen for the chrome's near-black. 1.3:1: in the DOM, invisible on the screen, which is the one bug the second ink scale exists to prevent. `--wr-ink` on `--wr-ink-border` now", "F09/F04 miss: `SceneFit` held its two offsets along with its scale, and the offsets are measured from the panel's top-left corner, so a panel that grew put every new pixel down the right-hand side. Dragging a docked wiki back into the middle left the map in the corner with the focused file 271px off centre. `centredFit` recomputes the offsets on every resize and holds the scale, which is the half `F04` is about", "found while driving: `.wr-reader-actions` did not wrap, so at half the window -- two readers side by side, the arrangement this app is for -- the strip's fourth control was drawn past the panel's edge with nothing to scroll it back. It wraps now", "tests: `[U12]` fills a strip to overflowing and asserts no bar is in its layout and every tab is the height it was with two; `[H10]` measures the chip's contrast; `[F09]` resizes the panel by hiding the library sidebar and asserts the focused file is still within two pixels of the middle with `data-fit` unchanged. `tests/e2e/support/contrast.ts` is `[UX01]`'s arithmetic, moved out and shared rather than copied"], "findings": ["the four fixes read right to a human at every arrangement: the strip sits below the band and every tab is hit-able at its drawn centre, a mark is painted over its own sentence and stays there at half width and after a restart, the discs stand apart at 60/150/300 and the map is the same map each time, and focusing centres while the rest of the library stays drawn and clickable", "`data-fit` publishes the scale alone, which is why re-centring the offsets leaves `[F04]`'s 'docked keeps its scale' assertion exactly where it was", "left for the researcher, not fixed: the wiki's labels still interleave, and a wrapped quote label (`F06`) is drawn across whatever disc is beside it. Placing labels rather than discs is the real answer and it is a design decision, not a pass's"], "criteria_advanced": [], "artifacts_created": ["tests/e2e/support/contrast.ts"], "errors": [], "gates": {"typecheck": "pass", "lint": "pass", "tests": "874 pass (67 files)", "e2e": "targeted: wiki/graph/links 22 pass, U12+H10+UX 12 pass, reader/markdown/librarian/shell-chrome/reading-surface 18 pass. The whole suite is the verifier's run, which is next", "verifier": "not run in this checkpoint -- it *is* the full e2e run, and the ladder forbids both at once"}, "next_action": "python3 scripts/verify_completion.py, then the independent audit per docs/LOOP.md, then swap /Applications (still the 2026-08-01T21:25 build).", "state_version": 1}
{"checkpoint_id": "cp-verify-after-the-driving-pass", "iteration_hint": "the verifier, over the four found-by-using-it criteria and the four misses driving them turned up", "started_at": "2026-08-02T00:20:00-0700", "finished_at": "2026-08-02T00:30:00-0700", "phase": "milestone-7-found-by-using-it", "actions_taken": ["ran `python3 scripts/verify_completion.py` at 0a20de1 with the tree clean and HEAD on origin/main: 208/208 in 73.1s, up from 204/204 in 72.0s at the milestone-7 close -- the four extra checks are U12, H10, F08 and F09"], "findings": ["the audit gate passes on ancestry alone: reports/AUDIT.md answers for a912606, which is before U12, H10, F08 and F09 were written, so no auditor has read any of the four. That is the next rung, not the swap"], "criteria_advanced": [], "artifacts_created": [], "errors": [], "gates": {"typecheck": "pass", "lint": "pass", "tests": "874 pass (67 files)", "e2e": "run by the verifier", "verifier": "208/208 in 73.1s"}, "next_action": "the independent audit per docs/LOOP.md over the four found-by-using-it criteria, then swap /Applications (still the 2026-08-01T21:25 build).", "state_version": 1}
{"checkpoint_id": "cp-ship-the-four-found-by-using-it", "iteration_hint": "packaged, swapped /Applications, and verified: the four found-by-using-it criteria reach the researcher", "started_at": "2026-08-02T00:00:00-0400", "finished_at": "2026-08-02T00:12:00-0400", "phase": "milestone-7-found-by-using-it", "actions_taken": ["WR_BACKGROUND=1 pnpm package at e51983f -> apps/desktop/release/mac-arm64/wiki-reader.app (electron-builder 25.1.8, electron 33.4.11, arm64, unsigned by configuration). Checked the built bytes rather than trusting the exit code: out/main/index.js carries markSnapshotHtml (H10), the renderer chunk carries forcePositions (F08), and the renderer CSS carries env(titlebar-area-height) and one scrollbar-width:none (U12)", "swapped the recorded way: mv /Applications/wiki-reader.app -> /Applications/.wiki-reader-superseded-20260802-000114.app (never deleted -- the researcher may still be running out of it and holds its inodes until they restart), then ditto'd the fresh bundle in. 353M installed, ten superseded bundles now on disk (~3.5G, 173G free)", "python3 scripts/verify_completion.py with the tree clean and HEAD on origin/main: 208/208, checks_failed 0, exit 0, duration_seconds 75.3 -- 874 vitest tests and 133 e2e specs, 0 failed in either. It was 208/208 in 73.1s at 0a20de1 and 204/204 in 72.0s at the milestone-7 close", "updated state/NEXT_ACTION.md and state/experiment_state.json: the bundle in /Applications is the 2026-08-02T00:00 build, so U12, H10, F08 and F09 are in the researcher's hands; the swap paragraph now records the tenth superseded bundle and says plainly that a swap is not a restart"], "findings": ["the audit gate is still passing on ancestry alone: reports/AUDIT.md answers for a912606, which predates U12, H10, F08 and F09, so the verifier is green over four criteria no auditor has read. The ship went first on purpose -- all four are fixes to things that were wrong in the researcher's hands -- but no milestone-complete promise can follow until that audit closes clean", "the verifier's wall clock is holding: 72.0s -> 73.1s -> 75.3s across the four criteria and the four misses, against 255.8s at the milestone-6 close"], "criteria_advanced": [], "artifacts_created": ["/Applications/wiki-reader.app (2026-08-02T00:00, built from e51983f)", "/Applications/.wiki-reader-superseded-20260802-000114.app", "logs/package-20260802-000044.log"], "errors": [], "gates": {"typecheck": "pass", "lint": "pass", "tests": "874 pass (67 files)", "e2e": "133 pass, run by the verifier", "verifier": "208/208 in 75.3s, exit 0"}, "next_action": "the independent audit per docs/LOOP.md over U12, H10, F08 and F09, then re-read the promise gate.", "git_commit": null, "pushed": false, "state_version": 1}
