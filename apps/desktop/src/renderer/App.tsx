/**
 * Application shell.
 *
 * Milestone 1 replaces this body with the Dockview workbench from @wr/workbench
 * (criterion M02): activity bar, library sidebar, Dockview main area, annotation sidebar,
 * bottom panel, status bar.
 */
export function App(): JSX.Element {
  return (
    <div data-testid="app-shell" className="app-shell">
      <h1>wiki-reader</h1>
      <p>Workbench not yet mounted — see docs/MILESTONE.md criterion M02.</p>
    </div>
  );
}
