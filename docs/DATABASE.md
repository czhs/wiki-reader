# DATABASE

This document is required by `scripts/verify_completion.py` and must be written
as part of milestone 1. See `docs/SPEC.md` and `README.md` for the authoritative
content to expand here.

Status: **not yet written**. The completion verifier requires it to exceed 200 bytes
and be internally consistent with the implementation, so filling it in with filler text
is not sufficient — it must describe what the code actually does.
